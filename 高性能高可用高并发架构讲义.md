
# 高性能、高可用、高并发架构讲义

> 本讲义系统讲解分布式系统三大非功能性目标：**高性能（Performance）、高可用（Availability）、高并发（Concurrency）**，及其设计原则、常见模式、典型方案与实战权衡。每章配"知识点 + 笔试题"。
>
> 约定：以互联网/云原生场景为背景；示例技术栈跨语言通用，设计原则优先于具体工具。

## 目录

1. [三高的概念与关系](#1-三高的概念与关系)
2. [衡量指标与基线](#2-衡量指标与基线)
3. [CAP、BASE 与一致性谱系](#3-capbase-与一致性谱系)
4. [通用设计原则](#4-通用设计原则)
5. [高性能：延迟优化](#5-高性能延迟优化)
6. [高性能：吞吐量优化](#6-高性能吞吐量优化)
7. [高并发：连接、线程与异步](#7-高并发连接线程与异步)
8. [缓存体系与热点治理](#8-缓存体系与热点治理)
9. [数据库扩展：读写分离、分库分表](#9-数据库扩展读写分离分库分表)
10. [异步化、消息队列与事件驱动](#10-异步化消息队列与事件驱动)
11. [高可用：冗余、故障转移与隔离](#11-高可用冗余故障转移与隔离)
12. [流量治理：限流、熔断、降级、排队](#12-流量治理限流熔断降级排队)
13. [一致性与分布式事务](#13-一致性与分布式事务)
14. [秒杀 / 抢购 / 红包案例](#14-秒杀--抢购--红包案例)
15. [Feed / 推送 / IM 案例](#15-feed--推送--im-案例)
16. [多机房 / 异地多活](#16-多机房--异地多活)
17. [容量规划与压测](#17-容量规划与压测)
18. [常见反模式与权衡](#18-常见反模式与权衡)
19. [综合笔试练习](#19-综合笔试练习)

---

## 1. 三高的概念与关系

### 1.1 定义

- **高性能（Performance）**：单位时间处理更多请求、更低延迟
- **高可用（Availability）**：长时间对外服务，故障时快速恢复
- **高并发（Concurrency）**：能同时处理大量请求 / 连接 / 用户

它们**不是彼此独立**：高并发是达到高吞吐的手段；高性能支撑高可用（慢系统更易被拖垮）；冗余为可用性带来额外开销。

### 1.2 "三高"与成本的权衡

任何非功能性目标都在与**成本与复杂度**交换：

```
质量目标↑  →  硬件/人力/运维复杂度↑
```

**务实原则**：

- 按业务价值**分层投入**（核心链路 > 边缘功能）
- 设计 **SLO**（目标）而非追求极致
- 先把"对"的架构搭起来，再按瓶颈精准优化

### 1.3 常见误区

- "只要代码写得好就能高并发" —— 架构比代码更重要
- "上云 / 上 K8s 自动高可用" —— 工具是手段，设计才是关键
- "缓存能解决一切" —— 错缓存比没缓存更糟
- "异步化能提高性能" —— 异步是解耦而非加速，链路仍存在
- "追求零 downtime" —— 成本指数级上升，99.95%-99.99% 是多数业务合理目标

### 📝 笔试题 1-1：高并发与高性能的区别？

- **高性能**关注**单次**请求的速度（延迟、吞吐）
- **高并发**关注**同时**处理多少请求/连接/用户

一个单机单线程也能有高性能（低延迟），但不高并发；一个扛着百万连接但每秒只能处理几千请求的系统是高并发但不高性能。两者常同时追求，但**优化手段不同**：高性能靠减少/优化路径，高并发靠扩展 + 异步 + 资源隔离。

---

## 2. 衡量指标与基线

### 2.1 性能指标

- **吞吐量（Throughput）**：QPS / TPS / RPS
- **延迟（Latency）**：P50 / P90 / P95 / P99 / P999
- **并发数（Concurrency）**：同时在途请求数
- **饱和度（Saturation）**：排队长度 / 资源使用率

关键：**关注尾部延迟**，平均值会掩盖用户真实体验。

### 2.2 可用性指标

- **SLA / SLO / SLI**（见下章）
- **MTBF**（平均无故障时间）
- **MTTR**（平均恢复时间）
- **RTO / RPO**：故障恢复时间目标 / 数据丢失目标

```
99.9%   (三个九)  →  每月停机 ≈ 43.2 分钟
99.95%  (三个半九)→  每月停机 ≈ 21.6 分钟
99.99%  (四个九)  →  每月停机 ≈ 4.32 分钟
99.999% (五个九)  →  每月停机 ≈ 26 秒
```

### 2.3 并发指标

- **在线用户数**、**活跃连接数**
- **每秒新连接数（CPS）**
- **长连接数量**
- **线程 / 协程数**

### 2.4 Little's Law

```
L = λ × W
```

- **L**：系统内平均请求数（并发）
- **λ**：到达率（QPS）
- **W**：平均响应时间

**启示**：

- QPS × 延迟 = 并发；要提 QPS 就要**降低延迟**或**提升并发能力**
- 并发数超过系统处理能力 → 排队 → 延迟飙升（雪崩前兆）

### 2.5 金字塔：延迟层级（数量级）

```
L1 cache       ~1 ns
L2 cache       ~10 ns
Main memory    ~100 ns
SSD random     ~100 μs
SSD sequential ~1 ms
Network (LAN)  ~0.5 ms
Disk seek      ~10 ms
Network (WAN)  ~150 ms+
```

设计时预估"每次调用多远"决定延迟上限。

### 📝 笔试题 2-1：为什么"平均延迟 100 ms"的系统仍可能有严重体验问题？

长尾：P99 可能 2 s、P999 可能 5 s。用户每次请求**被最慢那条路径主宰**，尤其并发多次请求时（一个页面往往 10+ 次 API），**用户感受 ≈ 最慢请求的延迟**。所以生产上以 **P95/P99** 为目标，必要时看 P999 或分位直方图。

---

## 3. CAP、BASE 与一致性谱系

### 3.1 CAP 定理

分布式系统三者**无法同时兼得**：

- **C**onsistency：所有节点看到一致数据
- **A**vailability：每个请求都有响应
- **P**artition tolerance：容忍网络分区

**分布式=P 必选**，在 CP 与 AP 之间抉择：

- **CP**：ZooKeeper、etcd、HBase、RDB 强一致复制
- **AP**：Cassandra、DynamoDB、Eureka、DNS
- 不是完全二选一，多数系统在特定操作上调权衡

### 3.2 BASE

NoSQL 的务实替代：

- **B**asically Available
- **S**oft state
- **E**ventual consistency

接受短暂不一致，用异步复制/最终一致换取可用性与伸缩性。

### 3.3 一致性谱系

从强到弱：

```
线性一致 (Linearizable)
  ↓
顺序一致 (Sequential)
  ↓
因果一致 (Causal)
  ↓
读己之写 (Read-your-writes)
  ↓
单调读 (Monotonic Reads)
  ↓
最终一致 (Eventual)
```

不同业务选不同层级。支付强一致，点赞最终一致足矣。

### 3.4 PACELC

扩展 CAP：**没有分区时（ELC）** 也有权衡。**E**lse **L**atency vs **C**onsistency：

- 同一系统同步强一致 → 延迟上升
- 异步复制 → 延迟下降但短暂不一致

如 Spanner 选 CP/EC，Cassandra 选 AP/EL。

### 📝 笔试题 3-1：秒杀扣库存用 CP 还是 AP？

**通常 CP**：不能超卖。实际实现常常把**扣减计数**放在**单一强一致存储**（Redis + Lua，或 DB 乐观锁）保证原子性；订单、通知、发货等后续流程走 AP 最终一致。

---

## 4. 通用设计原则

### 4.1 高层原则

1. **无状态化**：便于水平扩展
2. **分层解耦**：接入 / 网关 / 应用 / 数据 / 存储
3. **冗余与隔离**：副本、舱壁
4. **按需放大**：热路径性能优化，冷路径保持简单
5. **为失败设计**：任何依赖都会挂
6. **异步化 & 最终一致**：松耦合换可扩展
7. **观测先行**：看不到的系统无法优化
8. **按访问模式建模**：读多写少 / 写多读少 / 点查 / 范围 / 聚合

### 4.2 规模通用手段清单

- **水平扩展 > 垂直扩展**
- **缓存**
- **分片 / 分区**
- **异步 / 批量 / 流水线**
- **读写分离**
- **CDN / 边缘计算**
- **压缩 / 协议优化**（Protobuf、HTTP/2、gRPC）
- **长连接复用**
- **就近访问 / 多机房**

### 4.3 Amdahl 定律（并行化天花板）

```
S = 1 / ((1-P) + P/N)
```

- P：可并行部分比例，N：并行度
- 若串行部分 5%（P=95%），N→∞ 时加速极限 = 20 倍

**启示**：**先消除串行瓶颈**，再加并行；锁、单点、集中式协调经常是"5% 串行"的根源。

### 4.4 USE 与 RED 方法

- **RED**（面向服务）：Rate / Errors / Duration
- **USE**（面向资源）：Utilization / Saturation / Errors
- **四大黄金信号**：Latency / Traffic / Errors / Saturation

组合使用构建监控与容量视角。

### 📝 笔试题 4-1：水平扩展优于垂直扩展的原因？

- **线性成本**：多买几台同规格比一台顶级机器便宜
- **容错**：天然冗余，单点故障影响小
- **无上限**：垂直扩展受单机物理极限
- **弹性**：易按需扩缩容
- 缺点：需要应用**无状态化** + 分布式协调，复杂度提升

单机易到瓶颈但代码简单；水平扩展复杂度高但更具可伸缩性。所以"先优化单机再加机器"的说法对开发阶段成立；架构规划阶段应优先水平扩展的可能性。

---

## 5. 高性能：延迟优化

### 5.1 定位瓶颈的思路

"性能问题先测量，后优化"：

1. 指标：P99 在哪个接口、哪个环节
2. 链路追踪：具体 span 慢在哪一跳
3. 资源：CPU（算）/ IO / 锁 / GC
4. 热点代码：Profile（火焰图）

### 5.2 常见延迟来源与对策

| 来源 | 对策 |
|------|------|
| CPU 密集算法 | 算法优化 / 原生库 / GPU |
| 频繁 GC | 对象复用、off-heap、调参 |
| 锁竞争 | 无锁数据结构、分段锁、CAS、减少临界区 |
| 远程调用 | 本地缓存、批量、并行调用 |
| 数据库慢查询 | 索引、查询重写、读写分离、缓存 |
| 序列化开销 | 二进制协议（Protobuf/MsgPack） |
| 过多网络往返 | 合并、批量、HTTP/2 多路复用 |
| 队头阻塞 | 多连接池、并发限制分舱 |
| 日志同步 IO | 异步日志、合并刷盘 |

### 5.3 并行化请求

```
传统串行：
  A → B → C → D     总耗时 = a + b + c + d

并行：
  A ┐
  B ┼→ 合并         总耗时 = max(a, b, c) + d
  C ┘
```

关键：**识别可并行的独立调用**。Java `CompletableFuture`、Node.js `Promise.all`、Go goroutine 等。

### 5.4 批量化 & 流水线

- 批量 DB 查询：`IN (...)` 取代 N 次 `=`
- 批量写入：累积后一次 flush
- RPC 批接口：减少握手 & 网络 roundtrip
- **Pipeline（Redis）**：攒一批命令一次网络
- **Batch Consumer**：MQ 消息批量处理

### 5.5 网络与协议

- **HTTP/2 多路复用**：单 TCP 连接多流，消除队头阻塞
- **HTTP/3 (QUIC)**：UDP 上的现代传输，抗丢包
- **gRPC / Protobuf**：二进制、流式
- **Keepalive + 连接池**：避免每请求握手
- **TLS 会话复用 / 0-RTT**
- **Brotli / zstd 压缩**

### 5.6 数据结构与内存

- 预分配集合容量，避免反复扩容
- 用**数组** / **紧凑结构**代替大量小对象（减少 GC）
- **对象池**（连接、buffer、worker）
- **Flyweight**、享元
- 合理**数据局部性**（cache line、false sharing）

### 5.7 常用优化工具

- **性能剖析**：perf、async-profiler、py-spy、pprof、flamegraph
- **APM**：DataDog / New Relic / SkyWalking
- **基准**：JMH（Java）、benchmark.js、pytest-benchmark、wrk / hyperfine

### 📝 笔试题 5-1：接口 P99 突然从 100ms 涨到 1s，排查链路？

1. **看监控**：哪一时刻开始？哪个实例？
2. **看 Trace**：具体慢在哪个 span（DB / RPC / 自身）
3. **资源**：CPU / GC / 连接池 / 线程池饱和？
4. **依赖**：DB / Redis / 下游 RPC 是否同时变慢？
5. **近期变更**：代码发布？索引变化？数据量激增？
6. **网络**：是否跨机房 / 网关抖动？
7. **排除**：先回滚最近变更；若是外因则扩容 + 限流保护

系统化而非"猜"。

---

## 6. 高性能：吞吐量优化

### 6.1 吞吐量 vs 延迟的权衡

二者常冲突：

- 批量化 → 吞吐 ↑、单请求延迟 ↑
- 减少等待 → 延迟 ↓、资源利用率可能 ↓
- 异步提交 → 客户端响应快，后端吞吐可控

**按业务选 SLO 而非两头都拉满**。

### 6.2 吞吐提升手段

- **水平扩展**：多实例
- **异步 / 流水线 / 批量**
- **压缩**：网络吞吐瓶颈时有效，CPU 瓶颈时反效
- **零拷贝**：`sendfile`、`splice`、`mmap`
- **减少每请求的工作量**：预计算、物化视图
- **读写分离**：读从写主
- **缓存**：少做重复工作
- **SIMD / 向量化**：计算密集型

### 6.3 零拷贝示例

传统 IO：

```
disk → kernel buffer → user buffer → kernel socket buffer → nic
         (拷贝 × 2-4 次, 上下文切换多次)
```

`sendfile`：

```
disk → kernel buffer → nic  (拷贝 1-2 次, 仅内核态)
```

Kafka、Nginx 大量用 `sendfile` 提升吞吐。

### 6.4 Amdahl 与 Gustafson

- **Amdahl**：固定问题规模下的加速上限
- **Gustafson**：问题规模随并行度增长时，加速近乎线性

**现实含义**：在"规模越大越值得"的场景（大数据、批处理），并行化收益高；在"规模固定"的低延迟请求链，优化串行比扩并行更重要。

### 6.5 吞吐瓶颈定位

- 查找**饱和的单一资源**：CPU / 内存带宽 / 网卡 / 磁盘 IO / 连接数 / DB 连接池 / 锁
- 单点瓶颈不解，加再多并行都没用
- 数据库常是最后的瓶颈：读写分离、缓存、分片依次上

### 📝 笔试题 6-1：批量写入数据库明显比逐条快，为什么？

- 减少**事务开销**（commit 次数）
- 减少**网络 RTT**
- 批量走数据库的**批量路径**（减少解析、锁获取）
- InnoDB 可**合并 redo log 刷盘**
- 索引批量更新比逐条重建更优

代价：批量失败时粒度大，需要幂等与重试设计。

---

## 7. 高并发：连接、线程与异步

### 7.1 经典 IO 模型

- **BIO（阻塞）**：一连接一线程，C10K 问题严重
- **NIO（多路复用）**：epoll / kqueue，一线程管大量连接
- **AIO / Proactor**：内核完成后通知
- **协程（goroutine / async）**：用户态轻量线程，语言/运行时调度

### 7.2 C10K → C10M

C10K 问题（1999）提出：单机能否处理一万连接。现代技术解决后目标演进到 C10M：

- 事件驱动 + 非阻塞 IO
- 每连接状态最小化
- 绕过内核（DPDK / io_uring）
- 无锁数据结构 + 多队列网卡
- NUMA-aware、CPU 绑核

### 7.3 线程模型

- **Worker 线程池**（CPU 密集）
- **IO 线程池**（IO 密集，非阻塞更好）
- **Reactor 单线程 / 多线程 / 主从多 Reactor**（Netty 默认）
- **Actor**（Akka、Erlang）：消息驱动隔离
- **协程**（Go、Kotlin、Python async、Loom）：降低切换成本

### 7.4 异步编程范式

```
回调（Callback）         低级，层层嵌套
Promise / Future         链式组合
async/await              同步风格写异步
Reactive Streams         背压（RxJava、Project Reactor）
协程                     同步代码，调度器挂起
```

常见对比：

- **Java**：Future + CompletionStage / Reactor / Loom 虚拟线程
- **Node.js**：Promise / async-await / streams
- **Python**：asyncio / trio
- **Go**：goroutine + channel
- **Rust**：async/.await + tokio

### 7.5 池化资源

**数据库 / HTTP / Redis / Kafka**客户端都应用**连接池**：

- 预热（预建连接）
- 空闲超时回收
- 最大连接上限
- 取得连接有超时，避免拖死上游

常见参数 DB 连接池（HikariCP）：

```
maximumPoolSize       最大连接
minimumIdle           最小空闲
connectionTimeout     取连接超时
idleTimeout           空闲多久回收
maxLifetime           连接最大生命周期（小于 DB wait_timeout）
```

### 7.6 背压（Backpressure）

下游处理不过来时，上游必须**慢下来**：

- **拉模式**（Kafka、Reactive）：下游按能力拉
- **限流**：主动丢弃或排队
- **队列**：短时缓冲，长时过载必须拒绝

### 📝 笔试题 7-1：Java 的虚拟线程（Loom）与传统线程区别？

- **传统平台线程**：与 OS 线程 1:1，栈大（默认 1MB），数量上限几千
- **虚拟线程**：用户态调度（由 JVM 在少量 carrier 线程上调度），栈按需扩展，百万级轻松
- 阻塞 IO 在虚拟线程里不"占"OS 线程：挂起后释放 carrier 给其他虚拟线程
- 使用方式和传统线程几乎一样：大多数"同步阻塞"代码无需改造即可获得高并发

适合 IO 密集服务；CPU 密集仍需线程池控制并行度。

---

## 8. 缓存体系与热点治理

### 8.1 缓存层次

```
浏览器 → CDN → 边缘网关缓存 → 本地/进程内缓存 → 分布式缓存 → 数据库自身缓存
```

每层都能截流，越靠近用户越省后端资源。

### 8.2 常用模式

- **Cache-Aside**（业务最常用）：读未命中回源；写删缓存
- **Read-Through / Write-Through**：缓存层封装存储访问
- **Write-Behind**：先写缓存，异步落库（高吞吐但可能丢数据）
- **Refresh-Ahead**：过期前异步刷新，避免峰值回源

### 8.3 三大经典问题

- **穿透**：查不存在的 key → 空值缓存 + 布隆过滤器
- **击穿**：热 key 过期瞬间高并发回源 → 互斥锁 / 逻辑过期
- **雪崩**：大量 key 同时过期/缓存宕机 → TTL 加随机抖动 + 多级缓存 + 限流熔断

### 8.4 一致性

- **写 DB + 删缓存** + **删除失败重试**（MQ）
- **订阅 binlog 兜底**（Canal / Debezium）
- **延迟双删**：适用于"先删后写" 的方案
- 强一致路径不走缓存（或用"读主 + 缓存仅加速"）

### 8.5 热点 key 治理

- **本地缓存 L1**：Caffeine / Guava，直接在应用进程里顶掉高频
- **多副本拆分**：`hot:1`、`hot:2`... 随机读
- **分片**：大 key 拆小
- **探测**：Redis `--hotkeys`，旁路采样
- **预热**：发布/促销前主动灌入

### 8.6 多级缓存设计示例

```
请求 → 本地 Caffeine （1 ms）
       └ miss → Redis       （5 ms）
                 └ miss → DB （50 ms）+ 回填两级
```

本地命中率即使 50% 也能大幅降低 Redis 压力。

### 📝 笔试题 8-1：缓存和数据库数据不一致的根本原因？

因为**两者是独立系统，写操作不可原子**。无论"先写 DB 再删缓存"还是"先删缓存再写 DB"都存在时序窗口。只能通过**删除+重试+binlog 兜底**实现**最终一致**，强一致需要付出代价（2PC、或完全不用缓存）。

---

## 9. 数据库扩展：读写分离、分库分表

### 9.1 读写分离

```
Writes → Master → async replicate → Slave1/2/3 → Reads
```

- 主备：写主，读从
- **复制延迟**：读从可能拿到旧值 → 关键路径走主
- 级联复制 / 半同步 / 组复制

### 9.2 分库分表

- **垂直分库**：按业务拆（用户库、订单库、商品库）
- **垂直分表**：大字段 / 冷字段分离
- **水平分表**：一张大表按规则切成 N 张
- **水平分库**：N 张小库各自独立

### 9.3 分片策略

- **取模**：均匀但扩容重分布
- **范围**：按时间/ID 区间，易热点
- **一致性哈希**：扩容影响小
- **基因法**：订单号嵌入 userId 分片信息，避免跨片查

### 9.4 分布式挑战

| 挑战 | 对策 |
|------|------|
| 分布式 ID | Snowflake / 号段 / UUID |
| 跨分片 JOIN | 广播小表 / 应用侧聚合 / 禁止 |
| 分布式事务 | Saga / TCC / outbox |
| 跨分片分页 | 归并排序，限制跳页 |
| 跨分片聚合 | 实时计算到数仓 / OLAP |
| 扩容 | 预分片 / 一致性哈希 / 在线迁移 |

### 9.5 何时才分？

经验阈值：

- 单表 > 5000 万 ~ 1 亿行
- 单库容量接近硬件上限
- 写入 QPS 单实例扛不住

**过早分片代价巨大**，先优化 SQL/索引/缓存/读写分离/硬件。

### 9.6 NewSQL / 分布式数据库

TiDB、CockroachDB、OceanBase、Spanner、PolarDB-X：

- 自动分片 + 强一致
- 兼容 MySQL / PostgreSQL 协议
- 适合中大规模但不想自己分库分表的场景
- 代价：运维、成本、对业务 SQL 的适配度

### 📝 笔试题 9-1：按 userId 取模 16 分库的坑？

- 扩容到 32 需**全量 rehash**
- 按非 userId 查询（订单号、商品号）需**扫所有分片**
- **大 V 热点**：少数用户流量极大，分片不均
- 对策：一致性哈希或 "基因法"（订单号后几位带 userId 分片），热点拆分

---

## 10. 异步化、消息队列与事件驱动

### 10.1 同步 → 异步的收益

- **解耦**：生产者不关心消费者
- **削峰**：队列缓冲突发流量
- **扩展**：消费者可独立水平扩展
- **最终一致**：替代强分布式事务

### 10.2 注意事项

- 异步链路**仍然有延迟**
- **至少一次**投递 → 消费端必须**幂等**
- **顺序**：需分区 key 保证局部顺序
- **堆积**：监控 Lag，扩容 / 并行
- **失败**：重试 + DLQ

### 10.3 事件驱动架构（EDA）

- 服务发布领域事件 → Kafka
- 其他服务订阅各取所需
- **Outbox Pattern**：DB + outbox 表 → Publisher 投递，避免"双写不一致"

### 10.4 任务队列

- 短任务 / 后台作业：Celery、Sidekiq、自研
- 长任务 / 调度：Airflow、XXL-Job、Temporal
- 与 MQ 区别：更贴近"任务调度"，含重试/优先级/状态

### 10.5 选型

- **Kafka**：高吞吐流、日志、数仓
- **RabbitMQ / RocketMQ**：业务消息、事务消息
- **Pulsar**：多租户、云原生
- **NATS**：轻量、低延迟
- **Redis Stream**：小规模

### 📝 笔试题 10-1：异步化能"降低"系统总耗时吗？

不能。异步把**客户端等待**与**后台执行**解耦：客户端感受更快，但后端实际工作量不变（甚至因序列化/重试而略增）。异步的价值是**可伸缩**和**抗突发**，不是魔法加速。

---

## 11. 高可用：冗余、故障转移与隔离

### 11.1 冗余即可用性之母

单机永远不够。冗余层级：

- **进程冗余**：多实例 + LB
- **节点冗余**：多机
- **机架冗余**：防单机架故障
- **AZ 冗余**：跨可用区部署
- **地域冗余**：跨 region / 异地
- **数据冗余**：多副本 / 多机房同步

### 11.2 故障转移（Failover）

- **主备**：一主一备，主挂备顶（Keepalived、Sentinel）
- **多活**：所有副本都在工作，挂一个不影响
- **选主**：Raft / Paxos / ZooKeeper
- **健康检查**：主动探测 + 被动观察
- **防抖（Hysteresis）**：避免短暂抖动触发频繁切换

### 11.3 无状态化

- 把会话 / 上下文外置（Redis / DB / JWT）
- 任何实例能处理任何请求
- 是**水平扩展**和**快速替换**的基础

### 11.4 舱壁模式（Bulkhead）

故障隔离：一部分资源挂了不影响别的。

- **线程池隔离**：不同依赖独立线程池
- **连接池分舱**：按下游分配
- **服务隔离**：不同租户独立实例
- **资源组**：K8s namespace、ResourceQuota

### 11.5 发布策略（可用性视角）

- **蓝绿**：两份环境切流，回滚秒级
- **金丝雀**：按比例灰度，小范围验证
- **滚动**：逐步替换，控制 Surge / Unavailable
- **Dark Launch / Feature Flag**：代码先上，功能延后打开

### 11.6 服务降级

能少做就少做：

- 关闭非核心功能（推荐、评论折叠）
- 静态兜底（首页静态版本）
- 返回缓存值甚至空结果
- **限流触发时优先保护核心**

### 11.7 自愈机制

- **K8s restart / reschedule**
- **HPA / Karpenter 自动扩容**
- **熔断 + 回退**
- **幂等 + 重试**
- **自动化回滚**（发版失败 → rollback）

### 📝 笔试题 11-1：为什么说"状态"是高可用的敌人？

有状态意味着**某个请求只能由特定实例处理**：

- 实例挂了，状态丢失或需故障转移
- 很难水平扩展
- 负载均衡受限
- 升级更重

解法：把状态放到**专用存储**（DB、Redis、对象存储），服务层保持无状态；有状态组件（DB、Kafka）用 **Raft/Paxos/主从复制**等专门机制。

---

## 12. 流量治理：限流、熔断、降级、排队

### 12.1 限流（Rate Limiting）

**目的**：保护自身与下游，避免被打垮。

算法：

- **固定窗口**：最简单，边界双倍流量
- **滑动窗口**：更精确
- **令牌桶**：允许突发
- **漏桶**：平滑匀速

维度：全局、接口、用户、IP、租户、API Key。

响应：**429 Too Many Requests** + `Retry-After`。

### 12.2 熔断（Circuit Breaker）

```
CLOSED ──(error rate > threshold)── OPEN ──(cooldown)── HALF_OPEN ─成功─▶ CLOSED
                                                              └─失败─▶ OPEN
```

参数：

- 错误率/慢调用率阈值
- 滑动窗口（时间 / 次数）
- 半开探测次数
- 冷却时长

代表：Resilience4j、Hystrix（已退役）、Sentinel、Istio Envoy。

### 12.3 降级（Fallback）

触发时给兜底：

- 返回缓存 / 默认值 / 空集合
- 切换到备用数据源
- 关闭非核心特性
- 转静态页

### 12.4 排队（Queue）

瞬时过载不拒绝而是排队：

- 队列深度 + 入队超时
- 拒绝新请求但服务已入队的
- 配合预估耗时决定进还是拒

**秒杀抢购**常见"漏斗式排队"：入口固定速率往后透。

### 12.5 舱壁 + 超时 + 重试 + 熔断 + 限流 + 降级

叠加才健全：

```
客户端→[限流]→[熔断]→[超时]→[重试]→下游
                  ↓         ↓
              [舱壁]    [降级]
```

推荐库：**Resilience4j**（Java）、**Polly**（.NET）、**Go-kit / goresilience**（Go）、**Sentinel**。

### 📝 笔试题 12-1：为什么重试必须配合限流与熔断？

否则**重试风暴**：下游慢 → 上游超时 → 上游重试 → 下游更慢 → ... 级联雪崩。

正确组合：

- **重试次数有限 + 退避 + 抖动**
- **上游超时 < 下游链路总耗时**
- **熔断快速失败**，不让已挂的下游继续被重试
- **限流**保护自身别被用户的重试打垮

---

## 13. 一致性与分布式事务

### 13.1 强一致的代价

- **2PC / XA**：阻塞协议、协调者单点、分布式死锁风险
- **共识算法（Raft/Paxos）**：写延迟高于单机
- 使用场景：金融核心账务、分布式数据库内部

大多数业务**接受最终一致**。

### 13.2 常见模式

- **Saga**：长事务拆成本地事务 + 补偿动作；编排（Orchestration） / 编舞（Choreography）
- **TCC（Try-Confirm-Cancel）**：资源预留 + 确认/取消，金融常用
- **Outbox Pattern**：业务 DB 事务中同时写 outbox 表，独立 Publisher 投 MQ
- **CDC（Change Data Capture）**：订阅 binlog 派生事件
- **事件驱动 + 幂等消费**：组合拳

### 13.3 幂等设计

- **业务键**（订单号、请求 ID）+ 唯一索引 / 去重表
- **版本号 / CAS** 防并发覆盖
- **状态机**只接受合法迁移
- **Idempotency-Key Header**（Stripe 风格）

### 13.4 分布式锁

- Redis `SET NX PX` + Lua 释放（适合短临界区）
- Redisson watchdog 续约
- ZooKeeper / etcd 临时节点（强一致）
- 生产建议：**幂等优先于锁**，锁是 last resort

### 13.5 选型框架

```
问："能接受多久的不一致？"
  ≤ 毫秒   → 考虑单库 / 强一致分布式存储
  ≤ 秒     → Saga、本地事务 + 快速同步
  ≤ 分钟   → 事件驱动、最终一致
  小时级   → 批处理、夜间对账
```

### 📝 笔试题 13-1：订单-库存-支付一致性方案？

**Saga + 事件驱动 + 幂等**：

1. 订单服务创建订单（status=pending），写 outbox 事件 OrderCreated（DB 事务保证原子）
2. Publisher 投 Kafka
3. 库存服务消费 → 尝试扣减 → 发 InventoryDeducted / InventoryDeductFailed
4. 支付服务按成功事件扣款 → PaymentSucceeded / PaymentFailed
5. 订单服务汇总结果：全成功 → paid；任一失败 → 发补偿事件，库存回补
6. 所有步骤用业务键幂等，消费端去重

---

## 14. 秒杀 / 抢购 / 红包案例

### 14.1 特点

- **极短时间、极高并发**（万 QPS 到百万 QPS）
- **库存有限**，不能超卖
- **强业务正确性**，弱展示一致性
- **黄牛 / 作弊** 需风控

### 14.2 分层削峰

```
用户 → CDN / 静态页          （减少动态请求）
     → 前端防抖、答题、验证码  （减少无效请求）
     → 网关限流（IP/用户）
     → 预热缓存（商品详情）
     → 库存服务（Redis 原子扣减）
     → 异步下单（MQ）
     → 订单服务（DB 事务、最终一致）
```

### 14.3 关键技术点

- **商品详情静态化 + CDN**，不打后端
- **活动页兜底 / 限流**：99% 流量在入口就被挡住
- **库存分桶**：把 100 件商品库存拆 10 份，各桶并发扣减
- **Redis + Lua 原子扣减**：绝对单线程避免超卖
- **异步下单**：扣减成功即入 MQ，用户快速响应"排队中"
- **幂等**：用户 + 活动 唯一键
- **防刷**：令牌预发（登录即领），入口就拒掉机器人
- **风控**：IP 黑名单、设备指纹、行为识别

### 14.4 Redis + Lua 扣减骨架

```lua
-- KEYS[1]=stock_key, KEYS[2]=user_set
-- ARGV[1]=user_id, ARGV[2]=amount
if redis.call('SISMEMBER', KEYS[2], ARGV[1]) == 1 then
  return -1                  -- 已购
end
local left = tonumber(redis.call('GET', KEYS[1]) or '0')
if left < tonumber(ARGV[2]) then
  return 0                   -- 售罄
end
redis.call('DECRBY', KEYS[1], ARGV[2])
redis.call('SADD', KEYS[2], ARGV[1])
return 1
```

应用层：成功 → 投 MQ → 下单服务异步落库。

### 14.5 常见错误

- 直接扣 DB：DB 连接池秒被打满
- 先查后扣：并发下超卖
- 没有前端限流 + 没有验证码：CPU 打满在防 bot 上
- 订单强一致：拖慢响应导致重试 / 超时雪崩

### 📝 笔试题 14-1：秒杀中如何防止超卖？

- **原子扣减**：Redis Lua / DB 乐观锁 `UPDATE ... WHERE stock >= n`
- **单分区串行**：消息路由到同一分区顺序处理
- **预扣 + 异步确认**：先预占，超时未确认释放
- **最终核对**：对账脚本夜间检查

多管齐下，核心是**扣减这一步必须原子**。

---

## 15. Feed / 推送 / IM 案例

### 15.1 Feed 流（时间线）

三种典型策略：

- **推模式（Fan-out on write）**：发帖时写入所有粉丝的 feed 列表；读快，写放大（大 V 致命）
- **拉模式（Fan-out on read）**：读时聚合关注者最新帖；读贵，写省
- **推拉结合**：普通用户推、大 V 拉

存储：Redis / Cassandra / 时间线专用服务。

### 15.2 推送系统

- **长连接网关**：WebSocket / MQTT，维持百万级连接
- **连接 - 用户路由**：`user_id → gateway_instance`（Redis）
- **消息入队 → 定向推**：按路由投递到目标网关
- **批量 + 合并 + 去重**
- **离线消息**：存 DB/Redis，重连拉取

### 15.3 IM / 即时消息

关键组件：

- **接入层**：WebSocket / TCP，百万长连接
- **会话路由**：`conv → 参与者`
- **消息存储**：时间线 + 已读回执
- **离线消息**、**历史**、**漫游**
- **状态同步**：多端一致（桌面 + 手机）

消息投递语义：

- 单聊、群聊、频道广播
- 至少一次 + 客户端去重
- 顺序：按 `conv_id` 分区
- 严重依赖**客户端 SDK** 保证端到端体验（重连、补拉、ACK）

### 15.4 大房间广播

- 房间 > 1w 人：单节点广播吃不消
- 分层广播（分桶、树形分发）
- 抽样采样：热门房间把不活跃用户屏蔽广播

### 📝 笔试题 15-1：100 万在线的 WebSocket 系统架构？

- 接入层：Go / Netty / Nginx stream，多实例横向分担，单实例 5-10 万长连
- 连接表：`user → gateway_instance` 放 Redis
- 消息分发：Kafka / NATS，所有网关订阅对应主题
- 心跳：应用层 20-30s，中间件超时 > 心跳 × 2
- 升级发布：停机前通知客户端按需重连到新实例
- 监控：连接数、消息 QPS、延迟分位、断连率

---

## 16. 多机房 / 异地多活

### 16.1 为什么多机房

- **灾备**：单机房整体宕
- **低延迟**：就近访问（北京用户连北京机房）
- **合规**：数据必须本地化
- **容量**：单机房资源上限

### 16.2 架构模式

- **同城双活**：同城两 IDC，强一致（<5ms RTT），适合 DB 主备
- **两地三中心**：同城双 + 异地灾备
- **异地多活**：异地真正双写，最复杂但弹性最高

### 16.3 异地多活挑战

- **数据双写冲突**：同一记录在两地同时改
- **延迟**：跨地域 RTT 50-200ms+，同步复制不可行
- **路由**：用户流量到哪个机房？
- **一致性**：只能最终一致

常见策略：

- **单元化（Cell-based）**：按 userId 分片，每个用户只在一个机房"写主"
- **读本地写远程**：本地读近，写时同步异地
- **按业务分策略**：强一致业务（支付）集中一个机房，最终一致业务（日志、内容）多活

### 16.4 数据复制

- **DTS / Canal / Debezium**：binlog 复制
- **Kafka MirrorMaker 2**：消息跨集群
- 冲突检测：时间戳 / 版本号 / CRDT

### 16.5 流量调度

- **DNS 就近解析（GSLB）**
- **Anycast + BGP**（CDN 厂商）
- **客户端路由 + 服务发现**
- **按用户属性路由**：单元化核心

### 16.6 演练

- **断网演练**：故意切一个机房
- **切流测试**：定期把流量全部切到另一地
- **数据一致性对账**
- 不演练的多活 = 多一个故障源

### 📝 笔试题 16-1：异地多活和主备（DR）的区别？

- **DR（主备）**：平时只有主机房在用，备机房低流量或空闲；故障时切到备
  - 优点：简单，一致性易保证
  - 缺点：备机房资源浪费；切换时未必能扛全量
- **异地多活**：所有机房都在承担生产流量
  - 优点：资源利用率高，自然具备容灾能力
  - 缺点：架构复杂（单元化、冲突解决），运维门槛高

中大规模业务常从 DR 起步，逐步演进到多活。

---

## 17. 容量规划与压测

### 17.1 容量规划流程

1. 定义 SLO（吞吐 / 延迟 / 可用性）
2. 估算目标业务量（DAU、QPS、峰值系数）
3. 分解到各层（接入 / 应用 / 缓存 / DB / MQ）
4. 压测得到单实例上限
5. 算出需要的实例数 + 冗余系数（1.3-2x）
6. 明确扩容策略（预留 / 自动 / 手动）
7. 按灰度逐步放量验证

### 17.2 峰值系数估算

- 常见规律：**日 PV 80% 集中在 20% 时间**
- 电商大促：日常 10 倍
- 秒杀：瞬时 100 倍
- 要按峰值规划容量，不按平均

### 17.3 压测策略

- **基线压测**：每版本跑一次，对比回归
- **线性压测**：逐步加压找拐点
- **长时间稳态**：验证耐久性、内存泄漏
- **尖峰压测**：模拟突发
- **全链路压测**：用影子流量、影子表、压测标记区分生产与压测数据

### 17.4 数据 & 场景真实度

- 数据量级接近生产（空表太快、假数据结论失真）
- 业务比例真实（读/写、冷/热）
- 流量分布真实（长尾、热点）

### 17.5 常见结论类型

- **QPS 天花板**：受 CPU / GC / 连接池 / DB 限制
- **延迟分布**：正常 vs 长尾
- **失败模式**：过载时是变慢、报错、还是崩溃
- **恢复能力**：移除负载后多快回到稳态

### 17.6 工具

- **k6**、**JMeter**、**Gatling**、**Locust**、**wrk / wrk2**
- **全链路压测平台**：自研或基于开源（PTS / Takin）

### 📝 笔试题 17-1：压测跑通了就能高枕无忧吗？

不行。压测是**必要非充分**：

- 业务流量模式可能与压测脚本不符
- 真实流量的分布长尾、地域差异
- 故障场景（缓存宕、DB 抖动）未必覆盖
- 发布、配置变更会改变性能画像

生产应结合：**压测 + 灰度 + 实时监控 + 容量预警 + 应急预案**。

---

## 18. 常见反模式与权衡

### 18.1 反模式清单

❌ **分布式单体**：微服务形，但共享 DB、同步强依赖、集中发布
❌ **共享数据库**：跨服务读同一张表
❌ **无超时的 RPC**：一处卡顿拖死全链路
❌ **无限重试**：触发重试风暴
❌ **缓存和 DB 双写 + 未设计一致性**
❌ **大 key / 热 key 无治理**
❌ **异步 = 金箍棒**：不必要的异步复杂度
❌ **单点脚本定时任务**：无 HA、无重试
❌ **监控事后补**：出事才加日志指标
❌ **追求 5 个 9**：成本/复杂度 ROI 负
❌ **提前过度设计**：六级降级 + 双活 + 分片 + CQRS 上线第一天
❌ **过度抽象**：一切 SPI / 框架自造，团队心智崩溃

### 18.2 权衡框架

做架构决策时问自己：

1. **业务价值**：真的需要这个目标吗？
2. **成本**：多少钱 / 多少人 / 多少时间
3. **复杂度**：团队能长期维护吗
4. **反悔成本**：做错了多久能改
5. **风险**：失败后影响
6. **演进空间**：是否堵死未来选项

**简单打败复杂**：能用少量手段达到目标，就不多加一层。

### 18.3 按业务阶段演进

| 阶段 | 架构侧重 |
|------|----------|
| 0-1（验证） | 单体 + 云托管服务，快速上线 |
| 1-10（扩张） | 模块化 + 核心服务拆分 + 基础监控 |
| 10-100（规模化） | 读写分离 + 缓存 + 异步 + 灰度 |
| 100+（成熟） | 多机房 + 单元化 + 全链路压测 + SRE |

**过早优化**是创业期头号杀手；**过晚重构**是中后期头号杀手。

### 📝 笔试题 18-1：你的系统"目前能扛 1 万 QPS，老板让扛 10 万"，思路？

按优先级：

1. **测量**：瓶颈到底在哪（CPU / GC / 锁 / DB / 网络）
2. **不动代码先拿**：加机器 / 加副本 / 垂直升级硬件
3. **数据层**：加缓存、索引优化、读写分离
4. **接入层**：CDN、静态化、合并请求
5. **关键路径**：优化算法 / 异步化非核心
6. **分库分表 / 分服务**：只在真的单库瓶颈再做
7. **容量演练 + 灰度放量**验证

**先找瓶颈、再选方案**，避免拍脑袋上分片。

---

## 19. 综合笔试练习

### 19.1 选择题

**Q1** CAP 定理中分布式系统必然放弃的是？
A. C  B. A  C. P 必选，在 C 与 A 之间权衡  D. 都能同时满足

<details><summary>答案</summary>C。</details>

**Q2** Little's Law `L = λ×W`，若 QPS 保持，延迟 W 翻倍，则系统内并发 L？
A. 不变  B. 翻倍  C. 减半  D. 无关

<details><summary>答案</summary>B。</details>

**Q3** 以下哪项**不是**高并发系统常见手段？
A. 缓存  B. 异步化  C. 同步长链路调用  D. 分片

<details><summary>答案</summary>C。</details>

**Q4** 熔断的 OPEN 状态作用？
A. 允许所有请求通过
B. 快速失败以保护下游与自身
C. 记录日志
D. 随机丢弃 10% 请求

<details><summary>答案</summary>B。</details>

**Q5** 秒杀中避免超卖最关键的是？
A. DB 事务隔离级别调高
B. 扣减库存的操作原子且单点串行
C. 减少页面请求
D. 减少广告

<details><summary>答案</summary>B。</details>

**Q6** 下列关于异步化，错误的是？
A. 能解耦生产者与消费者
B. 能削峰填谷
C. 可以降低系统整体工作量
D. 消费端需要幂等

<details><summary>答案</summary>C。</details>

**Q7** 两地三中心属于？
A. 单机房  B. 同城双活  C. 异地灾备 + 同城双活的组合  D. 异地多活

<details><summary>答案</summary>C。</details>

**Q8** 高可用设计中，"状态"通常被视为？
A. 优势，便于定位问题
B. 敌人，阻碍水平扩展与故障转移
C. 与高可用无关
D. 仅在数据库层面有影响

<details><summary>答案</summary>B。</details>

### 19.2 判断题

1. 高并发 = 高性能。 ❌
2. 缓存越多越好。 ❌（内存、维护、一致性都有成本）
3. 重试策略应配合限流与熔断。 ✅
4. 消息队列能"降低"系统总工作量。 ❌
5. 异地多活必然比主备便宜。 ❌（架构复杂度更高）
6. 单机房 4 副本 比 跨 AZ 2 副本 更可用。 ❌（AZ 级故障一挂全挂）
7. 过早优化常常是问题来源。 ✅
8. 金融级一致性通常需要强一致存储或两阶段协调。 ✅

### 19.3 简答题

**Q1** 高并发系统的"五大常见手段"？

1. **水平扩展 + 无状态**：多实例分担流量
2. **缓存**：多级、热点治理
3. **异步化**：MQ 削峰、事件驱动
4. **分片 / 分区**：DB / 缓存 / 服务拆分
5. **限流 / 熔断 / 降级**：过载保护

**Q2** 面对"热点商品详情"流量突增，你的优化路径？

1. CDN 静态化 / 半静态化
2. 接入层缓存（Nginx / 网关）
3. 应用本地缓存（Caffeine / Guava）
4. 分布式缓存（Redis）
5. 热 key 治理：多副本 / 分桶
6. 限流与降级兜底
7. 监控 QPS、命中率、后端负载

**Q3** 你怎么为一个新业务做高可用规划？

- 明确 SLO（业务价值决定）
- 架构上**无状态 + 冗余**
- 依赖：**超时 + 重试 + 熔断 + 降级**
- **探针 + 自愈**（K8s / systemd）
- **监控 + 告警 + SLO 燃烧率**
- 部署：**灰度 + 金丝雀 + 自动回滚**
- 灾备：跨 AZ；重要业务两地三中心
- 人员：**on-call + runbook + 演练**

**Q4** 如何规划一个 10 倍流量增长的架构演进？

- 压测确定现系统瓶颈
- 分层改造：CDN → 缓存 → 应用无状态 → DB 读写分离
- 异步化非关键路径，削峰用 MQ
- 监控完善，SLO 先立
- 预算充足时直接水平扩展；成本敏感时做分片
- 灰度上线，按流量分阶段放量

### 19.4 设计题

**Q1** 设计一个百万 DAU 的电商下单系统。

**层级**：

```
CDN / 静态
   │
WAF / 网关（鉴权、限流）
   │
BFF / 聚合层
   │
订单 / 商品 / 库存 / 支付 / 促销 / 通知
   │
Redis（热 key、令牌、队列）
MySQL 分库分表（按 userId 取模）
Kafka（OrderCreated、PaymentSucceeded、InventoryDeducted 事件）
对象存储（图片、凭证）
ES（搜索）
```

**要点**：

- 下单前置：Redis + Lua 原子扣库存，MQ 异步入库
- 分库分表：订单按 userId（基因法带商品分片前缀）
- 订单、库存、支付：**Saga + 事件 + Outbox**
- 多级缓存 + 热点治理
- 每个关键接口：超时 + 重试 + 熔断 + 降级
- 发布：金丝雀 + 监控门禁
- 观测：Metrics / Logs / Traces 全贯穿
- 多 AZ 部署；大促期间扩容 3x；关键路径做压测

**Q2** 系统峰值 2 万 QPS，下游数据库 5000 QPS 上限，你怎么扛？

1. **缓存**：命中率 80% → DB 只需 4k QPS
2. **读写分离**：写主读从
3. **异步化**：非即时读可走 MQ 慢慢消化
4. **限流**：网关上限 2 万，溢出排队或拒绝
5. **批量**：批量 `IN` 合并
6. **分表**：按 key 分散热点，每分表 5k QPS
7. **优化 SQL/索引**：把单条 SQL 成本降一半等效翻倍容量

### 19.5 案例分析

**场景**：你的支付接口 P99 从 200ms 涨到 2s，错误率小幅上升，CPU 正常。

诊断思路：

1. 看链路追踪，确认慢在哪个 span
2. 慢调用集中在 DB：看慢日志 + EXPLAIN + 连接池饱和度
3. 如果 DB 无明显慢查询：看下游服务（银行/通道）是否超时
4. 如果通道慢：开启熔断，fallback 到备用通道或提示"稍后重试"
5. 同时通知业务方和 on-call
6. 事后：加补丁（超时缩短 / 多通道路由）、补监控、写 postmortem

---

## 📚 学习建议

1. **读经典**：
   - 《Designing Data-Intensive Applications》（DDIA）
   - 《Site Reliability Engineering》《The SRE Workbook》（Google）
   - 《The Art of Scalability》
   - 《系统设计面试—内幕指南》（Alex Xu）
2. **做案例**：每周读一篇大厂架构博客（Netflix、Uber、字节、美团）
3. **练手量化**：每次设计都算一算 QPS / 延迟 / 带宽 / 存储量
4. **压测成习惯**：每次改动都问"性能回归如何"
5. **敬畏生产**：再漂亮的架构也要让团队看得懂、扛得住
6. **永远先搞清楚瓶颈**：避免拍脑袋式优化

> 三高不是堆砌技术，而是对业务价值与工程成本的持续权衡。
