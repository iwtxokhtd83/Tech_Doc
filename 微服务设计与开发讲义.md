# 微服务设计与开发讲义

> 本讲义系统讲解微服务架构的设计原则、拆分方法、通信模式、数据管理、可靠性、可观测性与部署演进，配合笔试题加深理解。面向后端工程师、架构师与技术决策者。
>
> 约定：示例技术栈以 Java/Spring Cloud、Go、Kubernetes 为主；原则层面与语言无关。

## 目录

1. [为什么需要微服务](#1-为什么需要微服务)
2. [微服务核心特征与误区](#2-微服务核心特征与误区)
3. [服务拆分：DDD 与粒度](#3-服务拆分ddd-与粒度)
4. [服务通信：同步与异步](#4-服务通信同步与异步)
5. [API 网关与 BFF](#5-api-网关与-bff)
6. [服务注册与发现](#6-服务注册与发现)
7. [配置中心](#7-配置中心)
8. [分布式数据管理](#8-分布式数据管理)
9. [分布式事务模式](#9-分布式事务模式)
10. [可靠性：超时、重试、熔断、限流、舱壁](#10-可靠性超时重试熔断限流舱壁)
11. [一致性、幂等与重复消费](#11-一致性幂等与重复消费)
12. [服务安全：认证、授权、零信任](#12-服务安全认证授权零信任)
13. [可观测性：日志、指标、链路追踪](#13-可观测性日志指标链路追踪)
14. [部署、版本与发布策略](#14-部署版本与发布策略)
15. [Service Mesh 与 Sidecar](#15-service-mesh-与-sidecar)
16. [测试策略](#16-测试策略)
17. [团队、康威定律与演进](#17-团队康威定律与演进)
18. [常见反模式](#18-常见反模式)
19. [综合笔试练习](#19-综合笔试练习)

---

## 1. 为什么需要微服务

### 1.1 单体（Monolith）的痛点

- **部署耦合**：任何小改动都要发布整个应用，风险大
- **技术栈锁定**：整个团队绑死一门语言 / 一套框架
- **扩缩容粗粒度**：只能整体扩容，资源利用率低
- **模块边界模糊**：代码耦合 → 改一处动全身
- **单点故障**：一个模块 OOM 拖垮全体
- **团队协作瓶颈**：大团队争抢同一代码库

### 1.2 微服务的价值

- **独立部署**：每个服务独立发布、回滚、扩缩容
- **团队自治**：小团队完整拥有服务（You build it, you run it）
- **技术异构**：按场景选最合适的语言和存储
- **故障隔离**：舱壁隔离 + 限流熔断
- **快速迭代**：小而频繁发布，降低单次风险

### 1.3 微服务的代价（必须正视）

- **分布式系统的复杂度**：网络不可靠、部分失败、一致性难
- **运维成本**：多服务 + 多实例 + 多技术栈 → 需要 CI/CD / 编排 / 监控
- **跨服务事务难**：从本地 ACID 变成最终一致
- **调试 / 排障更难**：链路长，跨进程
- **开发环境复杂**：启动 N 个服务才能端到端
- **版本与契约**：接口变更牵一发动全身

### 1.4 何时考虑微服务

- 团队规模 > 一定门槛（一般 15-50+ 开发者）
- 业务域清晰、可独立演进
- 具备基础能力：CI/CD、容器化、监控、日志
- 成长中的业务，预期规模与复杂度会长期增长

否则：**从 Modular Monolith 起步**，等到痛点出现再拆。

### 📝 笔试题 1-1：所有系统都应该用微服务吗？

不应该。微服务带来分布式复杂度与运维成本。**小团队 + 小业务**用单体通常更合适，进化路径通常是：**单体 → 模块化单体 → 按需拆分微服务**。盲目追求微服务反而让团队被运维和一致性问题拖慢。

---

## 2. 微服务核心特征与误区

### 2.1 核心特征（Martin Fowler 总结）

- **通过服务组件化**：每个服务独立进程，通过 API 协作
- **围绕业务能力组织**：服务边界 ≈ 业务领域边界
- **产品化思维**：谁开发谁运维，长期负责
- **智能端点、哑管道**：业务逻辑在服务里，不要塞到 ESB
- **去中心化治理**：每个团队选合适的技术
- **去中心化数据管理**：**每个服务一套数据存储**
- **基础设施自动化**：容器、CI/CD、监控是前置条件
- **为失败设计**：默认"网络会断，下游会挂"
- **演进式设计**：允许服务边界随业务变化

### 2.2 常见误区

- **"把单体代码库按目录拆成几个工程就是微服务"**
  实际：进程未分离、部署仍耦合、数据库共享 → 只是"分布式单体"
- **"微服务越多越好"**
  实际：过度拆分带来指数级的跨服务调用与一致性问题
- **"先上 K8s / Istio / 注册中心再考虑业务"**
  实际：技术栈应服务业务，避免 **Resume-Driven Development**
- **"微服务解决性能问题"**
  实际：RPC 增加延迟；性能靠设计、缓存、索引，不靠"切成很多服务"
- **"共享数据库 = 方便"**
  实际：是微服务头号反模式，耦合度等同单体

### 📝 笔试题 2-1：微服务与 SOA 的区别？

- SOA 强调**企业级服务总线（ESB）** 与**协议标准化**（SOAP/WS-*）；中间件承担大量路由、编排
- 微服务强调**哑管道**（HTTP/REST、gRPC、消息队列），**业务逻辑在服务里**
- SOA 常与共享数据、集中治理绑定；微服务强调去中心化与独立部署
- 粒度：SOA 偏粗（粒度像系统），微服务偏细（按业务能力）

---

## 3. 服务拆分：DDD 与粒度

### 3.1 拆分原则

- **高内聚、低耦合**：同一业务逻辑不跨服务
- **单一职责**：一个服务一个清晰的目标
- **独立演进**：数据、部署、代码都可独立
- **围绕业务能力**（不是围绕技术分层）

### 3.2 DDD 视角

- **领域（Domain）**：业务范围
- **子域（Subdomain）**：核心子域 / 支撑子域 / 通用子域
- **限界上下文（Bounded Context）**：一套统一语言与模型的边界
- **聚合（Aggregate）**：一致性边界，作为事务单位
- **实体、值对象、领域事件**

**一个限界上下文 ≈ 一个微服务** 的候选边界。

### 3.3 事件风暴（Event Storming）

- 团队一起用便利贴梳理业务事件（橙色）、命令（蓝色）、聚合（黄色）
- 发现边界：事件前后发生明显语言/职责切换的地方
- 输出：候选服务清单、事件流、业务关键路径

### 3.4 粒度参考

| 过粗 | 合适 | 过细 |
|------|------|------|
| 核心服务像"小单体" | 团队 2-8 人可维护 | 一个服务只处理一个 API |
| 改动频繁冲突 | 业务内聚、独立发布 | 大量跨服务调用 |
| 数据耦合严重 | 数据独占 | 运维成本指数增长 |

经验法则：

- 服务数 ≈ 团队数的 1-5 倍
- 每个服务拥有自己的数据存储
- 单个服务能被一个小组在两周内大改
- 拆的时候先少后多：过粗容易改细，过细合回来成本极高

### 3.5 拆分示例：电商

```
用户服务 (User)            — 用户、认证、地址
商品服务 (Product)         — SKU、类目、库存
订单服务 (Order)           — 订单、支付状态
支付服务 (Payment)         — 支付通道、对账
库存服务 (Inventory)       — 锁定、扣减、回补
营销服务 (Promotion)       — 优惠券、活动
通知服务 (Notification)    — 短信/邮件/推送
搜索服务 (Search)          — ES 索引、推荐
```

- 订单 vs 支付：状态机分离，避免订单改支付策略
- 库存 vs 商品：库存模型独立，便于高并发扣减
- 通知：统一渠道，所有业务共享

### 📝 笔试题 3-1：如何判断服务拆分粒度是否合适？

观察信号：

- **修改频繁涉及多个服务** → 拆得太细，应合并
- **单个服务迭代慢、冲突多** → 可能还需拆
- **服务间调用深度超过 3-4 跳** → 调用链过长
- **多个服务共享数据库/表** → 数据边界没划清
- **业务变更时始终在同一对服务间反复来回** → 它们应在同一边界内

---

## 4. 服务通信：同步与异步

### 4.1 两大通信范式

| 范式 | 典型技术 | 特点 | 场景 |
|------|----------|------|------|
| **同步** | HTTP/REST、gRPC、GraphQL | 请求-响应，即时结果 | 查询、实时命令 |
| **异步** | Kafka、RabbitMQ、Pulsar、NATS | 消息驱动，解耦 | 事件广播、削峰、最终一致 |

### 4.2 REST vs gRPC

| 维度 | REST | gRPC |
|------|------|------|
| 传输 | HTTP/1.1 / 2 | HTTP/2 |
| 序列化 | JSON（文本） | Protobuf（二进制） |
| 契约 | OpenAPI | `.proto` |
| 性能 | 中 | 高 |
| 流式 | 弱（SSE） | 一元/服务端流/客户端流/双向流 |
| 浏览器 | 原生 | 需 gRPC-Web |
| 适用 | 对外、通用 | 内部、性能敏感 |

常见组合：**对外 REST + 内部 gRPC**。

### 4.3 事件驱动（EDA）

```
订单服务 ──OrderCreated──▶ Kafka ──▶ 库存服务
                                ──▶ 通知服务
                                ──▶ 数仓
```

- 生产者不关心消费者是谁
- 解耦、可扩展、最终一致
- 代价：追查因果需要链路追踪；实现幂等

### 4.4 命令 vs 事件

| | 命令 (Command) | 事件 (Event) |
|-|----------------|--------------|
| 意图 | 请求做某事 | 通知已发生 |
| 接收者 | 特定服务 | 任意感兴趣者 |
| 返回 | 通常有结果 | 无直接返回 |
| 耦合 | 较强 | 较弱 |
| 示例 | `CreateOrder` | `OrderCreated` |

### 4.5 服务间通信设计原则

- **偏好异步**：减耦合、提韧性
- **接口幂等**：所有跨服务调用默认可重试
- **契约先行**：OpenAPI / Protobuf，变更评审
- **避免同步长链路**：A→B→C→D 任一环卡住全链路失败
- **超时短、重试有限**：默认 3s / 重试 2 次（可调）
- **不跨服务事务**：用 Saga、事件驱动实现最终一致

### 📝 笔试题 4-1：为什么"接口要幂等"几乎是微服务的硬性要求？

网络不可靠，**客户端重试不可避免**。若下游不幂等：

- 创建类操作重复下单、重复扣款
- 更新类操作状态乱跳
- 删除类操作报 404 却实际已成功

幂等让**重试安全**，这是分布式可用性的前提。

---

## 5. API 网关与 BFF

### 5.1 API 网关的职责

- **入口统一**：对外一个域名/端口
- **路由**：按 path / host / header 转发
- **鉴权 / 限流 / 熔断**
- **协议转换**：外部 HTTP ↔ 内部 gRPC
- **聚合 / 编排**：合并多后端调用
- **可观测**：统一日志、指标、链路
- **灰度 / AB**：按规则分流

### 5.2 常见产品

- **Nginx / OpenResty**：轻量、高性能、Lua 扩展
- **Kong / APISIX**：插件生态丰富，控制面完善
- **Spring Cloud Gateway / Zuul**：Java 生态
- **Envoy**：云原生，xDS 动态配置
- **AWS API Gateway / Azure APIM / 阿里云 API Gateway**：托管
- **Traefik**：K8s/Docker 友好

### 5.3 BFF（Backend For Frontend）

针对不同前端（Web / Mobile / 小程序）提供**定制化聚合层**：

```
iOS App    ── iOS BFF    ─┐
Android    ── Android BFF ─┼── 核心微服务集群
Web        ── Web BFF    ─┘
```

优点：

- 每种前端独立演进，不让后端接口承担多端兼容
- 减少客户端多次请求（BFF 做聚合）
- 端上弱网时减少载荷

### 5.4 网关设计要点

- **无状态 + 水平扩展**
- **不放业务逻辑**：避免网关变成"新单体"
- **失败路径可测**：降级、静态返回、缓存兜底
- **版本管理**：`/v1/`、`/v2/` 并存
- **限流到用户/IP/租户维度**
- **日志带 trace_id**

### 📝 笔试题 5-1：API 网关 vs BFF 的关系？

- **API 网关**：通用层，所有前端共用；负责横切关注点（鉴权、限流、路由）
- **BFF**：业务聚合层，按前端形态定制接口

很多系统是 "**网关 + 多个 BFF**" 的组合：网关做安全与流控，BFF 做聚合与协议裁剪。

---

## 6. 服务注册与发现

### 6.1 为什么需要

微服务实例数量多、IP 动态变化（容器/云），**调用方不能硬编码地址**。

### 6.2 模式

- **客户端发现**：调用方从注册中心拉服务列表 + 本地 LB（Eureka + Ribbon、Nacos Client）
- **服务端发现**：调用方经过 LB/网关，LB 查注册中心（K8s Service、ALB、Istio Envoy）

### 6.3 常见注册中心

| 产品 | 特点 |
|------|------|
| **Consul** | 功能全（KV+DNS+健康检查），多数据中心 |
| **Eureka** | Netflix 经典，AP 优先 |
| **Nacos** | 阿里开源，注册 + 配置一体化 |
| **etcd / ZooKeeper** | 强一致（CP），K8s 底层 |
| **K8s Service + CoreDNS** | 云原生方案，大部分新系统首选 |

### 6.4 健康检查

- **心跳 / 主动探活**：客户端定期上报或 Agent 探测
- **优雅下线**：实例下线前从注册中心摘掉，等待流量排空
- **快速失败 vs 慢剔除**：过快易抖动，过慢存活旧实例

### 6.5 容错与一致性取舍

- **CP 系统**（ZK、etcd）：注册中心自身宕机期间注册/变更受限
- **AP 系统**（Eureka）：注册中心可用性优先，可能短暂读到旧数据
- 生产倾向 AP + 客户端本地缓存，避免注册中心抖动连锁

### 📝 笔试题 6-1：客户端发现 vs 服务端发现？

- **客户端发现**：调用方自己维护服务列表、做 LB；需要语言级 SDK。优点：灵活、少一跳；缺点：每语言都要实现
- **服务端发现**：有中间 LB/网关，调用方只需访问 VIP；优点：客户端简单、跨语言一致；缺点：多一跳延迟

**K8s 默认服务端发现**（Service + kube-proxy）；**Spring Cloud 传统客户端发现**（Ribbon/LoadBalancer）。

---

## 7. 配置中心

### 7.1 为什么独立

- **避免散落**：多服务、多环境、多实例
- **动态下发**：不重启生效（开关、阈值）
- **权限与审计**：谁改了什么、何时改的
- **版本与回滚**
- **多环境一致性**：dev/stg/prd 清晰隔离

### 7.2 主流产品

- **Nacos / Apollo**（携程）：国内主流
- **Spring Cloud Config** + Git
- **Consul KV / etcd**
- **HashiCorp Vault**：秘密管理
- **AWS Parameter Store / Secrets Manager**

### 7.3 设计要点

- **推 vs 拉**：长轮询 / Websocket 推、或定时拉（心智简单）
- **分级**：应用级 / 环境级 / 实例级
- **敏感信息加密**：密码、Key 走 Vault
- **变更灰度**：按实例比例 / IP 段推送
- **本地兜底**：配置中心挂了仍可读上次快照
- **审计日志 + 变更审批**

### 7.4 示例：Spring Cloud + Nacos

```yaml
spring:
  application:
    name: order-service
  cloud:
    nacos:
      config:
        server-addr: nacos:8848
        file-extension: yaml
        shared-configs:
          - common.yaml
        namespace: prod
```

配置文件命名：`order-service-prod.yaml`、`common.yaml`。

### 📝 笔试题 7-1：配置放代码里 vs 配置中心，优劣？

- **代码里**：版本化清晰、环境隔离简单；但每次变更需发版
- **配置中心**：动态生效、支持灰度；但增加依赖、需处理"配置中心挂了"

最佳：**不敏感的业务阈值/开关**放配置中心；**部署相关**仍入代码/镜像（声明式）；**密钥**放 Vault。

---

## 8. 分布式数据管理

### 8.1 核心原则：**每服务独占数据库**

- 不共享 schema，不跨库 join
- 其他服务需要数据 → 通过 API 或事件订阅
- 物理上可放同一实例，**逻辑上必须隔离**

### 8.2 跨服务数据访问模式

- **同步 API 查询**：简单但加耦合
- **事件复制**：通过事件异步同步到需要方的本地存储
- **CQRS**：命令走原服务，查询走聚合读模型
- **API Composition**：网关/BFF 聚合多服务结果（小规模数据适用）

### 8.3 数据一致性权衡

- **强一致**：代价高（分布式事务），大多数业务不需要
- **最终一致**：绝大多数业务能接受的默认选择
- 明确 **SLA**：允许多久的不一致？

### 8.4 数据复制与物化视图

- 订单服务发布 `OrderCreated` 事件
- 数据分析服务订阅并写入自己的 OLAP 存储
- 搜索服务订阅并更新 ES 索引
- 各取所需，不打扰源头

### 8.5 数据归属与所有权

- 明确每个聚合根/表的**唯一所有者服务**
- 禁止"跨部门直接读别人的表"
- 迁移老系统：先复制、再切换、最后清理

### 📝 笔试题 8-1：订单服务需要用户昵称显示，怎么做？

几种方案：

1. **同步查用户服务 API**：简单但有耦合和级联延迟
2. **订阅用户事件**，在订单服务本地冗余昵称（事件驱动复制）
3. **BFF 聚合**：前端展示时由 BFF 并发调用订单+用户
4. **读模型**：专门的"订单视图服务"订阅两端事件合成

权衡点：读频率、一致性要求、容错能力。高频展示推荐**本地冗余 + 订阅变更**。

---

## 9. 分布式事务模式

### 9.1 为什么没有分布式 ACID

跨服务 + 跨数据库，ACID 要两阶段提交（2PC），生产环境代价巨大：

- 阻塞协议，协调者单点
- 长事务期间资源锁定
- 参与者不可回滚的异常难处理

所以默认采用**最终一致**。

### 9.2 Saga 模式

长事务拆成一系列**本地事务 + 补偿动作**：

```
T1 订单    C1 取消订单
T2 扣库存  C2 回库存
T3 扣款    C3 退款
T4 发货
```

任何 Ti 失败 → 逆向执行已完成步骤的 Ci。

**两种编排**：

- **编排式（Orchestration）**：中央协调器驱动，状态清晰但有中心
- **编舞式（Choreography）**：各服务通过事件自发协作，解耦但难追踪

### 9.3 TCC（Try-Confirm-Cancel）

```
Try     : 资源预留（冻结库存/金额）
Confirm : 确认提交
Cancel  : 回滚预留
```

- 需要服务实现三接口
- 有状态要求
- 代表：Seata TCC 模式、蚂蚁 DTX

### 9.4 本地事务表 + 消息（可靠事件）

```
业务操作：
  BEGIN
    UPDATE orders SET status='paid'
    INSERT INTO outbox(topic, key, payload) VALUES (...)
  COMMIT

Publisher（独立）：
  读 outbox 未发条目 → 投 Kafka → 标记已发
```

业务 DB 事务保证"状态变更与事件记录原子"，Publisher 保证 at-least-once。

### 9.5 选择指南

| 方案 | 强一致 | 开发复杂度 | 适用 |
|------|--------|-------------|------|
| 2PC | ✅ | 低（框架支持时） | 同构 DB，慎用 |
| TCC | 接近 | 高 | 金融核心交易 |
| Saga | 最终一致 | 中 | 业务长流程 |
| 事件 + Outbox | 最终一致 | 低-中 | 大部分场景首选 |

### 📝 笔试题 9-1：下单扣库存扣款，三个服务，怎么保证一致？

**推荐 Saga + 事件驱动**：

1. 订单服务创建订单（状态 `pending`）并发事件 `OrderCreated`
2. 库存服务消费，成功扣减并发事件 `InventoryDeducted`；失败发 `InventoryDeductFailed`
3. 支付服务按 `InventoryDeducted` 扣款，发 `PaymentSucceeded`/`PaymentFailed`
4. 订单服务订阅结果：成功 → `paid`；任一失败 → 触发补偿（回库存 / 标记失败）

要点：**每个服务幂等**、**消息可靠投递**（Outbox Pattern）、**补偿幂等**。

---

## 10. 可靠性：超时、重试、熔断、限流、舱壁

### 10.1 为失败设计

分布式下**"部分失败"是常态**。核心模式：

- **超时 Timeout**：一切外部调用必须有超时
- **重试 Retry**：仅对幂等操作；配合退避和抖动
- **熔断 Circuit Breaker**：下游故障时快速失败，避免雪崩
- **限流 Rate Limit**：保护自己与下游
- **舱壁 Bulkhead**：资源隔离，局部故障不传染
- **降级 Fallback**：异常时返回静默结果

### 10.2 超时

- **连接超时**：几秒内；`connect_timeout`
- **读超时**：按业务 SLA；`read_timeout`
- 必须**层层设定**，上游总超时 > 下游之和 + 缓冲
- 永远不要 "不设超时"

### 10.3 重试

- **仅对幂等/安全操作**
- **指数退避 + 抖动**：`delay = base * 2^n + rand(jitter)`
- **限制次数**：通常 2-3 次
- **区分错误类型**：仅对网络错、5xx、408、429 重试；4xx（业务错）不重试
- **避免全链路重试放大**：上下游都重试会造成 N² 压力

### 10.4 熔断（Circuit Breaker）

三态：

```
CLOSED (正常) ──(错误率阈值)──▶ OPEN (拒绝请求)
                                   │
                                   ▼ (冷却时间到)
                              HALF_OPEN ─成功─▶ CLOSED
                                         └失败─▶ OPEN
```

关键参数：

- **错误率阈值**：如 50%
- **滑动窗口**：最近 N 次 / M 秒
- **半开探测次数**
- **冷却时间**

代表库：**Resilience4j**（Java）、**Hystrix**（退役）、**Sentinel**（阿里）、Go 的 `sony/gobreaker`。

### 10.5 限流

客户端/服务端双侧限流。算法：

- **固定窗口**：简单但边界双倍流量
- **滑动窗口**：更精确
- **令牌桶**：允许突发
- **漏桶**：平滑匀速输出

维度：API、用户、租户、IP、接口参数组合。

### 10.6 舱壁（Bulkhead）

- **线程池隔离**：不同下游调用用不同线程池，一个满不影响其他
- **连接池限额**：DB / Redis / HTTP 按服务隔离
- **信号量隔离**：轻量版的舱壁

### 10.7 降级（Fallback）

示例：

- 用户昵称查询失败 → 返回 "Unknown"
- 推荐服务挂 → 返回默认热门商品
- 详情页评价加载失败 → 显示"稍后重试"而不是白屏

### 10.8 组合拳示例（伪代码）

```java
CircuitBreaker cb = CircuitBreaker.of("paySvc", ...);
Retry retry = Retry.of("paySvc", RetryConfig.custom()
    .maxAttempts(3).waitDuration(ofMillis(100))
    .retryExceptions(IOException.class).build());
Bulkhead bh = Bulkhead.of("paySvc", BulkheadConfig.custom().maxConcurrentCalls(20).build());

Supplier<PayResult> call = () -> paymentClient.pay(req);
Supplier<PayResult> decorated = Decorators.ofSupplier(call)
    .withCircuitBreaker(cb)
    .withRetry(retry)
    .withBulkhead(bh)
    .withFallback(ex -> PayResult.pending())
    .decorate();
```

### 📝 笔试题 10-1：下游超时，上游重试，会发生什么？

**重试风暴**：下游本来就慢，上游又重试加倍流量，下游更慢 → 全链路级联超时。防范：

- 下游超时 << 上游超时
- 重试次数小，带退避与抖动
- 上游开熔断，超过阈值直接失败
- 全链路统一"deadline"，剩余时间不足就不重试

---

## 11. 一致性、幂等与重复消费

### 11.1 一致性模型

- **强一致**：任意读都看到最新写
- **读己之写**：自己的写能立刻读到
- **单调读**：不回退到更早的状态
- **因果一致**：因果相关的变更按顺序可见
- **最终一致**：一段时间后达到一致

### 11.2 幂等性落地

- **天然幂等**：`SET x=v`、`PUT /resource/:id`
- **业务幂等键**：`Idempotency-Key` Header + 去重表
- **状态机**：只允许特定状态转移
- **版本号 / CAS**：`UPDATE ... WHERE version=?`

```sql
-- 幂等表
CREATE TABLE idempotency (
  key VARCHAR(64) PRIMARY KEY,
  result TEXT,
  expire_at DATETIME
);
```

### 11.3 分布式锁

- 适合：**防重**、**独占资源**
- 实现：Redis `SET NX PX` + Lua 释放、Zookeeper 临时节点
- 风险：锁超时与业务耗时不匹配、锁丢失、脑裂
- 规避：**业务幂等优于分布式锁**

### 11.4 重复消费

消息 MQ 默认 **at-least-once**：

- 业务层做去重（幂等表）
- 单条消息处理失败 → 不 ACK，等 MQ 重投
- 超过重试次数 → DLQ

### 📝 笔试题 11-1：说明"幂等优于锁"的理由？

锁是**悲观**手段：持续竞争、死锁风险、续期复杂、锁失效时仍需兜底幂等。而幂等是**乐观**的：无论被调用几次结果一致，天然抵御重试与网络抖动。幂等做对了，锁往往就不需要了。

---

## 12. 服务安全：认证、授权、零信任

### 12.1 分层安全

```
外网 → 网关（WAF + TLS + 鉴权） → 内网微服务（mTLS + JWT） → DB（ACL）
```

### 12.2 认证方式

- **OAuth 2.0 / OIDC**：对外通用
- **JWT**：内部服务间、短期无状态
- **mTLS**：服务间强身份，Service Mesh 默认
- **API Key**：B2B / 机器间
- **SSO**：企业内部

### 12.3 授权模型

- **RBAC**：角色 - 权限
- **ABAC**：属性策略（用户 / 资源 / 环境）
- **ReBAC**：关系图（Google Zanzibar）
- **OPA (Open Policy Agent)**：策略即代码，跨服务统一

### 12.4 零信任（Zero Trust）

- **默认不信任任何网络位置**（即使内网）
- 每个请求都验证身份 + 授权 + 完整性
- 最小权限
- 全程加密 + 全程审计

### 12.5 敏感数据

- **加密**：传输（TLS）、存储（AES）
- **脱敏**：日志、响应里不回显明文
- **审计**：谁在什么时间访问了什么
- **最小化**：不要采集不需要的数据

### 12.6 密钥与凭据管理

- 绝不硬编码 / 进代码仓库
- Vault / KMS / 云 Secrets Manager
- 定期轮换
- 泄漏响应预案

### 📝 笔试题 12-1：微服务间通信是否一定要 mTLS？

**生产建议是**。内网不等于安全：

- 内部攻击、供应链威胁、容器逃逸都可能
- mTLS 提供身份验证 + 加密 + 防重放
- Service Mesh（Istio/Linkerd）可自动注入，业务无感

小规模或早期系统可先用 JWT + 网络分段，随规模演进到 mTLS。

---

## 13. 可观测性：日志、指标、链路追踪

### 13.1 三大支柱

```
Logs          Metrics        Traces
what happened  how many      when & where
   (事件)        (数)           (链路)
```

### 13.2 结构化日志

```json
{
  "ts":"2025-01-15T10:00:00Z",
  "level":"INFO",
  "service":"order",
  "trace_id":"abc123",
  "span_id":"def456",
  "user_id":42,
  "event":"order.created",
  "order_id":"o-100",
  "amount":99.9
}
```

要点：

- **统一字段**：`trace_id`、`service`、`level`
- **避免 print 明文**：用 logger（`logback` / `zap` / `structlog`）
- **敏感信息脱敏**
- **日志级别适度**：生产不要 DEBUG 海量

### 13.3 指标（Metrics）

- **四大黄金信号 (Golden Signals)**：
  - **Latency**：请求耗时
  - **Traffic**：请求量
  - **Errors**：错误率
  - **Saturation**：资源饱和度
- **RED 方法**（面向服务）：Rate / Errors / Duration
- **USE 方法**（面向资源）：Utilization / Saturation / Errors

技术栈：**Prometheus + Grafana + Alertmanager**。

### 13.4 链路追踪（Distributed Tracing）

通过 **trace_id + span** 串联跨服务调用：

```
[API Gateway] -trace_id=abc-
   [Order Svc] span_id=1
      [User Svc] span_id=2  (parent=1)
      [Inventory Svc] span_id=3  (parent=1)
```

工具：**OpenTelemetry**（事实标准 SDK）→ 后端 **Jaeger / Zipkin / Tempo / SkyWalking**。

### 13.5 SLO / SLA / SLI

- **SLI**：具体指标（如"请求成功率"）
- **SLO**：目标（99.9%）
- **SLA**：对外承诺（含违约赔偿）
- **错误预算（Error Budget）**：1 - SLO，超出就暂停新功能发布

### 13.6 告警设计

- **基于症状**（用户感受到的问题），而非单一组件
- **可操作**：告警要能对应明确行动
- **分级**：P0 立即响应 / P1 小时级 / P2 次日
- **避免噪音**：告警疲劳比无告警更可怕

### 📝 笔试题 13-1：微服务排查问题从哪开始？

**可观测性三件套**：

1. **指标**：看总体 RED / 黄金信号，确定哪个服务 / 端点异常
2. **链路**：选一个异常 trace，定位具体慢/错的 span
3. **日志**：按 `trace_id` 过滤该链路的详细日志

必备前提：全链路 **trace_id 传递**、结构化日志、统一时间戳（UTC）。

---

## 14. 部署、版本与发布策略

### 14.1 CI/CD 流水线

```
Commit → CI (lint/test/build)
  → 镜像构建 (Dockerfile)
    → 推送镜像仓库
      → CD (Helm/ArgoCD)
        → 部署到 K8s
          → 自动冒烟测试
            → 灰度 → 全量
```

### 14.2 容器化与编排

- **Docker**：镜像 + 运行时
- **Kubernetes**：编排（Deployment / Service / Ingress / HPA）
- **Helm / Kustomize**：配置模板
- **ArgoCD / Flux**：GitOps，声明式发布

### 14.3 发布策略

| 策略 | 做法 | 特点 |
|------|------|------|
| **蓝绿 (Blue-Green)** | 新旧两套环境，切换流量 | 回滚极快，成本高 |
| **滚动 (Rolling)** | 逐步替换旧实例 | 资源友好，回滚慢 |
| **金丝雀 (Canary)** | 先给少量流量，验证后扩大 | 风险可控，需指标配合 |
| **A/B** | 按规则给不同用户不同版本 | 用于实验 |
| **Dark Launch** | 功能隐藏在 feature flag 后 | 与发布解耦 |

### 14.4 API 版本管理

- URI 版本：`/v1/users`（最常见）
- Header：`Accept: application/vnd.x.v1+json`
- 非破坏性变更（加字段）不升版
- 破坏性变更 → 新版本 + Deprecation 周期（6-12 月）

### 14.5 优雅启停

- **健康检查**：`/livez`（存活）、`/readyz`（就绪）
- **启动时** 预热连接池、缓存
- **下线时** 先从 LB 摘除，处理 in-flight 请求，再退出
- K8s：`terminationGracePeriodSeconds` + `preStop` 钩子

### 14.6 Feature Flag

- **Flagsmith / Unleash / LaunchDarkly**
- 把"部署"和"发布"解耦
- 支持按用户 / 百分比开启
- 灾难时快速关闭新功能

### 📝 笔试题 14-1：为什么金丝雀发布是大型系统的首选？

- **风险可控**：先 1% 流量，有问题只影响 1%
- **数据验证**：配合监控，用真实流量跑 SLO
- **自动化**：结合指标可做自动推进/回滚
- **用户级灰度**：内部员工 / Beta 用户优先

代价是需要完善的监控、路由能力和流量切分工具。

---

## 15. Service Mesh 与 Sidecar

### 15.1 基本概念

**Service Mesh**：把服务间通信的横切关注点（重试、熔断、mTLS、路由、遥测）**下沉到独立的 Sidecar 代理**中，业务代码保持纯净。

```
Service A                  Service B
  │                          ▲
  ▼                          │
[Sidecar A] ─── mTLS ──▶ [Sidecar B]
  │                          │
  ├──── 遥测/策略 ◀──────────┤
                 │
         Control Plane (xDS)
```

### 15.2 代表产品

- **Istio + Envoy**：功能最全，生态好
- **Linkerd**：轻量、性能好
- **Consul Connect**
- **Cilium Service Mesh**（基于 eBPF，无 Sidecar 形态）

### 15.3 Mesh 能做什么

- **连接**：mTLS、路由、重试、熔断、负载均衡
- **安全**：身份、策略、授权
- **可观测**：指标、日志、追踪零侵入采集
- **流量管理**：金丝雀、镜像、故障注入

### 15.4 代价

- **复杂度**：多一个组件，学习曲线陡
- **性能**：每请求多几毫秒（Sidecar 转发）
- **资源**：每实例多一个 Sidecar 容器
- **升级**：控制面、数据面版本兼容

### 15.5 何时引入

- 服务数 > 几十，多语言混合
- 内部策略（mTLS、授权）复杂
- 已经上了 K8s，有运维能力
- 不是 "刚起步就上 Istio"，那通常是 over-engineering

### 📝 笔试题 15-1：Service Mesh 和 API 网关的区别？

- **网关**：处理**南北向**流量（外部 → 内部），通常对外；做鉴权、限流、聚合
- **Mesh**：处理**东西向**流量（服务 ↔ 服务），通常内部；做 mTLS、重试、追踪
- 两者互补，典型生产架构同时使用

---

## 16. 测试策略

### 16.1 测试金字塔

```
         /\
        /E2E\           少量，慢，脆
       /----\
      /整合 \          中量
     /------\
    / 契约   \         中量，跨服务
   /--------\
  /  单元    \        大量，快，稳定
 /___________\
```

- **单元**：函数/类级别，毫秒级，mock 外部
- **契约（Contract Test）**：消费者 - 生产者契约一致（Pact）
- **集成**：与真实 DB / 缓存 / MQ，`Testcontainers`
- **端到端**：全链路，少而精

### 16.2 契约测试

避免 "接口改了消费者不知道"：

```
Consumer 写预期 → 生成 pact 文件 → Provider 验证能满足
```

CI 中双方验证一致，有破坏性变更立即失败。

### 16.3 混沌工程（Chaos Engineering）

主动制造故障验证系统韧性：

- 杀实例（Chaos Monkey）
- 注入延迟
- 断网
- 填满磁盘

工具：**Chaos Mesh**、**LitmusChaos**、AWS FIS。

### 16.4 性能与压测

- **单服务**：k6、wrk、jmeter
- **全链路**：影子流量 / 录制回放
- **目标**：定位瓶颈、制定容量规划、验证 SLO

### 16.5 本地开发体验

- Docker Compose 拉起依赖
- Mock 远端服务（WireMock / Mockoon）
- Telepresence：本地代码接入远端 K8s
- Devspace / Skaffold：K8s 友好的开发循环

### 📝 笔试题 16-1：为什么微服务要重点投资"契约测试"？

- 单元测试对独立模块有用，但**跨服务接口变更**靠单元测试发现不了
- E2E 覆盖面大但慢、脆
- 契约测试把"接口期望"编码成可验证的规范，**CI 自动防止破坏性变更**
- 消费者发现新需求时先更新契约，推动生产者演进

---

## 17. 团队、康威定律与演进

### 17.1 康威定律（Conway's Law）

> 系统的架构反映组织的沟通结构。

若组织是按前端/后端/DBA 划分，系统很可能也分成几个大前端、大后端、大数据库项目。要让**微服务架构成功**，**团队也要按业务域切分**。

### 17.2 两个披萨团队（Two-Pizza Team）

- Amazon 经验：一个团队规模不超过 "两个披萨能喂饱"（6-10 人）
- 团队对一个服务**端到端**负责：开发 + 测试 + 部署 + 运维

### 17.3 Inverse Conway Maneuver

**刻意先设计团队结构**，让它塑造我们想要的架构：

- 想要松耦合服务 → 组建松耦合团队
- 想要共同基础设施 → 独立平台团队

### 17.4 平台团队 vs 业务团队

- **业务团队**：离业务近，交付功能
- **平台团队**：提供内部平台（CI/CD、日志、监控、网关）供业务自助使用
- 核心思路：**你构建它，你运行它**（You build it, you run it）

### 17.5 演进路径

```
单体 → 模块化单体 → 拆出第一个服务 → 持续演进 → 成熟微服务体系
```

**不要一步到位**。每一步都先解决痛点，再演进。

### 📝 笔试题 17-1：技术团队 15 人，系统是单体，老板让你转微服务，你怎么办？

慎重评估：

- 痛点是什么？部署慢？故障隔离？扩展？
- 有 CI/CD、容器化、监控吗？没有先补
- 从"模块化单体"开始重构，明确边界
- 挑一个最独立的子域**先拆一个**（绞杀者模式 Strangler Fig）
- 验证流程闭环：DevOps、观测、事故响应
- 逐步扩大，3-6 个月拆 2-3 个，积累能力
- **避免一次性重写**，那是失败率最高的做法

---

## 18. 常见反模式

❌ **分布式单体**：多服务 + 共享数据库 + 同步级联调用 + 统一发布 = 单体所有缺点 + 分布式所有复杂度
❌ **数据共享（Shared DB）**：直接读别人的表
❌ **同步长链路**：A→B→C→D→E 一路等待
❌ **无超时调用**：一处卡顿全链路挂
❌ **无幂等设计**：重试即灾难
❌ **事务穿透**：强求分布式 ACID
❌ **过度拆分**：每个 API 一个服务，调用风暴
❌ **运维裸奔**：没监控没链路就上线
❌ **忽略契约**：随意改接口，口头通知
❌ **网关塞业务**：把订单计算逻辑写网关
❌ **同步调用搭配强一致**：牺牲可用性换一致性，多数场景不值
❌ **单体思维做微服务**：用"一个大 main 类"的思维治理 50 个服务
❌ **为了简历用 K8s / Istio / gRPC**：Resume-Driven Development

---

## 19. 综合笔试练习

### 19.1 选择题

**Q1** 下列**不属于** 微服务核心特征的是？
A. 独立部署
B. 去中心化数据管理
C. 围绕业务能力组织
D. 通过 ESB 统一协调

<details><summary>答案</summary>D。</details>

**Q2** 生产环境下跨服务调用**必须**具备的是？
A. 同步 REST
B. 超时设置
C. 使用同一种语言
D. 共享数据库

<details><summary>答案</summary>B。</details>

**Q3** 关于 Saga 模式，说法错误的是？
A. 由本地事务 + 补偿组成
B. 分编排式与编舞式
C. 保证强一致
D. 每个本地事务要幂等

<details><summary>答案</summary>C（最终一致）。</details>

**Q4** 下列哪项可有效避免"重试风暴"？
A. 下游超时 > 上游超时
B. 每层都无限重试
C. 熔断 + 退避 + 限流
D. 不设超时

<details><summary>答案</summary>C。</details>

**Q5** Service Mesh 主要处理的是？
A. 南北向外部流量
B. 东西向服务间流量
C. 浏览器到服务器
D. 数据库访问

<details><summary>答案</summary>B。</details>

**Q6** 微服务**不推荐**的数据库访问方式？
A. 每服务独占数据库
B. 通过 API 访问其他服务的数据
C. 订阅事件复制到本地
D. 直接连别人的数据库读表

<details><summary>答案</summary>D。</details>

**Q7** API 网关常见职责**不包括**？
A. 鉴权 / 限流
B. 路由 / 聚合
C. 实现核心业务逻辑
D. 可观测性

<details><summary>答案</summary>C。</details>

**Q8** 对微服务部署，正确的是？
A. 使用容器和声明式编排
B. 手工 SSH 改配置最快
C. 所有服务必须同时发版
D. 不要设健康检查

<details><summary>答案</summary>A。</details>

### 19.2 判断题

1. 微服务可以消除分布式系统复杂度。 ❌
2. 共享数据库是微服务反模式。 ✅
3. at-least-once MQ 必须配合幂等消费。 ✅
4. 网关应避免承载业务逻辑。 ✅
5. Service Mesh 必然比没有 Mesh 性能更好。 ❌
6. 金丝雀发布比蓝绿发布资源占用更低。 ✅
7. 团队结构和架构无关。 ❌（康威定律）
8. 分布式事务首选 2PC。 ❌（多用 Saga / 事件驱动）

### 19.3 简答题

**Q1** 解释"为失败设计"，并给出三个具体手段。

在分布式系统中假定**任何远程调用都可能失败**，通过以下手段让系统可以优雅降级：

1. 超时 + 有限重试 + 指数退避
2. 熔断：失败率超阈值快速失败，保护链路
3. 舱壁 + 降级：资源隔离，失败时给兜底结果

**Q2** 简述如何让一个同步 REST 系统演进到事件驱动？

1. 识别可异步的业务（通知、审计、派生数据更新等）
2. 引入 MQ（Kafka/RabbitMQ）
3. 业务服务写 DB 时同步写 **Outbox 表**
4. Publisher 把 outbox 事件发到 MQ（保证 at-least-once）
5. 下游消费者幂等处理
6. 监控 Lag 与处理失败，建立 DLQ 与重放机制
7. 逐步把更多同步调用替换为事件订阅

**Q3** 微服务系统的 "trace_id" 有什么作用？如何传递？

- **作用**：贯穿多服务、多中间件的一次用户请求，用于聚合日志、指标、链路
- **生成**：网关/入口生成 UUID/ULID
- **传递**：HTTP Header（`X-Request-Id` / `traceparent` W3C 规范）、gRPC Metadata、MQ Header
- **传承**：每服务把父 trace_id 记录到日志和下游调用 Header 中；OpenTelemetry SDK 自动处理

**Q4** 说出 3 个"微服务反模式"并给出改进建议。

1. **分布式单体**：共享库 + 同步强依赖 → 数据拆分 + 异步事件
2. **共享数据库**：数据耦合 → 每服务独占，通过 API / 事件共享
3. **无契约随意改接口**：版本灾难 → OpenAPI + 契约测试 + 弃用策略

### 19.4 设计题

**Q1** 设计一个电商订单业务的微服务架构（含事件流）。

```
┌────────┐   REST    ┌──────────┐    ┌──────────┐
│ 客户端 │──────────▶│ API 网关 │────│ Order    │
└────────┘           └──────────┘    │ Service  │
                                      └────┬─────┘
                           发布 OrderCreated │
                                      ┌────▼─────┐ Kafka
                                      │  Broker  │
                                      └──┬──┬──┬─┘
                       ┌─────────────┐  │  │  │  ┌───────────┐
                       │ Inventory   │◀─┘  │  └─▶│ Search/   │
                       │  (扣减)     │     │     │ Analytics │
                       └─────┬───────┘     │     └───────────┘
        InventoryDeducted    │             ▼
                             └────▶ ┌──────────┐
                                    │ Payment  │
                                    └────┬─────┘
                       PaymentSucceeded  │
                                          ▼
                                    Order Service 更新状态
```

- **Outbox Pattern** 保证 DB 事务和事件投递原子
- 每个服务独立数据库、独立部署
- 通知服务、数仓服务订阅相同事件做各自业务
- 补偿：支付失败发 `PaymentFailed` → Order 取消 → Inventory 回补
- mTLS + JWT；网关做限流与鉴权；全链路 trace_id

**Q2** 现有单体下单接口 200ms，拆成订单 + 库存 + 支付三个服务后变成 800ms，如何优化？

- 分析：看链路追踪，定位哪一跳慢（网络？序列化？锁？）
- **并行化**：同时发起能并发的调用
- **异步化**：非关键路径走事件驱动（通知、统计）
- **合并 + 聚合**：BFF 里合并多次查询
- **缓存**：热数据本地 + Redis
- **gRPC**：内部高频调用替代 REST
- **连接池 + keepalive + HTTP/2**
- 拆分**同步关键路径** vs 异步路径
- 若仍慢：考虑**共置部署**或**回退合并**某些服务

**Q3** 团队想从单体演进到微服务，给出 6 个月路线图。

- **第 1 月**：补齐基础——容器化、CI/CD、集中日志、基础监控
- **第 2 月**：模块化单体——清理共享状态、定义模块边界、包级别依赖收敛
- **第 3 月**：先拆 1 个服务（最独立的子域），打通全链路：独立仓库、独立 DB、灰度发布、监控告警
- **第 4 月**：沉淀平台能力——模板项目、网关、注册中心、配置中心
- **第 5 月**：再拆 2-3 个服务；引入 MQ、事件驱动
- **第 6 月**：回顾与固化——契约测试、混沌演练、SLO 定义、团队重组为服务自治

**不要求一次性拆到位**，每一步都先交付可运行的系统，避免长时间"大重写"。

---

## 📚 学习建议

1. **先读经典**：Sam Newman 《Building Microservices》、Chris Richardson 《Microservices Patterns》、《DDD 领域驱动设计》
2. **分布式系统基础**：CAP、BASE、FLP、一致性模型要真的懂
3. **K8s + 可观测性**：现代微服务的运行时基础，越早上手越好
4. **看大厂博客**：Netflix / Uber / Airbnb / 字节 / 美团技术团队的实战文章
5. **动手做一个小微服务系统**：电商/博客/论坛均可，走完 CI/CD/监控/告警/发布全流程比空谈 100 页都有用
6. **警惕时尚驱动**：每个决策问自己"这带来的收益配得上复杂度吗"

> 祝你的服务既小且美，协作顺畅不内耗。
