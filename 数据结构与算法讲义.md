# 数据结构与算法讲义

> 本讲义以"知识点 + 笔试题"的形式组织，覆盖常用数据结构与经典算法，适合笔试面试复习与系统性回顾。
>
> 复杂度约定：`n` 表示元素规模，时间复杂度与空间复杂度分别记作 `T(n)` 与 `S(n)`。

## 目录

1. [基础：复杂度分析](#1-基础复杂度分析)
2. [数组与字符串](#2-数组与字符串)
3. [链表](#3-链表)
4. [栈与队列](#4-栈与队列)
5. [哈希表](#5-哈希表)
6. [树](#6-树)
7. [堆与优先队列](#7-堆与优先队列)
8. [并查集](#8-并查集)
9. [图](#9-图)
10. [排序算法](#10-排序算法)
11. [查找算法](#11-查找算法)
12. [递归与分治](#12-递归与分治)
13. [回溯](#13-回溯)
14. [贪心](#14-贪心)
15. [动态规划](#15-动态规划)
16. [字符串匹配](#16-字符串匹配)
17. [位运算技巧](#17-位运算技巧)
18. [综合笔试练习](#18-综合笔试练习)
19. [Python 内置数据结构深入](#19-python-内置数据结构深入)
20. [Java 内置数据结构深入](#20-java-内置数据结构深入)

---

## 1. 基础：复杂度分析

### 1.1 大 O 记号

- `O(1)`：常数时间，如访问数组下标
- `O(log n)`：对数时间，如二分查找
- `O(n)`：线性时间，如一次遍历
- `O(n log n)`：如归并、快排平均
- `O(n²)`：双重循环
- `O(2ⁿ)`、`O(n!)`：指数、阶乘，通常需剪枝或 DP 优化

### 1.2 主定理（Master Theorem）

对于递推 `T(n) = a·T(n/b) + f(n)`：

- 若 `f(n) = O(n^(log_b a - ε))`，则 `T(n) = Θ(n^log_b a)`
- 若 `f(n) = Θ(n^log_b a)`，则 `T(n) = Θ(n^log_b a · log n)`
- 若 `f(n) = Ω(n^(log_b a + ε))` 且正则性条件满足，则 `T(n) = Θ(f(n))`

### 📝 笔试题 1-1

下列算法的时间复杂度分别是？

```text
① for i=1; i<=n; i*=2 { print(i); }
② for i=1..n: for j=1..i: print(i,j);
③ T(n) = 2T(n/2) + n
```

**答案**：① `O(log n)`；② `O(n²)`（等差求和）；③ `O(n log n)`（归并排序）。

---

## 2. 数组与字符串

### 2.1 核心特性

- 内存**连续**存储，随机访问 `O(1)`
- 插入/删除非末尾元素 `O(n)`
- 动态数组（如 Java `ArrayList`、C++ `vector`）均摊插入 `O(1)`，扩容系数通常为 1.5 或 2

### 2.2 常用技巧

| 技巧 | 适用场景 | 复杂度 |
|------|----------|--------|
| 双指针（对撞） | 有序数组求和、回文 | `O(n)` |
| 双指针（快慢） | 去重、滑动窗口 | `O(n)` |
| 前缀和 | 区间和查询 | 预处理 `O(n)`，查询 `O(1)` |
| 差分数组 | 区间更新 | 更新 `O(1)`，查询 `O(n)` |
| 滑动窗口 | 定长/变长子数组问题 | `O(n)` |

### 2.3 前缀和模板

```python
# 一维前缀和
prefix = [0] * (n + 1)
for i in range(n):
    prefix[i + 1] = prefix[i] + a[i]
# 区间 [l, r] 的和
sum_lr = prefix[r + 1] - prefix[l]
```

### 📝 笔试题 2-1：两数之和 II（有序数组）

给定升序数组 `nums` 和目标 `target`，返回和为 `target` 的两个下标。

```python
def two_sum(nums, target):
    l, r = 0, len(nums) - 1
    while l < r:
        s = nums[l] + nums[r]
        if s == target: return [l, r]
        elif s < target: l += 1
        else: r -= 1
    return [-1, -1]
```

**分析**：双指针对撞，`O(n)` 时间，`O(1)` 空间。

### 📝 笔试题 2-2：最长无重复字符子串

```python
def length_of_longest_substring(s):
    last = {}
    left = ans = 0
    for right, ch in enumerate(s):
        if ch in last and last[ch] >= left:
            left = last[ch] + 1
        last[ch] = right
        ans = max(ans, right - left + 1)
    return ans
```

**核心**：滑动窗口 + 哈希表记录上次出现位置。

---

## 3. 链表

### 3.1 分类

- 单链表：每个节点指向下一个
- 双链表：额外的前驱指针
- 循环链表：尾部连向头部

### 3.2 核心操作复杂度

| 操作 | 数组 | 链表 |
|------|------|------|
| 随机访问 | O(1) | O(n) |
| 头部插入 | O(n) | O(1) |
| 尾部插入 | O(1)* | O(1)（带尾指针）|
| 中间删除 | O(n) | O(1)（已知节点）|

### 3.3 关键技巧

- **哨兵节点**（dummy）：简化头节点处理
- **快慢指针**：找中点、判环、找倒数第 k 个
- **反转链表**：三指针迭代

### 3.4 反转链表模板

```python
def reverse(head):
    prev, cur = None, head
    while cur:
        nxt = cur.next
        cur.next = prev
        prev, cur = cur, nxt
    return prev
```

### 📝 笔试题 3-1：判断链表是否有环（Floyd 判圈）

```python
def has_cycle(head):
    slow = fast = head
    while fast and fast.next:
        slow = slow.next
        fast = fast.next.next
        if slow is fast: return True
    return False
```

**扩展**：若相遇后再让一指针从 `head` 出发，两指针同速前进，再次相遇点即为**环入口**。

### 📝 笔试题 3-2：合并两个升序链表

```python
def merge(l1, l2):
    dummy = ListNode()
    tail = dummy
    while l1 and l2:
        if l1.val <= l2.val:
            tail.next, l1 = l1, l1.next
        else:
            tail.next, l2 = l2, l2.next
        tail = tail.next
    tail.next = l1 or l2
    return dummy.next
```

---

## 4. 栈与队列

### 4.1 栈（LIFO）

- 典型应用：括号匹配、表达式求值、函数调用、DFS、单调栈
- 实现：数组或链表

### 4.2 队列（FIFO）

- 典型应用：BFS、任务调度
- 变种：循环队列、双端队列（deque）、优先队列

### 4.3 单调栈

维护栈内元素**单调递增/递减**，用于求**下一个更大/更小元素**。

```python
def next_greater(nums):
    n = len(nums)
    ans = [-1] * n
    stack = []  # 存下标，栈内对应值单调递减
    for i, x in enumerate(nums):
        while stack and nums[stack[-1]] < x:
            ans[stack.pop()] = x
        stack.append(i)
    return ans
```

### 4.4 单调队列

滑动窗口最大值的经典解法，`O(n)`。

```python
from collections import deque
def max_sliding_window(nums, k):
    dq, ans = deque(), []
    for i, x in enumerate(nums):
        while dq and nums[dq[-1]] <= x:
            dq.pop()
        dq.append(i)
        if dq[0] <= i - k:
            dq.popleft()
        if i >= k - 1:
            ans.append(nums[dq[0]])
    return ans
```

### 📝 笔试题 4-1：有效括号

```python
def is_valid(s):
    pair = {')':'(', ']':'[', '}':'{'}
    st = []
    for c in s:
        if c in '([{':
            st.append(c)
        else:
            if not st or st.pop() != pair[c]:
                return False
    return not st
```

### 📝 笔试题 4-2：用两个栈实现队列

- `in` 栈负责 push；`out` 栈负责 pop
- `pop` 时若 `out` 为空，则把 `in` 全部倒入 `out`
- 均摊 `O(1)`

---

## 5. 哈希表

### 5.1 基本原理

- 通过**哈希函数**将 key 映射到桶
- 冲突解决：
  - **链地址法**（拉链）：桶内用链表或红黑树（Java 8 的 HashMap 当链长 > 8 且数组 ≥ 64 时转红黑树）
  - **开放地址法**：线性探测、二次探测、双重哈希

### 5.2 负载因子

- `load factor = 元素数 / 桶数`
- 超过阈值（如 0.75）触发**扩容**（rehash）

### 5.3 常见题型

- 频次统计
- 去重
- 两数之和 / 两数之差
- 判断字母异位词
- LRU 缓存（哈希 + 双链表）

### 📝 笔试题 5-1：两数之和

```python
def two_sum(nums, target):
    seen = {}
    for i, x in enumerate(nums):
        if target - x in seen:
            return [seen[target - x], i]
        seen[x] = i
```

### 📝 笔试题 5-2：LRU 缓存

核心：**哈希表 + 双向链表**。哈希表定位节点 `O(1)`，双链表维护访问顺序。

```python
class Node:
    __slots__ = ('k', 'v', 'prev', 'next')
    def __init__(self, k=0, v=0):
        self.k, self.v = k, v
        self.prev = self.next = None

class LRUCache:
    def __init__(self, cap):
        self.cap = cap
        self.map = {}
        self.head, self.tail = Node(), Node()
        self.head.next, self.tail.prev = self.tail, self.head

    def _remove(self, node):
        node.prev.next = node.next
        node.next.prev = node.prev

    def _add_front(self, node):
        node.next = self.head.next
        node.prev = self.head
        self.head.next.prev = node
        self.head.next = node

    def get(self, k):
        if k not in self.map: return -1
        node = self.map[k]
        self._remove(node); self._add_front(node)
        return node.v

    def put(self, k, v):
        if k in self.map:
            node = self.map[k]; node.v = v
            self._remove(node); self._add_front(node)
            return
        if len(self.map) == self.cap:
            lru = self.tail.prev
            self._remove(lru); del self.map[lru.k]
        node = Node(k, v)
        self.map[k] = node; self._add_front(node)

```

---

## 6. 树

### 6.1 基本概念

- **节点**：包含值与若干子节点引用
- **度**：子节点数；**树的度**：最大节点度
- **深度** / **高度**：根到节点 / 节点到叶的最长路径
- **满二叉树**：每层都满；**完全二叉树**：最后一层左起连续填满

### 6.2 二叉树遍历

| 遍历 | 顺序 | 典型用途 |
|------|------|----------|
| 前序 | 根 → 左 → 右 | 复制树、序列化 |
| 中序 | 左 → 根 → 右 | BST 有序输出 |
| 后序 | 左 → 右 → 根 | 释放节点、表达式求值 |
| 层序 | 逐层遍历 | 最短路径、按层问题 |

#### 递归模板

```python
def inorder(root, ans):
    if not root: return
    inorder(root.left, ans)
    ans.append(root.val)
    inorder(root.right, ans)
```

#### 迭代中序

```python
def inorder(root):
    st, ans, cur = [], [], root
    while cur or st:
        while cur:
            st.append(cur); cur = cur.left
        cur = st.pop()
        ans.append(cur.val)
        cur = cur.right
    return ans
```

#### BFS 层序

```python
from collections import deque
def level_order(root):
    if not root: return []
    q, ans = deque([root]), []
    while q:
        level = []
        for _ in range(len(q)):
            node = q.popleft()
            level.append(node.val)
            if node.left:  q.append(node.left)
            if node.right: q.append(node.right)
        ans.append(level)
    return ans
```

### 6.3 二叉搜索树（BST）

- 左子树 < 根 < 右子树
- 中序遍历有序
- 平均查找/插入/删除 `O(log n)`，最坏 `O(n)`（退化为链）

### 6.4 平衡二叉树

- **AVL**：每个节点左右子树高差 ≤ 1，旋转严格维持平衡
- **红黑树**：弱平衡，插入删除旋转次数更少；STL `map`、Java `TreeMap` 底层
- **B / B+ 树**：多路平衡查找树，数据库索引、文件系统

### 6.5 Trie（字典树）

用于字符串前缀检索，查询 `O(L)`，L 为串长。

```python
class Trie:
    def __init__(self):
        self.children = {}
        self.end = False
    def insert(self, word):
        node = self
        for c in word:
            node = node.children.setdefault(c, Trie())
        node.end = True
    def search(self, word):
        node = self
        for c in word:
            if c not in node.children: return False
            node = node.children[c]
        return node.end
```

### 6.6 线段树（简述）

区间查询 + 区间更新的经典结构，构建 `O(n)`，单次查询/更新 `O(log n)`。配合**懒标记**支持区间修改。

### 📝 笔试题 6-1：二叉树最大深度

```python
def max_depth(root):
    if not root: return 0
    return 1 + max(max_depth(root.left), max_depth(root.right))
```

### 📝 笔试题 6-2：验证 BST

**陷阱**：仅比较当前节点和左右孩子不够，需传入上下界。

```python
def is_bst(root, lo=float('-inf'), hi=float('inf')):
    if not root: return True
    if not (lo < root.val < hi): return False
    return is_bst(root.left, lo, root.val) and is_bst(root.right, root.val, hi)
```

### 📝 笔试题 6-3：二叉树最近公共祖先（LCA）

```python
def lowest_common_ancestor(root, p, q):
    if not root or root is p or root is q: return root
    L = lowest_common_ancestor(root.left,  p, q)
    R = lowest_common_ancestor(root.right, p, q)
    if L and R: return root
    return L or R
```

---

## 7. 堆与优先队列

### 7.1 堆的性质

- **二叉堆**：完全二叉树，父节点 ≤（小根）或 ≥（大根）子节点
- 数组存储：下标 `i` 的左右孩子为 `2i+1`, `2i+2`，父为 `(i-1)//2`

### 7.2 关键操作

| 操作 | 复杂度 |
|------|--------|
| 插入（上浮） | `O(log n)` |
| 弹出堆顶（下沉） | `O(log n)` |
| 建堆（自底向上下沉） | `O(n)` |
| 取堆顶 | `O(1)` |

### 7.3 典型应用

- Top-K 问题：维护大小为 K 的堆
- 堆排序
- Dijkstra、Prim 算法
- 数据流中位数（大小根堆）

### 📝 笔试题 7-1：数组中第 K 大的元素

```python
import heapq
def find_kth_largest(nums, k):
    h = []
    for x in nums:
        heapq.heappush(h, x)
        if len(h) > k:
            heapq.heappop(h)
    return h[0]
```

**复杂度**：`O(n log k)`。

### 📝 笔试题 7-2：数据流中位数

用**大根堆**存较小的一半，**小根堆**存较大的一半，保持两堆大小差 ≤ 1。

- 新元素先入大根堆，再把堆顶移入小根堆
- 若小根堆更大，则把堆顶再移回大根堆
- 中位数：元素数奇则大根堆顶；偶则两堆顶均值

---

## 8. 并查集

维护不相交集合，支持合并和查询。

### 8.1 核心操作

- `find(x)`：找 x 的根（代表元）
- `union(x, y)`：合并两个集合

### 8.2 优化

- **路径压缩**：`find` 时把路径上所有节点直接挂到根
- **按秩合并**（或按大小合并）：把小树挂到大树下

两者同用后，单次近似 `O(α(n))`（阿克曼反函数，几乎常数）。

```python
class DSU:
    def __init__(self, n):
        self.p = list(range(n))
        self.r = [0] * n
    def find(self, x):
        while self.p[x] != x:
            self.p[x] = self.p[self.p[x]]  # 路径压缩（隔代）
            x = self.p[x]
        return x
    def union(self, x, y):
        rx, ry = self.find(x), self.find(y)
        if rx == ry: return False
        if self.r[rx] < self.r[ry]: rx, ry = ry, rx
        self.p[ry] = rx
        if self.r[rx] == self.r[ry]: self.r[rx] += 1
        return True
```

### 📝 笔试题 8-1：省份数量（连通分量）

给邻接矩阵 `isConnected`，返回连通分量数。用并查集合并后统计根数量。

---

## 9. 图

### 9.1 表示

- **邻接矩阵**：`O(V²)` 空间，适合稠密图
- **邻接表**：`O(V + E)` 空间，适合稀疏图

### 9.2 遍历

- **DFS**：递归或栈，用于拓扑排序、连通分量、环检测
- **BFS**：队列，用于无权图最短路径

```python
from collections import deque
def bfs(graph, start):
    visited = {start}
    q = deque([start])
    while q:
        u = q.popleft()
        for v in graph[u]:
            if v not in visited:
                visited.add(v); q.append(v)
```

### 9.3 拓扑排序（Kahn 算法）

```python
def topo_sort(n, edges):
    from collections import defaultdict, deque
    g = defaultdict(list); ind = [0] * n
    for u, v in edges:
        g[u].append(v); ind[v] += 1
    q = deque(i for i in range(n) if ind[i] == 0)
    ans = []
    while q:
        u = q.popleft(); ans.append(u)
        for v in g[u]:
            ind[v] -= 1
            if ind[v] == 0: q.append(v)
    return ans if len(ans) == n else []  # 有环则返回空
```

### 9.4 最短路径

| 算法 | 适用 | 复杂度 |
|------|------|--------|
| BFS | 无权图 | `O(V+E)` |
| Dijkstra | 非负权单源 | `O((V+E) log V)`（堆优化） |
| Bellman-Ford | 含负权，可判负环 | `O(VE)` |
| Floyd-Warshall | 多源全对最短路 | `O(V³)` |
| SPFA | 含负权平均较快 | 最坏 `O(VE)` |

#### Dijkstra 模板

```python
import heapq
def dijkstra(graph, src, n):
    dist = [float('inf')] * n
    dist[src] = 0
    pq = [(0, src)]
    while pq:
        d, u = heapq.heappop(pq)
        if d > dist[u]: continue
        for v, w in graph[u]:
            nd = d + w
            if nd < dist[v]:
                dist[v] = nd
                heapq.heappush(pq, (nd, v))
    return dist
```

### 9.5 最小生成树

- **Kruskal**：按边权排序 + 并查集，`O(E log E)`
- **Prim**：类似 Dijkstra，维护到生成树的最小距离

### 📝 笔试题 9-1：课程表 II

给定先修关系，返回一种合法学习顺序。→ 直接用拓扑排序。

---

## 10. 排序算法

### 10.1 比较表

| 算法 | 平均 | 最坏 | 空间 | 稳定 |
|------|------|------|------|------|
| 冒泡 | O(n²) | O(n²) | O(1) | ✅ |
| 选择 | O(n²) | O(n²) | O(1) | ❌ |
| 插入 | O(n²) | O(n²) | O(1) | ✅ |
| 希尔 | O(n^1.3) | O(n²) | O(1) | ❌ |
| 归并 | O(n log n) | O(n log n) | O(n) | ✅ |
| 快排 | O(n log n) | O(n²) | O(log n) | ❌ |
| 堆排 | O(n log n) | O(n log n) | O(1) | ❌ |
| 计数 | O(n+k) | O(n+k) | O(k) | ✅ |
| 桶排 | O(n+k) | O(n²) | O(n+k) | ✅ |
| 基数 | O(d(n+k)) | — | O(n+k) | ✅ |

**稳定性**：相等元素排序后相对位置保持不变。

### 10.2 快速排序

```python
def quick_sort(a, l, r):
    if l >= r: return
    pivot = a[(l + r) // 2]
    i, j = l, r
    while i <= j:
        while a[i] < pivot: i += 1
        while a[j] > pivot: j -= 1
        if i <= j:
            a[i], a[j] = a[j], a[i]
            i += 1; j -= 1
    quick_sort(a, l, j)
    quick_sort(a, i, r)
```

**优化**：三数取中、小数组用插入排序、递归改迭代尾递归优化。

### 10.3 归并排序

```python
def merge_sort(a):
    if len(a) <= 1: return a
    m = len(a) // 2
    L = merge_sort(a[:m]); R = merge_sort(a[m:])
    res = []; i = j = 0
    while i < len(L) and j < len(R):
        if L[i] <= R[j]:
            res.append(L[i]); i += 1
        else:
            res.append(R[j]); j += 1
    res.extend(L[i:]); res.extend(R[j:])
    return res
```

### 10.4 堆排序

1. 自底向上建大根堆：从 `n//2 - 1` 到 `0` 依次下沉
2. 反复取堆顶与末尾交换，堆大小减 1，再下沉

### 📝 笔试题 10-1：用 O(n) 时间排序范围已知的整数

→ 计数排序。若值域 `[0, k]`，`O(n + k)`。

### 📝 笔试题 10-2：为什么快排最坏是 `O(n²)`？

当每次选到极值作为 pivot 时，划分成 `0` 和 `n-1` 两部分，递归深度 `n`，总比较次数 `O(n²)`。随机化 pivot 或三数取中可降低概率。

---

## 11. 查找算法

### 11.1 二分查找模板

**查找目标值**（闭区间）：

```python
def binary_search(a, target):
    l, r = 0, len(a) - 1
    while l <= r:
        m = (l + r) // 2
        if a[m] == target: return m
        elif a[m] < target: l = m + 1
        else: r = m - 1
    return -1
```

**查找左边界**（第一个 ≥ target）：

```python
def lower_bound(a, target):
    l, r = 0, len(a)
    while l < r:
        m = (l + r) // 2
        if a[m] < target: l = m + 1
        else: r = m
    return l
```

### 11.2 二分答案

当答案具有**单调性**（可行 → 更小/更大也可行）时，对答案二分，验证可行性。

典型：搬运工分配、传送带、最小最大值问题。

### 📝 笔试题 11-1：旋转有序数组查找

```python
def search(a, target):
    l, r = 0, len(a) - 1
    while l <= r:
        m = (l + r) // 2
        if a[m] == target: return m
        if a[l] <= a[m]:  # 左半有序
            if a[l] <= target < a[m]: r = m - 1
            else: l = m + 1
        else:            # 右半有序
            if a[m] < target <= a[r]: l = m + 1
            else: r = m - 1
    return -1
```

---

## 12. 递归与分治

### 12.1 递归三要素

1. **终止条件**：避免无限递归
2. **本层做什么**：处理当前子问题
3. **返回什么**：向上层提供结果

### 12.2 分治三步骤

- **分（Divide）**：拆成子问题
- **治（Conquer）**：递归求解
- **合（Combine）**：合并子结果

典型：归并排序、快速排序、最近点对、大整数乘法（Karatsuba）、FFT。

### 📝 笔试题 12-1：Pow(x, n) 快速幂

```python
def my_pow(x, n):
    if n < 0: x, n = 1 / x, -n
    ans = 1.0
    while n:
        if n & 1: ans *= x
        x *= x
        n >>= 1
    return ans
```

**思路**：`x^n = (x^(n/2))²`，`O(log n)`。

---

## 13. 回溯

回溯 = DFS + 状态恢复。模板：

```python
def backtrack(path, choices):
    if 满足结束条件:
        ans.append(path[:])
        return
    for c in choices:
        if 不合法: continue
        path.append(c)           # 做选择
        backtrack(path, next_choices)
        path.pop()               # 撤销选择
```

### 📝 笔试题 13-1：全排列

```python
def permute(nums):
    ans, path, used = [], [], [False] * len(nums)
    def dfs():
        if len(path) == len(nums):
            ans.append(path[:]); return
        for i, x in enumerate(nums):
            if used[i]: continue
            used[i] = True; path.append(x)
            dfs()
            path.pop(); used[i] = False
    dfs()
    return ans
```

### 📝 笔试题 13-2：N 皇后

按行放置，用三个集合标记**列**、**主对角线 (r-c)**、**副对角线 (r+c)** 是否被占用即可 `O(1)` 判合法。

### 📝 笔试题 13-3：子集

```python
def subsets(nums):
    ans = []
    def dfs(i, path):
        if i == len(nums):
            ans.append(path[:]); return
        dfs(i + 1, path)              # 不选
        path.append(nums[i])
        dfs(i + 1, path); path.pop()  # 选
    dfs(0, [])
    return ans
```

---

## 14. 贪心

**核心**：每步做**当前看起来最优**的选择，期望得到全局最优。

### 14.1 证明方法

- **交换论证**：假设有更优解，证明可交换成贪心解而不变差
- **数学归纳**：对问题规模归纳

### 14.2 典型题

- 区间调度（按结束时间排序）
- 跳跃游戏（维护最远可达）
- 分发饼干
- 哈夫曼编码

### 📝 笔试题 14-1：无重叠区间

```python
def erase_overlap(intervals):
    intervals.sort(key=lambda x: x[1])
    end = float('-inf'); removed = 0
    for l, r in intervals:
        if l >= end: end = r
        else: removed += 1
    return removed
```

### 📝 笔试题 14-2：跳跃游戏

```python
def can_jump(nums):
    reach = 0
    for i, x in enumerate(nums):
        if i > reach: return False
        reach = max(reach, i + x)
    return True
```

---

## 15. 动态规划

### 15.1 DP 四要素

1. **状态定义**：`dp[i]` 代表什么
2. **状态转移方程**
3. **初始化 / 边界**
4. **计算顺序**（保证子问题先计算）

### 15.2 经典模型

| 模型 | 典型题 | 状态 |
|------|--------|------|
| 线性 DP | 爬楼梯、打家劫舍 | `dp[i]` |
| 背包 | 0-1 / 完全 / 多重背包 | `dp[i][j]` |
| 区间 DP | 矩阵链乘、石子合并 | `dp[l][r]` |
| 树形 DP | 树上最大独立集 | 子树状态 |
| 状压 DP | TSP、棋盘覆盖 | `dp[mask]` |
| 数位 DP | 区间内符合条件的数 | 记忆化搜索 |

### 15.3 0-1 背包

```python
def knapsack01(w, v, W):
    dp = [0] * (W + 1)
    for i in range(len(w)):
        for j in range(W, w[i] - 1, -1):  # 倒序防重
            dp[j] = max(dp[j], dp[j - w[i]] + v[i])
    return dp[W]
```

### 15.4 完全背包（物品可无限次）

```python
for i in range(n):
    for j in range(w[i], W + 1):   # 正序
        dp[j] = max(dp[j], dp[j - w[i]] + v[i])
```

### 15.5 最长递增子序列（LIS）

朴素 `O(n²)`：

```python
def LIS(a):
    n = len(a); dp = [1] * n
    for i in range(n):
        for j in range(i):
            if a[j] < a[i]:
                dp[i] = max(dp[i], dp[j] + 1)
    return max(dp)
```

优化 `O(n log n)`：维护 `tails` 数组，`tails[k]` 表示长度为 `k+1` 的 LIS 结尾最小值，二分更新。

### 15.6 最长公共子序列（LCS）

```python
def LCS(s, t):
    m, n = len(s), len(t)
    dp = [[0] * (n + 1) for _ in range(m + 1)]
    for i in range(1, m + 1):
        for j in range(1, n + 1):
            if s[i-1] == t[j-1]:
                dp[i][j] = dp[i-1][j-1] + 1
            else:
                dp[i][j] = max(dp[i-1][j], dp[i][j-1])
    return dp[m][n]
```

### 15.7 编辑距离

```python
def edit_distance(s, t):
    m, n = len(s), len(t)
    dp = [[0]*(n+1) for _ in range(m+1)]
    for i in range(m+1): dp[i][0] = i
    for j in range(n+1): dp[0][j] = j
    for i in range(1, m+1):
        for j in range(1, n+1):
            if s[i-1] == t[j-1]:
                dp[i][j] = dp[i-1][j-1]
            else:
                dp[i][j] = 1 + min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1])
    return dp[m][n]
```

### 📝 笔试题 15-1：打家劫舍

```python
def rob(nums):
    prev = cur = 0
    for x in nums:
        prev, cur = cur, max(cur, prev + x)
    return cur
```

**状态**：`dp[i] = max(dp[i-1], dp[i-2] + nums[i])`。

### 📝 笔试题 15-2：零钱兑换（最少硬币数）

```python
def coin_change(coins, amount):
    INF = amount + 1
    dp = [0] + [INF] * amount
    for j in range(1, amount + 1):
        for c in coins:
            if j >= c:
                dp[j] = min(dp[j], dp[j-c] + 1)
    return dp[amount] if dp[amount] < INF else -1
```

---

## 16. 字符串匹配

### 16.1 朴素匹配

双重循环，最坏 `O(nm)`。

### 16.2 KMP

利用**失败指针 next 数组**避免主串回溯，`O(n + m)`。

```python
def kmp(text, pat):
    if not pat: return 0
    nxt = [0] * len(pat)
    k = 0
    for i in range(1, len(pat)):
        while k and pat[k] != pat[i]:
            k = nxt[k-1]
        if pat[k] == pat[i]: k += 1
        nxt[i] = k
    k = 0
    for i, c in enumerate(text):
        while k and pat[k] != c:
            k = nxt[k-1]
        if pat[k] == c: k += 1
        if k == len(pat): return i - k + 1
    return -1
```

### 16.3 其他

- **Rabin-Karp**：滚动哈希，均摊 `O(n + m)`
- **BM**：坏字符 + 好后缀，常数小
- **Z 算法**：求与前缀的最长匹配
- **Manacher**：`O(n)` 求所有回文子串

### 📝 笔试题 16-1：最长回文子串

- 中心扩展：`O(n²)`
- Manacher：`O(n)`
- 区间 DP：`dp[i][j] = (s[i]==s[j]) and (j-i<2 or dp[i+1][j-1])`

---

## 17. 位运算技巧

| 操作 | 公式 |
|------|------|
| 判奇偶 | `x & 1` |
| 清除最低位 1 | `x & (x - 1)` |
| 取最低位 1 | `x & (-x)`（lowbit） |
| 判 2 的幂 | `x > 0 and (x & (x-1)) == 0` |
| 交换两数（不用临时变量） | `a ^= b; b ^= a; a ^= b;` |
| 大小写转换 | `c ^= 32`（英文字母） |
| 第 i 位置 1 / 清 0 | `x |= (1<<i)` / `x &= ~(1<<i)` |

### 📝 笔试题 17-1：数组中只出现一次的数

其他元素都出现两次，异或全部元素即得。

```python
def single(nums):
    r = 0
    for x in nums: r ^= x
    return r
```

### 📝 笔试题 17-2：统计二进制中 1 的个数

```python
def popcount(x):
    c = 0
    while x:
        x &= x - 1
        c += 1
    return c
```

---

## 18. 综合笔试练习

### 18-1 选择题

**Q1** 以下哪种结构**不支持** `O(1)` 随机访问？
A. 数组  B. 哈希表（按 key）  C. 跳表  D. 位图

<details><summary>答案</summary>C。跳表查找为 `O(log n)` 期望。</details>

**Q2** 快速排序平均时间复杂度是？
A. O(n)  B. O(n log n)  C. O(n²)  D. O(log n)

<details><summary>答案</summary>B。</details>

**Q3** 以下关于红黑树叙述**错误**的是？
A. 根节点是黑色
B. 红节点的子节点必为黑
C. 任一节点到其叶节点的所有路径黑节点数相同
D. 红黑树是严格平衡的 AVL 树

<details><summary>答案</summary>D。红黑树是**弱平衡**，最长路径不超过最短路径的 2 倍。</details>

**Q4** 堆排序的空间复杂度是？
A. O(1)  B. O(log n)  C. O(n)  D. O(n log n)

<details><summary>答案</summary>A。原地排序。</details>

**Q5** 使用 BFS 寻找无权图最短路径，时间复杂度为？
A. O(V)  B. O(V+E)  C. O(V²)  D. O(E log V)

<details><summary>答案</summary>B。</details>

### 18-2 判断题

1. 哈希表的查找在最坏情况下为 `O(1)`。 ❌（最坏 `O(n)`）
2. 归并排序是稳定排序。 ✅
3. DFS 可用来判断有向图是否存在环。 ✅（三色标记法）
4. 最小生成树一定唯一。 ❌（边权存在重复时可能不唯一）
5. 动态规划要求子问题满足**最优子结构**和**重叠子问题**。 ✅

### 18-3 简答题

**Q1** 简述 Dijkstra 与 Bellman-Ford 的区别。

- Dijkstra：贪心 + 堆，仅适用于**非负权**；`O((V+E) log V)`
- Bellman-Ford：对所有边做 `V-1` 轮松弛，支持**负权**且能**检测负环**；`O(VE)`

**Q2** 解释动态规划与分治的区别。

分治的子问题**独立**；动态规划的子问题**重叠**，需存储结果避免重复计算。

**Q3** 什么是摊还分析？举例说明。

通过多次操作的**总代价**平均化到单次操作。例：动态数组扩容——单次扩容 `O(n)`，但 n 次插入总代价 `O(n)`，均摊每次 `O(1)`。

### 18-4 编程题

**Q1** 给定一个整数数组，找出**和为 `k`** 的**连续子数组个数**。

```python
def subarray_sum(nums, k):
    from collections import defaultdict
    cnt = defaultdict(int); cnt[0] = 1
    s = ans = 0
    for x in nums:
        s += x
        ans += cnt[s - k]
        cnt[s] += 1
    return ans
```

**思路**：前缀和 + 哈希，`O(n)`。

**Q2** 给 `n` 个任务和冷却时间 `k`，相同任务之间需隔至少 `k` 个时间单位，求最少耗时。

```python
def least_interval(tasks, k):
    from collections import Counter
    cnt = Counter(tasks).values()
    mx = max(cnt)
    same = sum(1 for c in cnt if c == mx)
    return max(len(tasks), (mx - 1) * (k + 1) + same)
```

**思路**：贪心——先填最多的任务，空位再填其他。

**Q3** 实现一个线程安全的**单例** LRU 缓存（伪代码即可）。

要点：双检锁 or 静态内部类实现单例；LRU 本体见第 5 节；多线程需对 `get/put` 加锁或用 `ConcurrentHashMap + ReadWriteLock`。

**Q4** 求一个字符串的所有不同子序列个数。

设 `dp[i]` 为前 `i` 个字符的不同子序列数：

- `dp[i] = 2 * dp[i-1] - dp[last[s[i]] - 1]`
- `last[c]` 记录字符 `c` 上次出现位置，初始 0；首次出现减去 0
- 结果 `dp[n] - 1`（去掉空序列）

---

## 📚 复习建议

1. **自顶向下**：先建立每种结构的"心智模型"，再抠细节
2. **写而非读**：每个模板亲手默写一遍，关注边界
3. **一题多解**：同一题用暴力 / 优化 / 最优解分别写，比较复杂度
4. **画图**：链表、树、图类问题画图是高效的思考方式
5. **复习节奏**：算法题遗忘曲线陡峭，建议 1 / 3 / 7 / 14 天滚动复习

> 祝笔试顺利！


---

## 19. Python 内置数据结构深入

Python 提供了丰富的内置数据结构，熟悉其**底层实现**与**时间复杂度**对写出高效代码至关重要。

### 19.1 总览对比

| 结构 | 可变 | 有序 | 可重复 | 可哈希 | 典型场景 |
|------|------|------|--------|--------|----------|
| `list` | ✅ | ✅ | ✅ | ❌ | 动态数组、栈 |
| `tuple` | ❌ | ✅ | ✅ | ✅（元素均可哈希时） | 不可变记录、字典键 |
| `str` | ❌ | ✅ | ✅ | ✅ | 文本 |
| `bytes` | ❌ | ✅ | ✅ | ✅ | 二进制数据 |
| `bytearray` | ✅ | ✅ | ✅ | ❌ | 可变二进制 |
| `set` | ✅ | ❌ | ❌ | ❌ | 去重、集合运算 |
| `frozenset` | ❌ | ❌ | ❌ | ✅ | 不可变集合、可作字典键 |
| `dict` | ✅ | ✅（3.7+ 保持插入序） | 键唯一 | ❌ | 键值映射 |

> 提示：Python 3.7 起 `dict` 官方保证按**插入顺序**迭代。

### 19.2 List（动态数组）

#### 底层实现

CPython 的 `list` 是指向 `PyObject*` 的**变长数组**（类似 C++ `vector`）。扩容策略约为 `new_size = old_size + (old_size >> 3) + 6`（大约 1.125 倍），比 Java 的 1.5 倍更保守。

#### 常用操作复杂度

| 操作 | 复杂度 | 说明 |
|------|--------|------|
| `a[i]` / `a[i] = x` | `O(1)` | 随机访问 |
| `a.append(x)` | 均摊 `O(1)` | 末尾添加 |
| `a.pop()` | `O(1)` | 末尾弹出 |
| `a.pop(0)` / `a.insert(0, x)` | `O(n)` | 头部操作需整体搬移 |
| `a.remove(x)` | `O(n)` | 线性查找 |
| `x in a` | `O(n)` | 线性扫描 |
| `a.sort()` | `O(n log n)` | Timsort，稳定 |
| `a + b` | `O(n + m)` | 生成新列表 |
| `a[i:j]` | `O(j - i)` | 切片拷贝 |

#### 实用技巧

```python
# 列表推导式：通常比 for 循环 + append 快
squares = [x * x for x in range(10) if x % 2 == 0]

# 反向遍历
for x in reversed(a): ...

# 解包（Python 3）
first, *middle, last = [1, 2, 3, 4, 5]  # first=1, middle=[2,3,4], last=5

# 二维列表的正确初始化
grid = [[0] * n for _ in range(m)]
# 错误写法：grid = [[0] * n] * m  → 所有行是同一引用！
```

### 19.3 Tuple（不可变序列）

#### 特点

- **不可变**：创建后元素不可替换
- **可哈希**（当所有元素均可哈希时）：可作为 `dict` 键、`set` 元素
- **内存更省**：相同元素下通常比 `list` 占用更少

```python
import sys
sys.getsizeof([1, 2, 3])   # 80 (CPython 3.11)
sys.getsizeof((1, 2, 3))   # 64
```

#### 语法陷阱

```python
t = (1)      # 整数 1，不是元组！
t = (1,)     # 单元素元组
t = 1, 2, 3  # 括号可省，本质是逗号
```

#### 命名元组（`namedtuple`）

```python
from collections import namedtuple
Point = namedtuple('Point', ['x', 'y'])
p = Point(3, 4)
p.x, p.y       # 3, 4
p[0]           # 仍可按索引访问
```

命名元组兼顾 tuple 的不可变性与结构体式的可读性，Python 3.6+ 更推荐 `typing.NamedTuple` 或 `dataclass(frozen=True)`。

### 19.4 Dict（哈希表）

#### 底层实现

- CPython 3.6+ 采用**紧凑字典**（compact dict）：
  - 一个稀疏的 `indices` 数组（索引表）+ 一个紧凑的 `entries` 数组（`hash, key, value`）
  - 节省内存且天然保留**插入顺序**
- 冲突解决：**开放地址法**（扰动探测 perturbation probing）
- 负载因子 ≥ 2/3 时扩容，容量按 2 的幂

#### 操作复杂度

| 操作 | 平均 | 最坏 |
|------|------|------|
| `d[k]` / `d[k] = v` | `O(1)` | `O(n)` |
| `k in d` | `O(1)` | `O(n)` |
| `del d[k]` | `O(1)` | `O(n)` |
| 迭代 | `O(n)` | `O(n)` |

最坏情况仅在大量**哈希冲突**（或被构造的攻击性 key）下出现；Python 默认对字符串哈希加**随机 salt**（`PYTHONHASHSEED`）防止 HashDoS。

#### 常用操作

```python
d = {'a': 1, 'b': 2}

d.get('c', 0)            # 不存在返回默认值 0
d.setdefault('c', []).append(1)  # 若不存在则置空列表再 append

# 合并（Python 3.9+）
{'a': 1} | {'b': 2}       # {'a': 1, 'b': 2}

# 字典推导式
{k: v for k, v in items if v > 0}

# 反转（值作键，要求值唯一且可哈希）
{v: k for k, v in d.items()}
```

#### 可哈希要求

- 键必须**可哈希**：`hash(x)` 可调用，且 `x == y ⇒ hash(x) == hash(y)`
- 内置不可变类型（数字、字符串、元组、`frozenset`）均可哈希
- `list`、`dict`、`set` **不可哈希**
- 自定义类默认按 `id` 哈希；若重写 `__eq__`，必须同步重写 `__hash__`，否则实例变为不可哈希

### 19.5 Set / Frozenset

#### 特点

- 底层同样是哈希表，**只存键不存值**
- 不保证顺序（CPython 中 `set` 无插入序保证，与 `dict` 不同）
- 支持集合运算：`|` 并，`&` 交，`-` 差，`^` 对称差

```python
a = {1, 2, 3}; b = {2, 3, 4}
a | b    # {1, 2, 3, 4}
a & b    # {2, 3}
a - b    # {1}
a ^ b    # {1, 4}

# 子集/超集
{1, 2} <= a       # True

# 空集
s = set()         # 注意 {} 是空 dict
```

`frozenset` 不可变、可哈希，可作 `dict` 键。

### 19.6 String / Bytes

- `str` 不可变；**每次拼接会生成新字符串**
- 多段拼接应使用 `''.join(iterable)` 而非 `+=` 循环
- CPython 对短字符串与**标识符样**字符串做**驻留（intern）**，相同内容共享内存

```python
# 反例：O(n²)
s = ''
for w in words:
    s += w

# 正例：O(total_len)
s = ''.join(words)
```

`bytes` 与 `str` 类似但存储原始字节；`bytearray` 为可变版本，适合大量字节级修改。

### 19.7 `collections` 常用结构

`collections` 模块提供了若干工程常用的特化容器。

#### `deque`（双端队列）

- 基于**双向链表**（分段数组）实现
- 两端 `append` / `pop` 均为 `O(1)`
- 可作队列或栈；支持 `rotate`、`maxlen`

```python
from collections import deque
q = deque([1, 2, 3], maxlen=5)
q.append(4)         # 右端入队
q.appendleft(0)     # 左端入队
q.pop(); q.popleft()
q.rotate(1)         # 整体右移 1
```

> BFS 一律用 `deque`，不要用 `list.pop(0)`（`O(n)`）。

#### `Counter`（计数器）

```python
from collections import Counter
c = Counter('abracadabra')
c.most_common(2)          # [('a', 5), ('b', 2)]
c['a'] -= 1
c + Counter('ab')         # 加法合并
c & Counter('abb')        # 取小
```

常用于词频、子串统计、滑动窗口。

#### `defaultdict`

访问不存在的键时，自动调用**工厂函数**生成默认值：

```python
from collections import defaultdict
g = defaultdict(list)
for u, v in edges:
    g[u].append(v)        # 无需先判断 key 是否存在

cnt = defaultdict(int)
for x in nums: cnt[x] += 1
```

#### `OrderedDict`

在 3.7 前用来保证插入序；现在 `dict` 已保证。仍独有两个能力：

- `move_to_end(key, last=True/False)`：移到尾或首
- `popitem(last=True/False)`：从尾或首弹出

构建 LRU 时 `OrderedDict` 非常便捷。

#### `ChainMap`

将多个 dict **逻辑合并**，查找时按顺序搜索，写入仅作用于第一个：

```python
from collections import ChainMap
defaults = {'color': 'red', 'user': 'guest'}
cfg = {'user': 'alice'}
merged = ChainMap(cfg, defaults)
merged['color'], merged['user']  # 'red', 'alice'
```

### 19.8 `heapq`（小根堆）

`heapq` 在普通 `list` 上实现**小根堆**。

```python
import heapq
h = []
heapq.heappush(h, 3)
heapq.heappush(h, 1)
heapq.heappop(h)            # 1
heapq.heapify([3, 1, 2])    # 原地 O(n) 建堆
heapq.nlargest(k, data)     # Top-K
heapq.nsmallest(k, data)
```

**大根堆**技巧：入堆时取相反数。

```python
heapq.heappush(h, -x)
max_val = -heapq.heappop(h)
```

### 19.9 `array` 与 `numpy.ndarray`

- `array.array('i', [...])`：类型化数组，内存更紧凑（元素为 C 原生类型）
- `numpy.ndarray`：多维数组 + 向量化运算，科学计算首选；数值类工作优先选它而非 `list`

### 19.10 可变性与浅/深拷贝

```python
import copy
a = [[1, 2], [3, 4]]
b = a                  # 引用同一对象
c = a.copy()           # 浅拷贝：外层新列表，内层仍共享
d = copy.deepcopy(a)   # 深拷贝：完全独立
a[0].append(9)
# b[0]==[1,2,9]  c[0]==[1,2,9]  d[0]==[1,2]
```

**常见坑**：函数**默认参数**使用可变对象会在调用间**共享**。

```python
# 反例
def f(x, acc=[]):        # acc 在所有调用间共享！
    acc.append(x); return acc

# 正例
def f(x, acc=None):
    if acc is None: acc = []
    acc.append(x); return acc
```

### 19.11 迭代器与生成器

- **可迭代（Iterable）**：实现 `__iter__`，如 `list`, `dict`, `str`
- **迭代器（Iterator）**：实现 `__next__`，单次使用
- **生成器（Generator）**：用 `yield` 定义，惰性计算、节省内存

```python
def fib():
    a, b = 0, 1
    while True:
        yield a
        a, b = b, a + b

import itertools
first10 = list(itertools.islice(fib(), 10))
```

生成器表达式：`(x * x for x in range(n))`，**不**占用 `O(n)` 内存。

### 19.12 性能速查与选型

| 需求 | 选择 |
|------|------|
| 下标随机访问 | `list`、`tuple` |
| 频繁首尾增删 | `deque` |
| 频繁成员判断 | `set` / `dict`（避免 `list in`） |
| 键值映射 | `dict` |
| 计数/频次 | `Counter` |
| 邻接表 | `defaultdict(list)` |
| 堆 / 优先队列 | `heapq` |
| 大规模数值计算 | `numpy.ndarray` |
| 线程安全队列 | `queue.Queue`（阻塞、加锁） |

### 📝 笔试题 19-1：为什么 `list` 不能作为 `dict` 的键？

因为 `list` 是**可变**的，其 `hash` 会随内容变化，无法维持哈希表定位一致性；Python 在 `list` 上调用 `hash` 会直接抛 `TypeError: unhashable type`。

### 📝 笔试题 19-2：以下代码输出什么？

```python
def add(x, lst=[]):
    lst.append(x); return lst

print(add(1))
print(add(2))
print(add(3, []))
print(add(4))
```

<details><summary>答案</summary>

```
[1]
[1, 2]
[3]
[1, 2, 4]
```

默认参数 `lst=[]` 在函数定义时只创建一次，被反复复用。
</details>

### 📝 笔试题 19-3：实现一个带过期时间的 LRU（思路）

- `OrderedDict` 维护访问顺序
- 每项存 `(value, expire_at)`
- `get`：命中时检查是否过期；若过期则删除并返回 miss；否则 `move_to_end` 并返回
- `put`：若超容，`popitem(last=False)` 淘汰最旧

### 📝 笔试题 19-4：统计数组中出现次数最多的 3 个元素

```python
from collections import Counter
Counter(nums).most_common(3)
```

内部用大小为 K 的堆，`O(n log k)`。

### 📝 笔试题 19-5：以下哪些是可哈希的？

```
[1, 2]              # ❌ list 可变
(1, 2)              # ✅
(1, [2, 3])         # ❌ 含可变元素
{1, 2}              # ❌ set 可变
frozenset({1, 2})   # ✅
{'a': 1}            # ❌ dict 可变
'abc'               # ✅
```

### 📝 笔试题 19-6：字符串拼接为什么要用 `join`？

`str` 不可变，`s += w` 每次都会**分配新内存并拷贝**原字符串，循环 `n` 次即 `O(n²)`。`''.join(list)` 先计算总长度，一次性分配并拷贝，`O(total_len)`。

### 📝 笔试题 19-7：`dict` 是否线程安全？

单个内建操作（如 `d[k] = v`）在 CPython 下因 **GIL** 是原子的，但**复合操作**（如"检查后写入"）不是原子的，需要 `threading.Lock` 或使用 `queue.Queue` / `concurrent.futures` 等工具。跨线程高频共享状态应优先考虑不可变数据 + 消息传递。

### 📝 笔试题 19-8：给定一个字符串列表，按长度分组

```python
from collections import defaultdict
groups = defaultdict(list)
for w in words:
    groups[len(w)].append(w)
```

### 📝 笔试题 19-9：合并 K 个有序链表（使用 `heapq`）

```python
import heapq
def merge_k_lists(lists):
    h = []
    for i, node in enumerate(lists):
        if node: heapq.heappush(h, (node.val, i, node))
    dummy = ListNode(); tail = dummy
    while h:
        val, i, node = heapq.heappop(h)
        tail.next = node; tail = node
        if node.next:
            heapq.heappush(h, (node.next.val, i, node.next))
    return dummy.next
```

**注意**：堆中加入下标 `i` 是为了防止 `node.val` 相同导致比较 `ListNode` 时报错。

---

## 🐍 Python 使用心得

1. **先用 dict/set 代替手动哈希**：哈希类题目 90% 情况 `dict`/`set` 足矣
2. **滑动窗口记得 `Counter` + `deque`**：代码量骤降
3. **BFS 永远用 `deque`**：禁止 `list.pop(0)`
4. **`heapq` 默认小根堆**：要大根堆就入负数，或用 `(-priority, item)` 元组
5. **字符串拼接用 `join`**：避免 `+=` 在循环中
6. **避免可变默认参数**：用 `None` 哨兵
7. **慎用 `is` 比较**：除 `None`/`True`/`False` 外一律 `==`
8. **理解 GIL**：CPU 密集用 `multiprocessing`，IO 密集用线程或 `asyncio`


---

## 20. Java 内置数据结构深入

Java 的数据结构主要集中在 `java.util` 包的 **Collections Framework**，另有 `java.util.concurrent` 提供并发容器。理解接口继承关系、底层实现、线程安全性，是 Java 笔试的高频考点。

### 20.1 集合框架总览

```
        Iterable
           │
       Collection                         Map
     ┌─────┼─────┐                  ┌─────┼─────┐
   List   Set   Queue          HashMap TreeMap LinkedHashMap
    │      │     │                 │
ArrayList HashSet Deque        ConcurrentHashMap
LinkedList TreeSet PriorityQueue
Vector   LinkedHashSet
Stack
```

- `Collection`：单列集合顶层接口
- `Map`：键值对接口，**不继承 Collection**
- `Iterator`：迭代器；`ListIterator` 支持双向
- 数组类型：`Arrays` 工具类；`Collections` 为集合工具类

### 20.2 数组（Array）

- Java 数组是**固定长度**的对象，有 `length` 字段（不是方法）
- 基本类型数组不装箱，效率高
- 多维数组本质是"数组的数组"，可不规则

```java
int[] a = new int[10];
int[][] g = new int[3][];
g[0] = new int[]{1, 2};   // 长度可不同

Arrays.sort(a);                    // 原地排序
Arrays.fill(a, 0);                 // 填充
int[] b = Arrays.copyOf(a, 20);    // 拷贝并扩容
List<Integer> list = Arrays.asList(1, 2, 3);  // 定长包装，不能 add/remove
```

> 注意：`Arrays.asList(int[])` 得到的是 `List<int[]>` 只含一个元素，要用装箱数组 `Integer[]`。

### 20.3 List 接口

| 实现 | 底层 | 随机访问 | 插入/删除 | 线程安全 |
|------|------|----------|-----------|----------|
| `ArrayList` | 动态数组 | `O(1)` | 尾部均摊 `O(1)`，中间 `O(n)` | ❌ |
| `LinkedList` | 双向链表 | `O(n)` | 已知节点 `O(1)` | ❌ |
| `Vector` | 动态数组 | `O(1)` | 同 ArrayList | ✅（全方法 synchronized，过时）|
| `Stack` | 继承 Vector | — | — | ✅（已不推荐，改用 Deque） |
| `CopyOnWriteArrayList` | 写时复制 | `O(1)` | `O(n)` | ✅（读多写少）|

#### ArrayList 扩容

- 默认初始容量 10
- 扩容策略：`newCapacity = oldCapacity + (oldCapacity >> 1)`（1.5 倍）
- 扩容需**整体拷贝**，预估容量时用 `new ArrayList<>(expectedSize)` 可避免多次扩容

```java
List<Integer> list = new ArrayList<>();
list.add(1); list.add(2);
list.get(0);              // O(1)
list.remove(0);           // O(n)，需整体前移
list.removeIf(x -> x > 0);// 删除满足条件
```

#### LinkedList 的特殊性

`LinkedList` 既实现 `List` 也实现 `Deque`，可当**双端队列**和**栈**用：

```java
Deque<Integer> stack = new LinkedList<>();
stack.push(1); stack.pop();       // 栈
Deque<Integer> queue = new LinkedList<>();
queue.offer(1); queue.poll();     // 队列
```

> 一般情况仍推荐 `ArrayDeque`，对 cache 更友好，没有节点指针开销。

### 20.4 Set 接口

| 实现 | 底层 | 有序 | 允许 null | 复杂度 |
|------|------|------|-----------|--------|
| `HashSet` | HashMap | ❌ | ✅（1 个） | `O(1)` 均摊 |
| `LinkedHashSet` | LinkedHashMap | ✅（插入序） | ✅ | `O(1)` |
| `TreeSet` | 红黑树（TreeMap） | ✅（自然/比较器序） | ❌（抛 NPE） | `O(log n)` |
| `EnumSet` | 位向量 | 声明序 | ❌ | `O(1)` 极快 |

```java
Set<Integer> s = new HashSet<>();
s.add(1); s.add(1); s.size();     // 1

NavigableSet<Integer> ts = new TreeSet<>();
ts.add(10); ts.add(5); ts.add(15);
ts.first(); ts.last();            // 5, 15
ts.floor(12);                     // ≤12 的最大，返回 10
ts.ceiling(12);                   // ≥12 的最小，返回 15
ts.headSet(10); ts.tailSet(10);
```

### 20.5 Map 接口

| 实现 | 底层 | 有序 | 线程安全 | 典型场景 |
|------|------|------|----------|----------|
| `HashMap` | 数组 + 链表 / 红黑树 | ❌ | ❌ | 通用映射 |
| `LinkedHashMap` | HashMap + 双向链表 | ✅（可配 LRU） | ❌ | 保持插入/访问序、LRU |
| `TreeMap` | 红黑树 | ✅ | ❌ | 需要排序或范围查询 |
| `Hashtable` | 数组 + 链表 | ❌ | ✅（全方法同步，过时） | — |
| `ConcurrentHashMap` | 分段/CAS + 红黑树 | ❌ | ✅ | 高并发 |
| `EnumMap` | 数组 | 声明序 | ❌ | 以枚举为键 |
| `IdentityHashMap` | 开放地址 | ❌ | ❌ | 按 `==` 比较键 |
| `WeakHashMap` | 弱引用键 | ❌ | ❌ | 缓存，自动回收 |

#### HashMap 核心机制（JDK 8+）

1. **结构**：`Node<K,V>[] table`，下标由 `(n - 1) & hash` 决定
2. **哈希扰动**：`hash = key.hashCode() ^ (key.hashCode() >>> 16)`，让高位参与低位
3. **冲突处理**：链地址法；**当链长 ≥ 8 且数组长度 ≥ 64** 时转**红黑树**；回落到链表阈值为 6
4. **扩容**：容量始终为 2 的幂，负载因子默认 0.75，超过即 `resize()` 为两倍；旧节点按 `(hash & oldCap)` 判断留在原位或移到 `i + oldCap`
5. **非线程安全**：JDK 7 并发扩容可能形成链表环；JDK 8 改为尾插避免环，但仍有数据丢失风险

```java
Map<String, Integer> m = new HashMap<>();
m.put("a", 1);
m.getOrDefault("b", 0);                       // 不存在返回默认
m.putIfAbsent("a", 99);                       // 已存在则不改
m.computeIfAbsent("k", k -> new ArrayList<>());
m.merge("a", 1, Integer::sum);                // 计数或合并
m.forEach((k, v) -> System.out.println(k + v));
```

#### LinkedHashMap 实现 LRU

构造时设置 `accessOrder = true`，重写 `removeEldestEntry`：

```java
class LRU<K, V> extends LinkedHashMap<K, V> {
    private final int cap;
    public LRU(int cap) {
        super(cap, 0.75f, true);  // accessOrder = true
        this.cap = cap;
    }
    @Override
    protected boolean removeEldestEntry(Map.Entry<K, V> e) {
        return size() > cap;
    }
}
```

#### TreeMap

基于**红黑树**，所有操作 `O(log n)`，支持**范围查询**与**有序遍历**：

```java
NavigableMap<Integer, String> tm = new TreeMap<>();
tm.put(3, "c"); tm.put(1, "a"); tm.put(5, "e");
tm.firstKey(); tm.lastKey();
tm.floorKey(4);    // 3
tm.ceilingKey(4);  // 5
tm.subMap(1, 5);   // [1, 5)
```

#### 键的 `equals` 与 `hashCode`

- **`equals` 相等 ⇒ `hashCode` 必须相等**
- 重写 `equals` **必须同时重写 `hashCode`**
- 作为 `TreeMap` 键需实现 `Comparable` 或提供 `Comparator`
- 作为 `HashMap` 键**不可变**更安全（否则 hash 改变后永远查不到）

### 20.6 Queue / Deque

| 实现 | 底层 | 线程安全 | 特性 |
|------|------|----------|------|
| `ArrayDeque` | 循环数组 | ❌ | 推荐的栈/队列 |
| `LinkedList` | 双向链表 | ❌ | 同时是 List |
| `PriorityQueue` | 二叉小根堆 | ❌ | 非 FIFO，按优先级出队 |
| `ArrayBlockingQueue` | 有界数组 | ✅ | 阻塞队列 |
| `LinkedBlockingQueue` | 链表 | ✅ | 可选有界 |
| `PriorityBlockingQueue` | 无界堆 | ✅ | 阻塞优先队列 |
| `ConcurrentLinkedQueue` | 链表 CAS | ✅ | 非阻塞 |
| `DelayQueue` | 堆 | ✅ | 延迟元素 |

#### ArrayDeque 常用 API

```java
Deque<Integer> dq = new ArrayDeque<>();
dq.offerFirst(1); dq.offerLast(2);
dq.pollFirst();  dq.pollLast();
dq.peekFirst(); dq.peekLast();
// 作为栈
dq.push(1);  // 等同 addFirst
dq.pop();    // 等同 removeFirst
```

> `Queue` 接口中 `add/remove/element` 失败抛异常，`offer/poll/peek` 返回特殊值（null/false）。

#### PriorityQueue

```java
PriorityQueue<Integer> min = new PriorityQueue<>();          // 小根堆
PriorityQueue<Integer> max = new PriorityQueue<>((a, b) -> b - a); // 大根堆
max.offer(3); max.offer(1); max.offer(5);
max.peek();    // 5
max.poll();    // 5
```

**注意**：`PriorityQueue` 的迭代器**不保证**按优先级顺序遍历，只能靠不断 `poll` 得到有序结果。

### 20.7 并发容器（java.util.concurrent）

#### ConcurrentHashMap（JDK 8+）

- 取消了 JDK 7 的分段锁（Segment），改为**数组 + CAS + synchronized**（锁链表/树的头节点）
- 结构与 `HashMap` 一致（数组 + 链表 / 红黑树）
- **读**无锁；**写**仅锁单个桶，并发度高
- 不允许 `null` 键/值（便于区分"不存在"和"值为 null"）

```java
ConcurrentHashMap<String, Integer> cm = new ConcurrentHashMap<>();
cm.compute("k", (k, v) -> v == null ? 1 : v + 1);  // 原子更新
cm.merge("k", 1, Integer::sum);
```

#### CopyOnWriteArrayList / CopyOnWriteArraySet

- 写操作**复制整个底层数组**，开销大，只适合**读远多于写**的场景
- 读完全无锁，弱一致迭代器

#### BlockingQueue 家族

生产者-消费者模型的核心工具：

```java
BlockingQueue<Task> q = new LinkedBlockingQueue<>(1000);
// 生产
q.put(task);          // 满则阻塞
q.offer(task, 1, TimeUnit.SECONDS);  // 超时
// 消费
Task t = q.take();    // 空则阻塞
```

### 20.8 String 相关

- `String` **不可变**，底层 JDK 9+ 是 `byte[] + coder`（LATIN1 或 UTF-16）
- **字符串常量池**：字面量在方法区常量池；`new String("x")` 生成堆上新对象
- `String.intern()` 可手动放入常量池
- 拼接：
  - 编译期常量折叠：`"a" + "b"` → `"ab"`
  - 变量拼接被 JDK 编译为 `StringBuilder`，但**循环内**的多次 `+=` 仍会每次 `new StringBuilder`，**应显式使用 `StringBuilder`**

```java
StringBuilder sb = new StringBuilder();
for (String w : words) sb.append(w);
String s = sb.toString();
```

- 线程安全：`StringBuffer` 方法同步（过时），`StringBuilder` 非线程安全（首选）

### 20.9 常用工具类

#### Arrays

```java
Arrays.sort(a);                              // 原地排序
Arrays.sort(a, 0, 5);                        // 区间排序
Arrays.sort(arr, Comparator.reverseOrder()); // 仅对象数组
Arrays.binarySearch(a, target);              // 数组需已排序
Arrays.stream(a).sum();
```

#### Collections

```java
Collections.sort(list);
Collections.sort(list, (a, b) -> b - a);
Collections.reverse(list);
Collections.shuffle(list);
Collections.unmodifiableList(list);         // 只读视图
Collections.synchronizedMap(new HashMap<>());// 同步包装，性能一般
int i = Collections.binarySearch(sortedList, key);
```

#### Stream（JDK 8+）

```java
List<Integer> squares = nums.stream()
    .filter(x -> x > 0)
    .map(x -> x * x)
    .sorted()
    .collect(Collectors.toList());  // JDK 16+ 可用 .toList()

Map<Integer, Long> freq = nums.stream()
    .collect(Collectors.groupingBy(x -> x, Collectors.counting()));
```

### 20.10 泛型与自动装箱

- 泛型在运行时**类型擦除**，容器内部实际存 `Object`
- 基本类型不能作为类型参数，只能用包装类（`List<Integer>`）
- 装箱/拆箱有开销；**数值密集场景**应直接用原生数组或 `IntStream`
- `Integer` 缓存 `-128..127`：

```java
Integer a = 127, b = 127;  a == b;  // true（缓存）
Integer c = 128, d = 128;  c == d;  // false（新对象）
// 比较值一律 .equals() 或 .intValue()
```

### 20.11 `equals` / `hashCode` / `Comparable`

- **`equals` 契约**：自反、对称、传递、一致、与 null 比较返回 false
- **`hashCode` 契约**：`equals` 相等则 `hashCode` 相等
- `Comparable<T>`：实现 `compareTo`，类"自然顺序"
- `Comparator<T>`：外部比较器，更灵活，支持链式：

```java
list.sort(Comparator.comparingInt(Person::getAge)
         .thenComparing(Person::getName)
         .reversed());
```

> ⚠️ **不要**在 `compareTo` / `Comparator` 中写 `a.val - b.val`：可能整型溢出。使用 `Integer.compare(a, b)`。

### 20.12 性能速查与选型

| 需求 | 首选 |
|------|------|
| 随机访问序列 | `ArrayList` |
| 频繁首尾增删 / 栈 / 队列 | `ArrayDeque` |
| 键值映射 | `HashMap` |
| 保持插入或访问顺序 | `LinkedHashMap` |
| 有序键 / 范围查询 | `TreeMap` |
| 去重 | `HashSet` |
| 有序去重 | `TreeSet` |
| 优先级调度 | `PriorityQueue` |
| 高并发映射 | `ConcurrentHashMap` |
| 生产者-消费者 | `LinkedBlockingQueue` |
| 读多写极少 | `CopyOnWriteArrayList` |

### 📝 笔试题 20-1：`HashMap` 为什么容量是 2 的幂？

确定桶位用 `(n - 1) & hash` 取模等价于 `hash % n`，**前提是 n 为 2 的幂**，且位运算比取模快。另外扩容时只需看某一位就能判断留在原位还是移到 `i + oldCap`。

### 📝 笔试题 20-2：`HashMap` 链表转红黑树的阈值？

- 链表长度 ≥ **8** 且数组长度 ≥ **64** 才树化；否则优先扩容
- 树化后节点数 ≤ **6** 退回链表
- 泊松分布下正常 hash 下链长 ≥ 8 的概率极小（约 `10⁻⁸`），转树主要是防止**恶意 hash 攻击**

### 📝 笔试题 20-3：`HashMap` 与 `ConcurrentHashMap` 区别？

| 维度 | HashMap | ConcurrentHashMap |
|------|---------|-------------------|
| 线程安全 | ❌ | ✅ |
| null 键值 | 允许 | 不允许 |
| 锁粒度 | — | 桶级（头节点 synchronized + CAS） |
| 迭代一致性 | fail-fast | 弱一致 |
| 典型用途 | 单线程 | 多线程 |

### 📝 笔试题 20-4：以下代码输出？

```java
List<String> list = new ArrayList<>(Arrays.asList("a", "b", "c"));
for (String s : list) {
    if (s.equals("b")) list.remove(s);
}
```

<details><summary>答案</summary>

抛 `ConcurrentModificationException`。增强 for 循环使用 `Iterator`，直接在 `list.remove` 修改结构触发 fail-fast。

正确写法：

```java
Iterator<String> it = list.iterator();
while (it.hasNext()) {
    if ("b".equals(it.next())) it.remove();
}
// 或 JDK 8+
list.removeIf("b"::equals);
```
</details>

### 📝 笔试题 20-5：`Arrays.asList(int[])` 的坑

```java
int[] arr = {1, 2, 3};
List list = Arrays.asList(arr);
System.out.println(list.size());    // 1
```

`int[]` 不是对象数组，被当作**单个对象**装入。正确写法：`Integer[] arr = {1, 2, 3};` 或 `IntStream.of(arr).boxed().collect(toList())`。

另外，`Arrays.asList` 返回的是**定长视图**（`Arrays$ArrayList`），调用 `add/remove` 会抛 `UnsupportedOperationException`。

### 📝 笔试题 20-6：`TreeMap` 实现滑动窗口最大值

利用 `TreeMap` 的 `lastKey` / 递减频次：

```java
public int[] maxSlidingWindow(int[] nums, int k) {
    TreeMap<Integer, Integer> map = new TreeMap<>();
    int[] ans = new int[nums.length - k + 1];
    for (int i = 0; i < nums.length; i++) {
        map.merge(nums[i], 1, Integer::sum);
        if (i >= k) {
            int out = nums[i - k];
            if (map.get(out) == 1) map.remove(out);
            else map.merge(out, -1, Integer::sum);
        }
        if (i >= k - 1) ans[i - k + 1] = map.lastKey();
    }
    return ans;
}
```

> 复杂度 `O(n log k)`，优于朴素 `O(nk)`，但不如单调队列的 `O(n)`。

### 📝 笔试题 20-7：实现大顶堆与 Top-K

```java
// Top-K 大元素：维护大小为 k 的小根堆
PriorityQueue<Integer> pq = new PriorityQueue<>();
for (int x : nums) {
    pq.offer(x);
    if (pq.size() > k) pq.poll();
}
return pq.peek();  // 第 K 大
```

### 📝 笔试题 20-8：`Comparable` vs `Comparator`

- `Comparable` 定义**自然顺序**，类自身实现，`compareTo(T other)`
- `Comparator` 外部策略，通常以 lambda/静态方法引用传入

```java
// Comparable
class Task implements Comparable<Task> {
    int priority;
    public int compareTo(Task o) { return Integer.compare(priority, o.priority); }
}

// Comparator
tasks.sort(Comparator.comparingInt(t -> t.priority));
```

### 📝 笔试题 20-9：`Integer` 比较的陷阱

```java
Integer a = 127, b = 127;
Integer c = 128, d = 128;
System.out.println(a == b);  // true
System.out.println(c == d);  // false
System.out.println(c.equals(d));  // true
```

`Integer.valueOf` 缓存 `-128..127`，超出范围每次 `new`，`==` 比较引用；**包装类型一律用 `.equals`**。

### 📝 笔试题 20-10：`HashMap` 遍历方式

```java
for (Map.Entry<K, V> e : map.entrySet()) { ... }   // 推荐，一次取 kv
for (K k : map.keySet()) { V v = map.get(k); }    // 多一次 get
map.forEach((k, v) -> { ... });                    // JDK 8 lambda
Iterator<Map.Entry<K, V>> it = map.entrySet().iterator();
while (it.hasNext()) {
    Map.Entry<K, V> e = it.next();
    if (...) it.remove();  // 安全删除
}
```

### 📝 笔试题 20-11：以下 `ConcurrentHashMap` 使用是否线程安全？

```java
ConcurrentHashMap<String, Integer> m = new ConcurrentHashMap<>();
// 方式一
if (!m.containsKey("k")) m.put("k", 1);
// 方式二
m.putIfAbsent("k", 1);
// 方式三
m.compute("k", (k, v) -> v == null ? 1 : v + 1);
```

<details><summary>答案</summary>

- 方式一**非原子**，check-then-act 可能竞争
- 方式二、三为**原子**操作，是正确写法
</details>

---

## ☕ Java 使用心得

1. **接口优先**：声明类型用接口（`List`, `Map`），实现可替换
2. **优先 `ArrayDeque`**：替代 `Stack` 和 `LinkedList` 作栈/队列
3. **`HashMap` 预估容量**：`new HashMap<>(expectedSize / 0.75f + 1)` 避免多次扩容
4. **重写 `equals` 同时重写 `hashCode`**：IDE 可一键生成，或用 `Objects.hash(...)`
5. **禁用 `a - b` 排序**：改用 `Integer.compare`，避免溢出
6. **并发场景别用 `Hashtable`/`synchronizedMap`**：首选 `ConcurrentHashMap`
7. **迭代中修改用迭代器**或 `removeIf`：否则 `ConcurrentModificationException`
8. **包装类比较用 `.equals`**：`==` 只在 `-128..127` 里恰好成立
9. **循环拼接用 `StringBuilder`**：且预估容量
10. **流 `Stream` 只用一次**：终结操作后不能重用；大数据才考虑 `parallelStream`

##  课后作业
1. 使用Python编写程序实现本讲义中的各个编程练习题：例如：对每个排序算法，各自写一个.py文件，给定无序数组，调用排序算法，输出排序的结果。
2. 针对LeetCode进行编程练习。
3. https://www.geeksforgeeks.org/
