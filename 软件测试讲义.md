
# 软件测试讲义

> 本讲义系统介绍软件测试的分类、方法、工具、组织实践与工程集成。覆盖单元测试、集成测试、端到端测试、契约测试、冒烟/回归、性能/负载/压力、混沌、A/B、安全、可访问性等测试类型。每章配"知识点 + 笔试题"。
>
> 约定：示例以 Java (JUnit 5)、Python (pytest)、JavaScript (Jest) 为主；CI 示例使用 GitHub Actions 语法。

## 目录

1. [为什么要测试](#1-为什么要测试)
2. [测试分层：金字塔与奖杯模型](#2-测试分层金字塔与奖杯模型)
3. [测试设计方法](#3-测试设计方法)
4. [单元测试](#4-单元测试)
5. [集成测试](#4-5-集成测试)
6. [契约测试](#6-契约测试)
7. [端到端（E2E）测试](#7-端到端e2e测试)
8. [冒烟与回归测试](#8-冒烟与回归测试)
9. [性能、负载、压力与容量测试](#9-性能负载压力与容量测试)
10. [混沌工程与韧性测试](#10-混沌工程与韧性测试)
11. [安全测试](#11-安全测试)
12. [可访问性与兼容性测试](#12-可访问性与兼容性测试)
13. [A/B 测试与灰度](#13-ab-测试与灰度)
14. [自动化测试与 CI/CD 集成](#14-自动化测试与-cicd-集成)
15. [测试数据、Mock 与夹具](#15-测试数据mock-与夹具)
16. [测试覆盖率与质量度量](#16-测试覆盖率与质量度量)
17. [流程、缺陷管理与组织](#17-流程缺陷管理与组织)
18. [反模式与最佳实践](#18-反模式与最佳实践)
19. [综合笔试练习](#19-综合笔试练习)

---

## 1. 为什么要测试

### 1.1 测试的目的

- **发现缺陷**：找到代码不符合预期的行为
- **建立信心**：让团队敢于重构、发布
- **活文档**：测试用例表达"系统应该怎么用"
- **回归保障**：防止老功能被新改动破坏
- **设计反馈**：难写的测试往往暗示设计问题

### 1.2 测试的代价

- 编写和维护测试耗时
- 脆弱/缓慢的测试拖慢迭代
- 覆盖率高 ≠ 质量高（可能是"假测试"）
- 测试环境/数据/并发带来复杂度

### 1.3 失败成本曲线

```
缺陷越晚发现，修复成本越高：
需求/设计阶段 → 开发阶段 → 测试阶段 → 预发布 → 生产事故
     1x            10x        100x        1000x       10000x+
```

**左移（Shift Left）**：尽早发现缺陷；单元测试、Code Review、静态分析是成本最低的发现缺陷方式。

### 📝 笔试题 1-1：为什么有了人工测试还要自动化测试？

自动化测试的核心价值在**频繁执行**：
- 每次提交/每次发布都能跑完，给出一致可重复的信号
- 人工测试无法覆盖每次改动的全回归
- 自动化测试作为"安全网"让团队敢重构、敢重写
- 人工测试更适合**探索性测试**和难以自动化的体验类验证

两者互补而非替代。

---

## 2. 测试分层：金字塔与奖杯模型

### 2.1 测试金字塔（Mike Cohn）

```
         /\
        /E2E\            少量，慢，覆盖关键用户旅程
       /----\
      /集成 \           中量，验证组件协作
     /------\
    / 单元   \          大量，快速，基础
   /---------\
```

**核心原则**：

- **越上层越贵**：环境复杂、运行慢、脆弱
- **越下层越快越稳**：应成为测试的主体
- **反金字塔**是反模式：大量 E2E、少单元 → 迭代慢、发现问题晚

### 2.2 测试奖杯模型（Kent C. Dodds）

```
       ┌──────┐
       │ E2E  │         少量
       ├──────┤
       │ 集成  │         占大头（奖杯身）
       ├──────┤
       │ 单元  │         关键逻辑/算法
       ├──────┤
       │ 静态  │         类型、lint、扫描
       └──────┘
```

更适合前端与动态语言：集成测试性价比高（一个测试能覆盖更多真实路径）。

### 2.3 按"执行时间"分级

- **秒级**：单元 / 契约（本地开发循环）
- **分钟级**：集成 / 组件 / 部分 E2E（CI 每次 PR）
- **小时级**：全量 E2E / 性能（夜间 / 发布前）
- **数小时+**：容量 / 耐久 / 混沌（计划执行）

### 2.4 测试类型矩阵

| 维度 | 可选值 |
|------|--------|
| 层次 | 单元 / 集成 / E2E |
| 可见性 | 白盒 / 灰盒 / 黑盒 |
| 目的 | 功能 / 性能 / 安全 / 可用性 |
| 时机 | 冒烟 / 回归 / 验收 / 上线前 |
| 手段 | 手工 / 自动 |

### 📝 笔试题 2-1：什么是反金字塔？有什么问题？

**反金字塔**：大量 E2E，少量集成，极少单元。问题：

- **慢**：E2E 跑完几十分钟甚至几小时
- **脆弱**：环境、时序、外部依赖一点抖动就挂
- **定位难**：失败时往往看不出根因
- **维护成本高**：UI 一改一堆测试要跟着改
- **反馈慢**：PR 合不进，迭代受阻

修复思路：抽关键逻辑做单元测试；把验证"组件协作"的放到集成层；E2E 只留最关键的用户旅程（smoke 级）。

---

## 3. 测试设计方法

### 3.1 黑盒方法

- **等价类划分**：把输入域划成若干组，每组选代表
- **边界值分析**：边界最易出 bug（`max`、`max-1`、`max+1`、`min-1`、`min`、`min+1`、`0`、空、null）
- **决策表**：条件组合 → 期望动作
- **因果图 / 状态迁移图**：状态机类功能
- **场景/用例法**：按业务流程组织

### 3.2 白盒方法（基于代码结构）

- **语句覆盖**：每行至少执行一次
- **分支覆盖**：每个 if 的 true/false 都跑到
- **条件覆盖**：每个布尔子表达式取各值
- **MC/DC**：Modified Condition/Decision Coverage，航空/医疗强制
- **路径覆盖**：组合爆炸，一般只对关键函数

### 3.3 基于风险的测试

不是所有代码都一样重要。按**影响面 × 失败概率**排序，把测试资源投向：

- 支付、登录、订单等核心链路
- 改动频繁的模块
- 历史 bug 高发区
- 外部集成点

### 3.4 探索性测试

没有预先脚本，测试人员基于经验在产品里"玩"，边玩边记录异常。适合：

- 新功能上线前
- 难以自动化的体验类问题
- 弱项/未知区的漏网之鱼

**Session-Based Testing**：限时 60-90 分钟聚焦某区域，事后写笔记。

### 3.5 基于属性的测试（Property-Based Testing）

不是检查具体输入-输出，而是检查**性质**（例如"反转两次等于自己"）：

```python
# hypothesis
from hypothesis import given
from hypothesis.strategies import lists, integers

@given(lists(integers()))
def test_reverse_twice(xs):
    assert list(reversed(list(reversed(xs)))) == xs
```

框架会自动生成边缘用例（空、极值、重复）并做**shrinking**（最小化失败样例）。

### 📝 笔试题 3-1：测试一个年龄输入（1-120）用什么方法？

**等价类 + 边界值**：

- 有效等价类：典型值 30
- 无效等价类：0、121、负数、非数字、null、"abc"
- 边界：0、1、2、119、120、121
- 非法类型：字符串、特殊字符、超长

加上决策表组合校验错误提示是否正确。

---

## 4. 单元测试

### 4.1 原则：FIRST

- **F**ast：毫秒级
- **I**ndependent：互不依赖
- **R**epeatable：任何时间、任何环境结果一致
- **S**elf-validating：明确 pass/fail
- **T**imely：与代码同时或先于代码写

### 4.2 结构：AAA / GWT

```
Arrange  (Given) → 准备输入和依赖
Act      (When)  → 触发被测行为
Assert   (Then)  → 验证输出
```

### 4.3 示例

**Java / JUnit 5**：

```java
class PriceCalculatorTest {
    PriceCalculator calc = new PriceCalculator();

    @Test
    void shouldApplyDiscountWhenAmountOverThreshold() {
        // Given
        var order = new Order(List.of(item(100), item(50)));
        // When
        var price = calc.totalWithDiscount(order);
        // Then
        assertThat(price).isEqualTo(Money.of(135));
    }

    @ParameterizedTest
    @CsvSource({ "0, 0", "100, 100", "200, 180", "500, 400" })
    void shouldApplyTieredDiscount(int input, int expected) {
        assertThat(calc.apply(input)).isEqualTo(expected);
    }
}
```

**Python / pytest**：

```python
import pytest
from app.pricing import apply_discount

@pytest.mark.parametrize("amount,expected", [
    (0, 0), (100, 100), (200, 180), (500, 400),
])
def test_apply_tiered_discount(amount, expected):
    assert apply_discount(amount) == expected

def test_raises_on_negative():
    with pytest.raises(ValueError):
        apply_discount(-1)
```

**JavaScript / Jest**：

```javascript
import { applyDiscount } from './pricing';

describe('applyDiscount', () => {
  test.each([
    [0, 0], [100, 100], [200, 180], [500, 400],
  ])('amount %i → %i', (a, e) => {
    expect(applyDiscount(a)).toBe(e);
  });

  test('throws on negative', () => {
    expect(() => applyDiscount(-1)).toThrow(RangeError);
  });
});
```

### 4.4 Mock / Stub / Spy / Fake

| 种类 | 说明 |
|------|------|
| **Dummy** | 占位对象，不会被真正调用 |
| **Stub** | 预设返回值的假对象 |
| **Spy** | 记录被调情况的对象 |
| **Mock** | 带行为期望的替身，验证交互 |
| **Fake** | 有简化但可运行实现（内存版 DB） |

**优先真实协作对象 + Fake**，`mock` 一切会让测试脆弱。

### 4.5 常见反模式

- **测了实现细节**（私有方法、内部状态）→ 重构就挂
- **一个测试多个断言**不同关注点
- **睡 sleep 等异步**→ 用 awaitility / 虚拟时钟
- **共享可变状态**导致顺序依赖
- **断言是 `assertTrue(true)`**—没在断言任何事

### 📝 笔试题 4-1：单元测试应该 mock 数据库吗？

**纯业务逻辑**单元测试应该**不依赖数据库**——用接口 + fake/内存实现。
**Repository/Mapper 层**的测试是**集成测试**范畴（用 Testcontainers 起真 DB），而不是单元测试。
把数据库访问混进大量 "单元" 测试里是反模式：慢、脆、又没真正覆盖 SQL 正确性。

---

## 5. 集成测试

### 5.1 目标

验证**多个组件协作**是否正确，例如：

- 应用 + 数据库
- 应用 + 缓存 / MQ
- 内部模块间的 API
- 外部服务的适配层

### 5.2 Testcontainers（推荐）

用 Docker 在测试里**真实**启动依赖：

```java
@Testcontainers
class OrderRepositoryIT {
    @Container
    static PostgreSQLContainer<?> pg = new PostgreSQLContainer<>("postgres:16")
        .withDatabaseName("app").withUsername("app").withPassword("pw");

    @DynamicPropertySource
    static void props(DynamicPropertyRegistry r) {
        r.add("spring.datasource.url", pg::getJdbcUrl);
        r.add("spring.datasource.username", pg::getUsername);
        r.add("spring.datasource.password", pg::getPassword);
    }

    @Autowired OrderRepository repo;

    @Test
    void saveAndFind() {
        var id = repo.save(new Order(...));
        assertThat(repo.findById(id)).isPresent();
    }
}
```

其他语言同理（`pytest-testcontainers`、`testcontainers-node`）。

### 5.3 Spring Boot 的切片测试

- `@DataJpaTest`：只装 JPA + 嵌入式 DB
- `@WebMvcTest`：只装 Web 层 + MockMvc
- `@SpringBootTest`：完整上下文（最重）

根据需要选最小切片，加速测试。

### 5.4 外部服务集成

- **Mock Server**：WireMock、MockServer、Mountebank
- **Stub 契约**：按契约文件回放（见"契约测试"）
- **沙箱**：支付、短信等用厂商提供的 sandbox

### 5.5 异步与消息驱动

- 用**真实 Kafka/Redis** 容器
- 断言异步结果：`awaitility` 轮询到期望或超时失败
- 避免 `Thread.sleep`

```java
await().atMost(5, SECONDS).untilAsserted(() ->
    assertThat(repo.findById(id)).isPresent());
```

### 📝 笔试题 5-1：集成测试怎么避免慢和不稳定？

- 用 Testcontainers 而不是共享测试环境（避免互相踩）
- 测试**自己管理**数据：建→测→清；或用事务回滚
- 拆分：高频跑 "关键路径集成"；全量放夜间
- 并行运行，CI 分片
- 不 sleep，用 awaitility
- 内部 mock 第三方，契约测试保证真实对接

---

## 6. 契约测试

### 6.1 为什么需要

微服务下，消费者与生产者分属不同团队/仓库。传统方式：

- **集成环境**联调 → 不稳定、慢、故障定位难
- **共享 stub** → 过时不易察觉

**契约测试**：用一份共享的"契约文件"描述 API，双方各自验证。

### 6.2 消费者驱动契约（Pact 流程）

```
Consumer 编写期望 → 生成 pact.json
       │
       ▼
   Pact Broker (中央仓库)
       │
       ▼
Provider 从 Broker 拉契约 → 按契约验证自己的真实实现
       │
       ▼
双方 CI 均需通过才能合入 / 发布
```

### 6.3 Pact 示例（JS 消费者）

```javascript
const { PactV3, MatchersV3 } = require('@pact-foundation/pact');

const provider = new PactV3({ consumer: 'web', provider: 'order-svc' });

provider
  .uponReceiving('get order by id')
  .withRequest({ method: 'GET', path: '/orders/1' })
  .willRespondWith({
    status: 200,
    body: { id: 1, status: MatchersV3.like('paid') }
  });

await provider.executeTest(async (mock) => {
  const o = await getOrder(mock.url, 1);
  expect(o.id).toBe(1);
});
```

生成 `web-order-svc.json` 上传 broker；Provider 侧拉回重放验证。

### 6.4 Schema/OpenAPI 驱动

- **Spectral**：lint OpenAPI
- **Schemathesis / Dredd**：按 OpenAPI 自动生成测试
- CI 中对比新旧 spec（`oasdiff`），防破坏性变更

### 6.5 契约测试 vs 集成测试

- **契约测试**：验证"契约"是否被遵守（不真正跨服务跑）
- **集成测试**：两个真实服务对接
- 前者更快更稳，适合微服务团队协作；后者偶尔验证真实集成

### 📝 笔试题 6-1：为什么契约测试推荐"消费者驱动"而非"生产者驱动"？

消费者才真正知道自己需要哪些字段/行为。由消费者编写期望，确保 Provider 不会悄悄破坏消费者使用模式；Provider 只需保证实现能满足 Consumer 们的并集即可。生产者驱动容易出现"我不知道谁在用"的遗留接口变更事故。

---

## 7. 端到端（E2E）测试

### 7.1 E2E 的定位

- 验证**真实用户旅程**（登录 → 下单 → 支付 → 发货）
- 跨越多个服务、UI、DB
- 一般部署在类生产环境

**取舍**：慢、脆，所以要**少而精**。挑最关键的几条"happy path + key unhappy path"。

### 7.2 Web E2E 工具

- **Playwright**（推荐）：多浏览器、自动等待、强并发
- **Cypress**：DX 极佳，内置录屏
- **Selenium / WebDriver**：成熟传统，生态最大
- **Puppeteer**：Chrome 定向

### 7.3 Playwright 示例

```javascript
import { test, expect } from '@playwright/test';

test('checkout happy path', async ({ page }) => {
  await page.goto('/');
  await page.getByRole('link', { name: 'Login' }).click();
  await page.getByLabel('Email').fill('alice@example.com');
  await page.getByLabel('Password').fill('pass1234');
  await page.getByRole('button', { name: 'Sign in' }).click();

  await page.getByRole('link', { name: 'Shop' }).click();
  await page.getByText('iPhone 15').click();
  await page.getByRole('button', { name: 'Add to cart' }).click();
  await page.getByRole('link', { name: 'Cart' }).click();
  await page.getByRole('button', { name: 'Checkout' }).click();

  await expect(page.getByText('Order placed')).toBeVisible();
});
```

Playwright 自带：

- 自动等待可见/可交互
- 多浏览器并行
- 失败时留 trace / 视频 / 截图

### 7.4 API E2E

不经过 UI，而是按真实生产路径打 API：

- **Postman/Newman**：脚本化接口测试
- **REST Assured**（Java）
- **Pytest + requests**（Python）
- **Karate**：BDD 风格 DSL

### 7.5 E2E 的稳定性技巧

- **数据隔离**：每个 case 用独立账号 / 租户
- **幂等清理**：teardown 删除自己产生的数据
- **重试但透明**：失败重试 1-2 次，记录不掩盖
- **唯一定位器**：`data-testid` 优先，不用脆弱的 XPath
- **不 sleep**：显式等待条件
- **跑在快速环境**：足够资源，避免 CI 抖动

### 📝 笔试题 7-1：E2E 测试一定要跑在"类生产环境"吗？

尽量。E2E 的价值是发现**集成层面**的问题，若环境与生产差异大就失去意义。实用妥协：

- 预发（staging）与生产**配置一致、数据脱敏**
- 开发环境跑 smoke 子集
- 真生产用只读 / 合成流量做拨测（synthetic）

---

## 8. 冒烟与回归测试

### 8.1 冒烟测试（Smoke Test）

- 目的：快速判断版本**基本可用**，否则不值得后续深度测试
- 范围：最关键的几条主路径（能登录、能下单、首页能打开）
- 速度：分钟级
- 典型运行时机：**每次构建后 / 部署后**

术语来源：电器通电看"冒不冒烟"的比喻。

### 8.2 Sanity Test（理智测试 / 健全测试）

- 针对**某个修复或小变更**做的窄范围验证
- 不如冒烟全面，但更聚焦
- 用法上有时与冒烟混用

### 8.3 回归测试（Regression Test）

- 目的：保证**老功能**在新代码下仍然正确
- 手段：**自动化测试套件**全量或关键子集重跑
- 触发：每次 PR / 每次合入 / 每次发布
- **回归套件**是团队的核心资产，需要长期维护

### 8.4 生产冒烟（Synthetic Monitoring）

- 在生产定时跑极小"冒烟脚本"：登录、关键 API、首页
- 工具：CloudWatch Synthetics、Datadog Synthetic、Grafana k6 Cloud
- 第一时间发现线上问题，早于用户报告

### 8.5 构建"冒烟集"的标准

- 运行 < 5 分钟（慢了就不会跑）
- 失败即阻断（明确语义）
- 不依赖复杂数据造型
- 覆盖每条关键链路的**一个**代表性用例

### 📝 笔试题 8-1：回归测试一定要每次都"全量"吗？

不一定。常见策略：

- **PR**：只跑改动影响范围 + 核心回归子集
- **主干合入**：更大范围的回归
- **夜间**：全量回归
- **发布前**：完整 E2E + 性能基线

根据**变更风险**与**反馈速度**权衡，"智能选择"依赖代码 - 用例映射工具（Test Impact Analysis）。

---

## 9. 性能、负载、压力与容量测试

### 9.1 术语澄清（常被混用）

| 名称 | 目的 | 负载特征 |
|------|------|----------|
| **Performance Test（性能）** | 广义伞术语 | 任意 |
| **Load Test（负载）** | 在**预期负载**下验证 SLO | 平稳、目标负载 |
| **Stress Test（压力）** | 找到**系统极限**与失败模式 | 逐步加压到过载 |
| **Spike Test（尖峰）** | 突发大流量的应对 | 瞬时陡增 |
| **Soak / Endurance（耐久）** | 长时间运行是否泄漏/退化 | 中负载持续数小时/天 |
| **Capacity（容量）** | 系统能承担多大规模 | 找到单位成本的上限 |
| **Scalability（伸缩）** | 扩容时线性度 | 逐步加资源 |

**关键区别**：

- 负载测试：**预期量级**下能否达标
- 压力测试：**超出预期**时如何失败
- 容量测试：为未来**多少规模**准备

### 9.2 性能指标

- **吞吐量 (Throughput)**：RPS / TPS
- **延迟 (Latency)**：P50 / P95 / P99 / P999
- **错误率**
- **资源使用**：CPU / 内存 / 网络 / IO / GC
- **并发数 (Concurrency)**

### 9.3 工具

| 工具 | 特点 |
|------|------|
| **k6**（推荐）| JS 脚本、云原生、指标友好 |
| **JMeter** | 老牌、GUI、协议全 |
| **Gatling** | Scala DSL，性能好 |
| **Locust** | Python 脚本，分布式友好 |
| **wrk / wrk2** | 极致 C 性能，简单协议 |
| **ab (Apache Bench)** | 最简单 |
| **Vegeta** | Go，声明式 |
| **Artillery** | Node，YAML |
| **Taurus** | 多工具统一封装 |

### 9.4 k6 示例

```javascript
// script.js
import http from 'k6/http';
import { check, sleep } from 'k6';
import { Trend, Rate } from 'k6/metrics';

const latency = new Trend('api_latency');
const errors  = new Rate('errors');

export const options = {
  stages: [
    { duration: '2m', target: 100 },   // ramp up
    { duration: '10m', target: 100 },  // steady
    { duration: '2m',  target: 500 },  // spike
    { duration: '5m',  target: 500 },
    { duration: '2m',  target: 0 },    // ramp down
  ],
  thresholds: {
    http_req_duration: ['p(95)<300', 'p(99)<800'],
    errors: ['rate<0.01'],
  },
};

export default function () {
  const res = http.get('https://api.example.com/products');
  latency.add(res.timings.duration);
  errors.add(res.status !== 200);
  check(res, { 'status is 200': (r) => r.status === 200 });
  sleep(1);
}
```

运行：`k6 run script.js`，内置 Prometheus/InfluxDB/DataDog 输出。

### 9.5 测试策略

- **基线测试**：主要路径在固定负载下拿基线数字；每个版本对比
- **容量规划**：逐步加压到 SLO 打破 → 得到每实例承载能力 → 推算总容量
- **混合场景**：组合真实业务比例（70% 读 / 20% 搜 / 10% 写）
- **数据真实**：数据量级、分布都要接近生产，避免"空表跑得飞快"
- **链路全覆盖**：网关 / LB / 应用 / DB / 缓存 / MQ 全程观测
- **热身期**：JIT / 连接池建立；排除启动期抖动

### 9.6 结果解读

发现"平均 100ms 但 P99 是 5s"时：

- 看延迟分布（heatmap）
- 看 GC 停顿 / 慢查询 / 连接池饱和
- 区分"吞吐瓶颈"和"尾部延迟"
- 小心"平均值陷阱"：用户感受是尾部

### 9.7 生产环境性能保护

- **限流 / 熔断 / 降级**
- **排队而非崩溃**（队列缓冲 + 超时拒绝）
- **资源隔离**（线程池、连接池分舱）
- **慢查询告警**
- **灰度先行**，用真实流量验证

### 📝 笔试题 9-1：负载测试与压力测试的最关键区别？

- **负载**：验证"系统在**预期量级**下达标（SLO）"—— 结果是"合格/不合格"
- **压力**：故意**超过**预期，找到**临界点**和**失败模式**—— 结果是"我们能扛到哪里，崩得怎么样，恢复如何"

两者都要做；只做负载测试无法知道过载后的行为。

---

## 10. 混沌工程与韧性测试

### 10.1 思想

> 在受控条件下**主动引入故障**，验证系统是否能按预期降级与恢复。

Netflix 提出，基于四原则：

1. 建立稳态假设（Steady State Hypothesis）
2. 假设**真实**的故障会发生
3. 在**生产**（或类生产）中实验
4. 自动化、持续运行

### 10.2 常见实验

- **杀实例**：随机杀 Pod / 节点
- **网络**：延迟注入、丢包、分区、DNS 故障
- **资源**：CPU/内存/磁盘满、句柄耗尽
- **依赖**：DB 慢查询、缓存宕机、下游返回 5xx
- **时钟**：时钟漂移
- **区域**：模拟整个 AZ 挂掉

### 10.3 工具

- **Chaos Mesh** / **LitmusChaos**：K8s 原生 CR
- **Chaos Monkey** / **Simian Army**：Netflix 祖师
- **AWS Fault Injection Simulator (FIS)**
- **Pumba**：Docker
- **Toxiproxy**：网络故障代理

### 10.4 Chaos Mesh 示例（杀 Pod）

```yaml
apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata: { name: kill-order-pod, namespace: prod }
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces: [prod]
    labelSelectors:
      app: order
  scheduler:
    cron: "@every 10m"
```

### 10.5 Game Day（演练日）

团队约定一天，集中演练：

- 准备剧本（scenarios）
- 值班 on-call 像真实事故一样响应
- 总结 runbook、告警、流程漏洞

### 10.6 边界与风险

- **别在没监控/没回滚能力时做**
- **生产混沌**需要灰度、爆炸半径控制
- **通知相关方**，避免被误以为事故
- **始终可一键停止**（`kubectl delete PodChaos`）

### 📝 笔试题 10-1：混沌工程只能在生产做吗？

不一定。严肃的混沌实践鼓励**最终**在生产跑（因为只有生产才有真实依赖与流量），但**应该先**在测试/预发跑稳；需要有：

- 完整的可观测性
- 熔断/限流/降级
- 回滚能力
- 有限爆炸半径（分片/灰度）

逐步提高信心才能安全地在生产进行。

---

## 11. 安全测试

### 11.1 测试目标

- **漏洞**：SQL 注入、XSS、CSRF、SSRF、RCE、路径穿越、反序列化...
- **认证授权**：越权访问、水平 / 垂直越权、token 泄漏
- **秘密**：代码/镜像/日志里硬编码
- **依赖**：存在已知 CVE 的第三方库
- **配置**：TLS 弱加密、默认密码、开放端口

### 11.2 SAST（静态应用安全测试）

分析**源码**：

- **SonarQube**：通用代码质量+安全
- **Semgrep**：规则友好
- **Checkmarx / Fortify**：商业
- **CodeQL**：GitHub 内置，规则强大
- **Bandit (Python) / Brakeman (Ruby) / spotbugs-fb-contrib (Java)**：语言专用

集成到 CI：每 PR 扫描，阻止含高危的合入。

### 11.3 DAST（动态应用安全测试）

运行中应用黑盒扫描：

- **OWASP ZAP**：开源，可自动化
- **Burp Suite**：渗透测试必备
- **Nikto**：快速 Web 服务器扫描

### 11.4 SCA（软件成分分析）

扫描**第三方依赖**：

- **Snyk**
- **Dependabot / Renovate**
- **OWASP Dependency-Check**
- **Trivy**：镜像 + 依赖
- **Grype**

镜像扫描是 CI 必备（很多安全事件源于 base image CVE）。

### 11.5 Secrets 扫描

防止把密钥提交到仓库：

- **git-secrets**
- **trufflehog**
- **gitleaks**

Pre-commit + CI 双重防护。

### 11.6 渗透测试（Penetration Testing）

#### 11.6.1 定义与定位

**渗透测试**是由专业人员在**授权范围内**，以真实攻击者的视角对目标系统进行模拟攻击，发现并验证**可被利用**的漏洞，评估安全风险。

与自动化扫描（SAST / DAST / SCA）的差异：

- 扫描擅长**发现已知模式**；渗透擅长**发现逻辑 / 业务越权 / 组合利用**
- 扫描是"广撒网"；渗透是"深打井"
- 渗透更贴近**真实损失评估**：不仅"有漏洞"，还证明"可被攻进"

**典型时机**：

- 新系统 / 大版本上线前
- 年度 / 合规审计（PCI-DSS、ISO 27001、等保）
- 重大架构变更后
- 发生安全事件后的根因验证

#### 11.6.2 按"信息披露程度"分类

| 类型 | 测试者已知 | 模拟视角 | 特点 |
|------|------------|----------|------|
| **黑盒（Black-box）** | 几乎无信息 | 外部攻击者 | 最真实，耗时长 |
| **灰盒（Gray-box）** | 部分信息（账号、文档） | 合法用户 / 内部低权限 | 平衡真实度与效率（最常见） |
| **白盒（White-box）** | 源码、架构、凭证 | 内部知情人 | 深度覆盖，类似 code review + 渗透 |

#### 11.6.3 按"目标范围"分类

- **Web 应用渗透**：OWASP 场景为主
- **移动应用渗透**：iOS / Android、逆向、通信链路
- **API 渗透**：REST / GraphQL / gRPC 的越权、枚举、批量
- **网络渗透**：主机、内网、横向移动
- **云环境渗透**：IAM 配置、S3 权限、metadata 服务、容器逃逸
- **无线 / 物理 / 社工**：Wi-Fi、门禁、钓鱼
- **红队行动（Red Team）**：不设范围，端到端模拟 APT

#### 11.6.4 标准流程（PTES 七阶段）

Penetration Testing Execution Standard：

```
1. 前期交互（Pre-engagement）
   - 定义范围、时间窗、联系人、授权书
2. 信息收集（Intelligence Gathering / OSINT）
   - 子域名、端口、指纹、暴露资产
3. 威胁建模（Threat Modeling）
   - 识别高价值资产与攻击面
4. 漏洞分析（Vulnerability Analysis）
   - 扫描 + 手工验证
5. 渗透利用（Exploitation）
   - 实际利用漏洞，取得权限
6. 后渗透（Post-Exploitation）
   - 权限维持、横向移动、数据访问
7. 报告（Reporting）
   - 漏洞清单、风险评级、复现步骤、修复建议
```

#### 11.6.5 常用方法论 & 知识库

- **OWASP WSTG**：Web Security Testing Guide
- **OWASP ASVS**：应用安全验证标准
- **OWASP MASTG**：移动端
- **PTES**：流程标准
- **MITRE ATT&CK**：TTP（战术 / 技术 / 过程）知识库
- **NIST SP 800-115**：技术指导
- **CWE / CVE**：弱点与漏洞编号体系

#### 11.6.6 常用工具

- **综合代理**：**Burp Suite**（行业标配）、**OWASP ZAP**
- **网络 / 端口**：**nmap**、**masscan**、**netcat**
- **框架 / Exploit**：**Metasploit**、**Cobalt Strike**（红队商用）
- **Web 专项**：**sqlmap**（注入）、**Nuclei**（模板化扫描）、**ffuf / gobuster**（目录/子域爆破）、**wpscan**
- **无线 / 网络层**：**Aircrack-ng**、**Wireshark**、**Responder**
- **密码 / 哈希**：**Hashcat**、**John the Ripper**、**Hydra**
- **云安全**：**ScoutSuite**、**Prowler**、**Pacu**（AWS）
- **移动端**：**Frida**、**MobSF**、**Objection**
- **发行版**：**Kali Linux**、**Parrot OS** 集大成

#### 11.6.7 常见渗透场景

- **SQL 注入 / NoSQL 注入 / ORM 注入**
- **XSS**（反射、存储、DOM）
- **CSRF / SSRF**
- **命令注入 / 反序列化 RCE**（Log4Shell、Fastjson、Shiro、Spring4Shell）
- **IDOR**（直接对象引用越权）
- **认证绕过 / JWT 漏洞**（`alg=none`、密钥泄漏、越权 claim）
- **权限越权**（水平 / 垂直）
- **文件上传 / 路径穿越**
- **业务逻辑漏洞**：支付金额篡改、重放、短信轰炸、优惠券薅羊毛
- **云 IAM 错配**：过宽策略、公开 S3、metadata SSRF → STS 凭证
- **容器逃逸**：特权容器、挂载 docker.sock、CAP_SYS_ADMIN
- **供应链攻击**：依赖投毒、镜像后门、CI/CD 凭证泄漏

#### 11.6.8 红队 / 蓝队 / 紫队

| 团队 | 定位 |
|------|------|
| **Red Team（红队）** | 攻方，模拟真实 APT，长期隐蔽；目标通常是关键资产 |
| **Blue Team（蓝队）** | 防守方，监控、响应、取证、加固 |
| **Purple Team（紫队）** | 红蓝协同，实时对抗与改进；从"对抗"转向"共学" |

**钻石模型 / Kill Chain**：用来描述攻击链路，帮助防御方定点部署检测。

#### 11.6.9 漏洞报告的关键要素

一份合格的渗透报告应包含：

- **执行摘要**（面向管理层，非技术）
- **范围与方法**（目标、时间、方式、限制）
- **发现清单**：漏洞名、影响、CVSS 评分、证据（截图 / payload）
- **复现步骤**：精确到请求参数
- **风险评估**：影响 × 可能性
- **修复建议**：具体、可操作，并区分短期缓解 / 长期根治
- **附录**：工具输出、日志、时间线

CVSS 评分（0-10）是常见风险量化方式；超过 7 通常为高危。

#### 11.6.10 合规与法律边界

- **必须书面授权**（Scope of Work / Rules of Engagement）
- **未授权渗透** = 非法入侵（各国均有明确法律处罚）
- **数据保护**：渗透中获取的数据不得外泄，测试后销毁
- **业务影响**：DDoS / 高并发 / 破坏性操作需额外约定
- **紧急停止流程**：出现非预期影响时联系谁、如何回滚
- **合规场景**：PCI-DSS、HIPAA、SOC 2、ISO 27001、GDPR、等级保护常年度要求
- **漏洞披露**：负责任披露（CVD）给厂商修复窗口，再公开

#### 11.6.11 渗透测试 vs 漏洞扫描 vs 红队

| 维度 | 漏洞扫描 | 渗透测试 | 红队 |
|------|----------|----------|------|
| 自动化程度 | 高 | 中（手工为主） | 低 |
| 范围 | 广 | 中（深度） | 窄（目标导向） |
| 是否利用 | 否 | 是 | 是 |
| 持续时间 | 小时 | 周 | 月 |
| 主要产出 | 漏洞清单 | 漏洞利用链 + 修复建议 | 对抗评估 + 防御能力提升 |

#### 11.6.12 本地演练：靶场与 CTF

培养渗透能力的合法途径：

- **DVWA / bWAPP / WebGoat / Mutillidae**：经典 Web 靶场
- **HackTheBox / TryHackMe / PortSwigger Web Security Academy**：交互式训练
- **VulnHub**：下载靶机镜像
- **CTF 比赛**：Pwn / Web / Crypto / Re / Misc
- 内部**红蓝对抗**：安全团队 vs 研发团队，在预发/专用环境进行

### 📝 笔试题 11-2：一次规范的渗透测试应具备哪些关键要素？

1. **书面授权**（Scope + RoE + 紧急联系人）—— 合法性前提
2. **明确范围**：哪些资产/环境/IP 段可以测，哪些禁止
3. **时间窗**：何时开始、何时停止、禁止对生产造成破坏性影响
4. **遵循方法论**：PTES / OWASP WSTG 等
5. **证据可复现**：payload、截图、时间戳
6. **分级评估**：CVSS + 业务影响，不只是"找漏洞"
7. **可执行修复建议**：分短期缓解与长期根治
8. **数据保护与销毁**：测试得到的敏感数据不得外泄
9. **报告闭环**：跟进修复与复测（retest）

### 11.7 OWASP Top 10（2021）

重点知识：

1. Broken Access Control
2. Cryptographic Failures
3. Injection（SQL/NoSQL/Cmd）
4. Insecure Design
5. Security Misconfiguration
6. Vulnerable and Outdated Components
7. Identification and Authentication Failures
8. Software and Data Integrity Failures
9. Security Logging and Monitoring Failures
10. Server-Side Request Forgery

### 📝 笔试题 11-1：CI 里应该加哪些安全测试？

至少四项：

1. **SAST**：代码静态扫描（Semgrep / CodeQL）
2. **SCA**：依赖漏洞扫描（Snyk / Dependabot）
3. **镜像扫描**：Trivy / Grype，在 build 后扫
4. **Secrets 扫描**：gitleaks / trufflehog，pre-commit + CI

额外：基础设施即代码扫描（Checkov / tfsec）、部署后的 DAST。

---

## 12. 可访问性与兼容性测试

### 12.1 可访问性（Accessibility / a11y）

确保残障用户（视障、听障、运动障碍）可以使用产品。

- **WCAG 2.1/2.2** 标准：A / AA / AAA 三级
- 典型要求：色彩对比度、键盘可达、屏幕阅读器语义、表单 label

### 12.2 a11y 工具

- **axe-core** / **@axe-core/playwright**：自动检测
- **Lighthouse**（Chrome DevTools）：综合审计
- **Pa11y**：CLI 扫描
- **WAVE**：浏览器扩展
- **NVDA / JAWS / VoiceOver**：真实屏幕阅读器验证

### 12.3 示例（Playwright + axe）

```javascript
import { test, expect } from '@playwright/test';
import AxeBuilder from '@axe-core/playwright';

test('home page a11y', async ({ page }) => {
  await page.goto('/');
  const results = await new AxeBuilder({ page }).analyze();
  expect(results.violations).toEqual([]);
});
```

### 12.4 兼容性测试

- **浏览器**：Chrome / Firefox / Safari / Edge，以及历史版本
- **分辨率 / DPR**：手机 / 平板 / 桌面 / 4K
- **网络**：4G / 弱网 / 断网
- **OS / 设备**：iOS / Android 各版本
- **国际化**：RTL、字符长度、字体

### 12.5 工具

- **BrowserStack / Sauce Labs / LambdaTest**：云浏览器农场
- **Playwright** 支持 Chromium / Firefox / WebKit 三核
- **Chrome DevTools**：设备模拟、网络 throttle
- **真机** 仍不可完全取代

### 📝 笔试题 12-1：为什么 a11y 测试应该列入 CI？

- 法规要求（美国 ADA、欧盟 EN 301 549、中国《信息无障碍》）
- 用户基数巨大（约 15% 人口有某种功能障碍）
- 问题一旦累积，返工成本高
- 自动化工具能发现大部分低 hanging fruit（ARIA、对比度、标签）

自动扫描是基线，最终仍需手工用屏幕阅读器验证关键用户旅程。

---

## 13. A/B 测试与灰度

### 13.1 A/B 测试是什么

- 随机把用户分成两组：**A（对照）** 和 **B（实验）**
- 对两组做统计比较，验证某改动是否带来可量化提升
- 不是"验证系统对不对"，是"验证产品假设对不对"

### 13.2 关键概念

- **Hypothesis（假设）**：例如"新下单按钮能把转化率提升 ≥ 1%"
- **OEC（Overall Evaluation Criterion）**：主指标（转化率、ARPU、留存）
- **Guardrail 指标**：副指标防止主指标上升但副作用严重（崩溃率 / 客诉）
- **样本量 / 功效（power）**：多少用户 × 多久，保证统计显著
- **置信度 / p 值**：通常 p < 0.05 视为显著
- **SRM（Sample Ratio Mismatch）**：实际分流比 ≠ 期望 → 通常说明分流或数据有问题
- **辛普森悖论**：分总体看结果相反，需按子群分析

### 13.3 典型流程

1. 明确假设、主指标、副指标
2. 估算样本量（G*Power / Evan Miller 在线计算器）
3. 灰度发布：1% → 5% → 50% → 100%
4. 同期对比：**不要做"前后对比"**（其他变量干扰太多）
5. 达到样本量才判定，**不要提前偷看**（multiple testing 问题）
6. 观察 guardrail，异常立即中止
7. 结果：显著正向 → 全量；无显著差异 → 放弃或迭代；显著负向 → 回滚

### 13.4 工具平台

- **自建**：基于 feature flag + 数据仓库
- **LaunchDarkly / Split.io / Optimizely / VWO / Statsig**
- **Google Optimize**（已下线）
- 大厂内部平台（Airbnb ERF、微软 ExP、字节火山引擎）

### 13.5 与灰度发布（Canary）的区别

| | A/B 测试 | 灰度发布（Canary） |
|-|----------|---------------------|
| 目的 | 验证**业务假设** | 验证**技术稳定性** |
| 关注指标 | 业务（转化率、LTV） | 技术（错误率、延迟） |
| 决策 | 数据显著即全量 | 无异常即全量 |
| 持续时长 | 天-周 | 分钟-小时 |
| 同期运行多组 | 是 | 否 |

两者常同时使用：Canary 保证技术安全 → A/B 验证业务效果。

### 📝 笔试题 13-1：上线一周发现 A/B 指标提升 10%，结果显著，能马上全量吗？

先做交叉检查：

- **SRM 检查**：分流比是否符合预期
- **细分群体**：不同地区/渠道/设备是否一致
- **Guardrail 是否恶化**
- **新奇效应**：刚上线的新奇感是否高估效果，多观察几周
- **数据埋点对齐**：是否算错
- **是否多重比较**：同时测 10 个指标，至少 1 个 p<0.05 是概率事件

全部排除后再决定全量。

---

## 14. 自动化测试与 CI/CD 集成

### 14.1 自动化金字塔

```
                    ┌─────────┐
                    │ E2E/UI  │  慢、贵、少
                    ├─────────┤
                    │集成/契约│  中等
                    ├─────────┤
                    │  单元   │  快、多、稳
                    ├─────────┤
                    │ 静态/lint │
                    └─────────┘
```

### 14.2 CI 流水线典型阶段

```
commit → lint + format
       → 静态分析 / SAST / 类型检查
       → 单元测试 + 覆盖率
       → 构建（编译 / 打包镜像）
       → 镜像扫描
       → 集成测试（Testcontainers）
       → 契约测试
       → 部署到 staging
       → E2E 冒烟
       → 手动 / 自动 approve → 生产发布
```

### 14.3 GitHub Actions 片段

```yaml
name: ci
on: [pull_request, push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci
      - run: npm run lint
      - run: npm test -- --coverage
      - uses: codecov/codecov-action@v4
      - name: e2e smoke
        run: npx playwright test --grep @smoke

  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          severity: 'HIGH,CRITICAL'
          exit-code: '1'
```

### 14.4 本地开发循环

- **Pre-commit 钩子**：lint / format / 单元（仅相关）
- **Watch 模式**：`jest --watch`、`pytest-watch`，改一保一跑
- **Testcontainers 快速启动**：本地即可跑集成
- **`docker compose up`** 一键拉起依赖

### 14.5 并行化与分片

- **按文件分片**：`--shard=1/4` / `--num-machines`
- **按用例 / 按标签**：`@smoke`、`@slow`
- **测试影响分析（TIA）**：按改动映射到受影响用例

### 14.6 稳定性治理

- **Flaky 测试隔离**：检测到连续不稳定 → 自动打标、禁用、创建 ticket
- **重试策略**：最多重试 2 次；**每次重试都要记录**，不要静默掩盖
- **测试用例红榜**：最慢 / 最常失败 top 10，定期优化

### 📝 笔试题 14-1：CI 跑得太慢怎么办？

分层处理：

1. **分层执行**：快速反馈层（lint/单元）秒级；重层（集成/E2E）并行
2. **并行分片**
3. **缓存**：依赖（node_modules、maven）、Docker layer
4. **TIA**：只跑受影响用例
5. **慢测试**优化或分桶
6. **并行构建多镜像**
7. **移除无价值测试**：高维护成本且很少发现问题的

---

## 15. 测试数据、Mock 与夹具

### 15.1 测试数据策略

- **每测试生成**：函数/工厂造数据，独立干净
- **夹具（Fixtures）**：共用基础数据
- **脱敏生产副本**：只在必要时用，处理 PII
- **合成数据**：按分布造假数据（faker / mimesis）

### 15.2 工厂模式

```python
# factory_boy
import factory
from app.models import User

class UserFactory(factory.Factory):
    class Meta: model = User
    name = factory.Faker('name')
    email = factory.Faker('email')
    age = factory.Faker('random_int', min=18, max=80)

u = UserFactory()
admin = UserFactory(role='admin')
```

```java
// Java: 自写 ObjectMother 或用 instancio
User u = Instancio.create(User.class);
User admin = Instancio.of(User.class).set(field("role"), "admin").create();
```

### 15.3 数据库清理策略

- **事务回滚**：测试跑在事务里，结束 rollback（快但限制并发）
- **Truncate 表**：每测试/每类前清
- **每测试独立 schema**：最干净，成本高
- **Testcontainers 每类重建**：天然隔离

### 15.4 Mock / Stub 服务

- **WireMock / MockServer**：HTTP 接口回放
- **Mountebank**：多协议 mock
- **Prism**：基于 OpenAPI 自动 mock
- **Mocks.js / nock**（Node）
- **responses**（Python）

示例（Python + responses）：

```python
import responses, requests

@responses.activate
def test_charge():
    responses.add(responses.POST,
                  "https://api.pay/charge",
                  json={"id":"c1","status":"ok"}, status=200)
    r = requests.post("https://api.pay/charge", json={"amount":100})
    assert r.json()["status"] == "ok"
```

### 15.5 时间与随机

- 测试里**不要直接 `new Date() / time.now()`**
- 注入时钟：`Clock.fixed(...)`、libfaketime、`freezegun`
- 种子化随机：`random.seed(42)`
- 让"昨天/下周"在任何时区/闰秒都稳定

### 15.6 快照测试（Snapshot）

```javascript
expect(component.toJSON()).toMatchSnapshot();
```

利弊：

- 优点：不用写断言
- 缺点：随意 `--updateSnapshot` 容易让测试失去意义

**仅当输出稳定、评审严格时**才用。

### 📝 笔试题 15-1：共享一份生产脱敏数据做测试有什么坑？

- **隐私合规**：脱敏不彻底仍可能泄露
- **环境差异**：数据分布可能变化导致测试结果随时间波动
- **用例间耦合**：并发跑可能互相踩数据
- **数据演化**：schema 升级时脱敏数据可能失效
- **难以表达边界**：造特定边界场景不方便

倾向：**每测试小数据 + 工厂造 + 必要时小量脱敏样本** 的组合。

---

## 16. 测试覆盖率与质量度量

### 16.1 覆盖率类型

- **行覆盖率 (Line)**：最常见
- **分支覆盖率 (Branch)**：if/else 两边
- **函数覆盖率 (Function)**
- **条件覆盖率 / MC/DC**：严苛行业

### 16.2 工具

- Java：**JaCoCo**
- JS/TS：**Istanbul / c8**（Jest 内置）
- Python：**coverage.py / pytest-cov**
- Go：`go test -cover`
- 聚合：**Codecov / Coveralls / SonarCloud**

### 16.3 正确看待覆盖率

- 覆盖率**不等于**测试质量
- 70-80% 分支覆盖是务实目标，100% 追求代价极高
- 更要关心：**关键路径**是否覆盖、**断言是否有意义**
- 只增不减：**新增代码覆盖率不低于 80%** 的 Ratchet 策略

### 16.4 变异测试（Mutation Testing）

更严格地评价测试质量：工具对代码做小改动（变异），看测试是否能 catch：

- **PIT**（Java）
- **Stryker**（JS / TS / Scala / C#）
- **mutmut**（Python）

"mutation score" 低说明测试只跑了代码但没真正断言。

### 16.5 质量度量综合指标

- **缺陷逃逸率**：生产缺陷 / 总缺陷
- **测试运行时长**：CI 快速才频繁
- **测试稳定性**：flaky 率
- **修复耗时（MTTR）**
- **发布频率 & 失败率**：DORA 四指标之一
- **代码审查通过率与耗时**

### 📝 笔试题 16-1：团队覆盖率 95%，但仍然频繁出 bug，可能原因？

- **断言无效**：`assertTrue(true)` 类测试刷数字
- **只覆盖主路径**：边界、异常分支漏
- **测试依赖实现细节**：代码改一下就过，行为改了却还过
- **集成问题**：单元覆盖高但跨服务 / DB / 时序问题没测
- **环境差异**：测试与生产配置不一致
- **非功能性缺陷**：性能、并发、安全不在覆盖率视野

可引入 **变异测试**与 **测试用例评审**；把覆盖率从"指标"降级为"辅助"。

---

## 17. 流程、缺陷管理与组织

### 17.1 缺陷生命周期

```
New → Assigned → In Progress → Fixed → Verified → Closed
              ↘ Rejected / Won't Fix / Duplicate
              ↘ Reopened
```

### 17.2 严重度 vs 优先级

- **Severity（严重度）**：技术层面影响（崩溃？数据丢失？）
- **Priority（优先级）**：业务层面紧急度（下周发版 / 影响多少用户）

二者独立：可能是 "高严重度但低优先级"（罕见崩溃），反之亦然。

### 17.3 好的缺陷报告

- 复现步骤（精确、最小）
- 实际结果 vs 期望结果
- 环境、版本、浏览器/设备
- 日志 / 截图 / 视频 / trace_id
- 影响范围
- 规避方法（如有）

### 17.4 测试策略文档

项目级别的测试策略（Test Strategy）通常包含：

- 范围与目标
- 测试分层与覆盖类型
- 环境与数据
- 入/出准则（Entry/Exit Criteria）
- 工具与框架
- 角色与责任
- 自动化范围
- 风险与缓解

### 17.5 测试角色演进

- 传统：独立 QA 团队，开发 → 测试 → 发布
- 现代：
  - **SDET（Software Development Engineer in Test）**：工程能力的 QA，写自动化 + 框架
  - **Quality Engineer**：关注质量，引导开发者写好测试
  - **全员质量**：开发对测试负责，QA 赋能而非"把关"

### 17.6 流程模型

- **V 模型**：需求 - 设计 - 编码 对应 验收 - 集成 - 单元，自下而上验证
- **敏捷 / Scrum**：每 sprint 小闭环，测试贯穿开发
- **BDD（行为驱动开发）**：Gherkin 语法 + Cucumber/SpecFlow/Behave
- **TDD（测试驱动开发）**：Red → Green → Refactor
- **ATDD**：验收测试驱动

### 17.7 Shift Left & Shift Right

- **Shift Left**：把测试左移到开发早期（lint / 单元 / 契约 / PR review / 静态分析）
- **Shift Right**：把验证延伸到生产（灰度、金丝雀、合成监控、生产 A/B、混沌）
- 现代团队两者兼有

### 📝 笔试题 17-1：TDD 的三步循环是什么？为什么这么排序？

1. **Red**：先写一个**会失败**的测试（表达需求）
2. **Green**：写最少代码让测试通过
3. **Refactor**：重构代码质量

顺序的意义：
- 先 Red 确保测试**真的在测试某行为**（不小心写了"永远通过"会被抓出）
- 先 Green 快速给到基线
- 有测试兜底的 Refactor 才安全

---

## 18. 反模式与最佳实践

### 18.1 常见反模式

❌ **Ice Cream Cone**：少单元、多 UI（金字塔倒置）
❌ **Flaky 容忍**：失败就重试掩盖，不查根因
❌ **共享可变夹具**：用例间互相污染
❌ **Sleep 驱动**：用 `sleep(N)` 代替条件等待
❌ **测试实现细节**：私有方法、内部状态
❌ **Mock 一切**：测试通过但集成全错
❌ **断言太宽泛** 或 **断言太具体**（稍改就挂）
❌ **覆盖率目标主义**：凑数刷行覆盖
❌ **巨型测试**：一个 case 几百行、多处断言不同主题
❌ **条件断言**：`if (cond) assert …`，测试逻辑本身 bug 隐藏
❌ **环境依赖**：只能在某同学电脑上跑过
❌ **测试无备份**：只有一人懂，离职即遗产

### 18.2 最佳实践清单

- ✅ 单元 / 集成 / E2E 分层，各司其职
- ✅ 测试命名表达意图：`should_XYZ_when_ABC`
- ✅ 一个测试一个主题，一两个断言
- ✅ 确定性 > 真实度：使用固定时钟/随机种子
- ✅ Testcontainers 代替共享环境
- ✅ 契约测试代替"联调"
- ✅ 关键路径上 smoke + synthetic
- ✅ Flaky 零容忍，发现即隔离修复
- ✅ 覆盖率作参考，变异测试作辅证
- ✅ 生产观测到的缺陷 → 回推补一个自动化回归
- ✅ 测试代码与生产代码**一视同仁**：走 code review、重构、设计模式

### 18.3 常用命名与组织约定

- 测试文件就近放：`src/foo.ts` + `src/foo.test.ts`
- 标记分层：`@Unit`、`@Integration`、`@E2E`、`@Slow`、`@Smoke`
- 构建任务分层：`mvn test` / `mvn verify` / `mvn integration-test`
- Makefile 或 package.json 聚合：`test`, `test:int`, `test:e2e`, `test:smoke`

### 📝 笔试题 18-1：用例命名是否重要？举两个好例子。

非常重要——命名即**文档**与**失败报告**。好例子：

- `shouldRefundFullAmount_whenOrderCancelledWithin24h`
- `throwsInvalidCouponException_whenCouponExpired`

失败时一眼看出**被测行为与上下文**，不需要去读断言。避免 `test1`、`testOrder`、`shouldWork`。

---

## 19. 综合笔试练习

### 19.1 选择题

**Q1** 测试金字塔倡导的测试数量比例是？
A. E2E > 集成 > 单元
B. 单元 > 集成 > E2E
C. 三层相等
D. 没有规定

<details><summary>答案</summary>B。</details>

**Q2** 下列哪个不是性能测试的子类？
A. Load  B. Stress  C. Smoke  D. Spike

<details><summary>答案</summary>C。Smoke 是功能冒烟。</details>

**Q3** 关于冒烟测试，正确的是？
A. 发现所有缺陷
B. 范围窄，验证基本可用
C. 等价于回归测试
D. 仅手工

<details><summary>答案</summary>B。</details>

**Q4** 契约测试最大好处？
A. 完全避免集成测试
B. 保证消费者与生产者接口兼容，降低联调成本
C. 加密传输
D. 提升单元覆盖率

<details><summary>答案</summary>B。</details>

**Q5** 下列哪项**不是**单元测试的 FIRST 原则？
A. Fast  B. Independent  C. Final  D. Timely

<details><summary>答案</summary>C。</details>

**Q6** 关于 A/B 测试，错误的是？
A. 随机分流两组对照
B. 达到样本量再判定
C. 前后对比也等价于 A/B
D. 需要 guardrail 指标

<details><summary>答案</summary>C。</details>

**Q7** 下列关于测试覆盖率的说法正确的是？
A. 100% 覆盖率等于无 bug
B. 覆盖率越高，测试质量越高
C. 覆盖率是辅助指标，断言质量更重要
D. 覆盖率不需要度量

<details><summary>答案</summary>C。</details>

**Q8** 下列哪种策略最能加速 CI 执行？
A. 串行执行所有测试
B. 分层 + 并行 + 缓存 + 测试影响分析
C. 去掉单元测试
D. 每次清空 Docker 缓存

<details><summary>答案</summary>B。</details>

### 19.2 判断题

1. 压力测试就是负载测试的别名。 ❌
2. 单元测试应该不依赖数据库。 ✅
3. E2E 测试越多越好。 ❌
4. 契约测试可以替代所有集成测试。 ❌
5. A/B 测试能验证技术稳定性。 ❌（那是灰度）
6. 混沌工程必须在生产做。 ❌
7. Flaky 测试可以靠重试解决。 ❌（治标不治本）
8. 变异测试可以评估测试本身的质量。 ✅

### 19.3 简答题

**Q1** 请描述从 commit 到发布的完整测试链路，每层的目的与代表工具。

1. **静态层**：lint（ESLint / Checkstyle）+ 类型检查 + SAST
2. **单元**：JUnit / pytest / Jest；毫秒级
3. **集成**：Testcontainers 起 DB/Redis/Kafka
4. **契约**：Pact；消费者与生产者共享契约
5. **构建 + 扫描**：Docker + Trivy / Grype
6. **部署 staging**
7. **E2E 冒烟**：Playwright / Cypress，关键旅程
8. **性能基线**：k6 / JMeter 对比历史
9. **手动 / 自动审批 → 灰度 → 全量**
10. **生产**：Synthetic monitoring + A/B + 混沌

**Q2** 性能测试怎么定 SLO 与报告？

- 明确场景（读 / 写 / 混合）与目标吞吐
- 定义 SLI：P95 / P99 延迟、错误率、吞吐
- 定 SLO：例如 P99 < 300ms、错误率 < 0.1%、吞吐 ≥ 1000 TPS
- 使用 k6 脚本运行，多阶段（ramp up / 稳态 / spike）
- 报告：吞吐曲线、延迟分布、资源（CPU/GC/DB）、对比上版本基线
- 不达标：给出瓶颈分析（DB 慢查询 / GC / 网络）并提改进项

**Q3** 举一个 A/B 测试失败的常见原因。

**SRM**（分流比不一致）：期望 50:50，实际 48:52，说明分流逻辑或数据采集有 bug，再漂亮的指标也不可信；应先修数据管道。其他常见坑：样本量不足就停、多重比较、选择偏差、季节性混淆。

### 19.4 设计题

**Q1** 为一个电商订单服务设计完整测试方案。

- **单元**：
  - 价格计算、促销规则、折扣叠加、状态机流转
  - 纯逻辑函数 100% 覆盖；边界 / 异常全覆盖
- **集成**：
  - Testcontainers：Postgres + Redis + Kafka
  - `OrderRepository` CRUD、索引、并发扣减库存
  - Outbox Publisher 对 Kafka 投递
- **契约**：
  - 消费者（App / 运营后台）编写 Pact
  - Provider 侧 CI 验证
- **E2E 冒烟**：
  - 登录 → 下单 → 支付 → 查看订单
  - 生产预发跑；失败阻断
- **性能**：
  - k6：下单 P99 < 300ms，100 TPS 稳态、300 TPS 尖峰
  - 耐久：稳态 8 小时无内存泄漏
- **混沌**：
  - Chaos Mesh：kill 1 Pod 不应产生错单
  - DB 故障 → 订单写入走 outbox 重试
- **安全**：
  - SAST / SCA / 镜像扫描
  - 支付签名、防重放、金额篡改
- **a11y / i18n**：
  - 下单页面屏幕阅读器、RTL
- **灰度 + A/B**：
  - 支付按钮文案 A/B
  - 新版本灰度 1% → 10% → 100%

**Q2** 老项目回归测试每次跑 4 小时，你怎么优化到 30 分钟？

1. **分层**：哪些测试其实可以放到单元？拆出去
2. **并行分片**：CI 运行 N 台 × 并行
3. **TIA**：按改动路径选相关用例
4. **缓存**：依赖与 Docker layer
5. **去重**：重复场景合并
6. **移除无价值用例**：覆盖率与价值审计，砍掉"永远绿又没发现过问题"的
7. **慢用例治理**：top 20 最慢定向优化（索引 / 数据 / 并行）
8. **E2E 瘦身**：保留冒烟套件；全量移到夜间
9. **测试数据工厂化**：避免 seed 全库

---

## 📚 学习建议

1. **读《Google Testing Blog》与 "Test Desiderata"**（Kent Beck）
2. **书单**：
   - 《Growing Object-Oriented Software, Guided by Tests》
   - 《Working Effectively with Legacy Code》
   - 《The Art of Unit Testing》
   - 《Software Engineering at Google》测试章节
3. **每周一小时**：重构一个测试，优化命名与稳定性
4. **出 bug 必补测试**：建立"回归 ticket"习惯
5. **关注 DORA 指标**：部署频率 / 变更失败率 / MTTR / 前置时间
6. **工具是手段**：最终还是 **人 + 流程 + 文化** 决定质量

> 祝你的测试又快又稳，代码自带安全网。
